# SyntheticShared API Documentation

_Auto-generated by generate_docs.py. Do not edit namespace or class headers manually._


## Namespace: `Synthetic.Core`

### Class: `App`
**File:** [src/SyntheticShared/Core/App.cs](../src/SyntheticShared/Core/App.cs)

#### Methods
##### `ConfigCollection`
```csharp
public static ConfigCollection Configurations = new ConfigCollection()
```
Gets the configuration collection managing app and project settings.

##### `OnStartup`
```csharp
public Result OnStartup(UIControlledApplication appControlled)
```
Called when Revit starts up. Initializes the ribbon UI and registers document event handlers.

##### `OnShutdown`
```csharp
public Result OnShutdown(UIControlledApplication appControlled)
```
Called when Revit shuts down. Performs any necessary cleanup.

##### `OnDocumentOpen`
```csharp
public void OnDocumentOpen(object? sender, Autodesk.Revit.DB.Events.DocumentOpenedEventArgs args)
```
Event handler triggered when a Revit document is opened. Reads and registers the project configuration for the opened document.

##### `OnDocumentClose`
```csharp
public void OnDocumentClose(object? sender, Autodesk.Revit.DB.Events.DocumentClosingEventArgs args)
```
Event handler triggered when a Revit document is closing. Removes the project configuration for the closing document.

#### Fields
- **`IExternalApplication`**: `public class App : IExternalApplication {`
  *Description:* The main external application class for the Revit addon. Manages the application lifecycle, ribbon UI creation, and document events.
- **`AppControlled`**: `public static UIControlledApplication? AppControlled;`
  *Description:* Gets or sets the UIControlledApplication instance for the application session.

---
### Class: `SyntheticRibbon`
**File:** [src/SyntheticShared/Core/SyntheticRibbon.cs](../src/SyntheticShared/Core/SyntheticRibbon.cs)

#### Methods
##### `Create`
```csharp
public static void Create (UIControlledApplication appControlled, string path)
```
Creates a new Ribbon Tab for the App along with panels and buttons.

#### Fields
- **`SyntheticRibbon`**: `public class SyntheticRibbon {`
  *Description:* Creation and management class for the Synthetic Revit ribbon interface.
- **`TabName`**: `public const string TabName`
  *Description:* The Ribbon Tab Name.
- **`PanelName`**: `public const string PanelName`
  *Description:* The Ribbon Panel Name.

---
## Namespace: `Synthetic.Infrastructure.Diagnostics`

### Class: `StorageDelete`
**File:** [src/SyntheticShared/Infrastructure/Diagnostics/StorageDelete.cs](../src/SyntheticShared/Infrastructure/Diagnostics/StorageDelete.cs)

#### Methods
##### `Execute`
```csharp
public Result Execute(ExternalCommandData commandData, ref string message, ElementSet elements)
```
Executes the extensible storage deletion command.

#### Fields
- **`IExternalCommand`**: `public class StorageDelete : IExternalCommand {`
  *Description:* Deletes all extensible storage created by any application all active documents. This command will also report if there is no storage in the active document to delete. The document must be saved after the storage is deleted to commit the deletion.

---
### Class: `StorageQuery`
**File:** [src/SyntheticShared/Infrastructure/Diagnostics/StorageQuery.cs](../src/SyntheticShared/Infrastructure/Diagnostics/StorageQuery.cs)

#### Methods
##### `Execute`
```csharp
public Result Execute(ExternalCommandData commandData, ref string message, ElementSet elements)
```
Execute a Revit Command

#### Fields
- **`IExternalCommand`**: `public class StorageQuery : IExternalCommand {`
  *Description:* Checks to see if any extensible storage in the document exists and displays elements containing storage to the user.

---
## Namespace: `Synthetic.Infrastructure.IO`

### Class: `Excel`
**File:** [src/SyntheticShared/Infrastructure/IO/Excel.cs](../src/SyntheticShared/Infrastructure/IO/Excel.cs)

#### Methods
##### `Excel`
```csharp
public Excel(string path)
```
Initializes a new instance of the Excel class with a file path.

##### `Excel`
```csharp
public Excel(string path, string? worksheetName)
```
Initializes a new instance of the Excel class with a file path and worksheet name.

##### `ReadExcel`
```csharp
public List<List<object>>? ReadExcel()
```
Reads the spreadsheet and returns cell contents as a 2D list.

##### `WorkSheetNames`
```csharp
public List<string> WorkSheetNames ()
```
Retrieves the list of worksheet names from the Excel workbook.

#### Fields
- **`Excel`**: `public class Excel {`
  *Description:* Helper class to read data from Microsoft Excel spreadsheets using Interop.
- **`path`**: `public string path`
  *Description:* Gets or sets the file path to the Excel workbook.
- **`worksheetName`**: `public string? worksheetName`
  *Description:* Gets or sets the name of the worksheet to read.
- **`cells`**: `public List<List<object>>? cells`
  *Description:* Gets or sets the cell values read from the spreadsheet, stored as a 2D list of objects.

---
### Class: `FileUtil`
**File:** [src/SyntheticShared/Infrastructure/IO/FileUtil.cs](../src/SyntheticShared/Infrastructure/IO/FileUtil.cs)

#### Methods
##### `CopyFiles`
```csharp
public static Dictionary<string, Dictionary<string,object>> CopyFiles (List<string> filePaths, string newPathRoot, List<string>? rootNames = null, bool overwrite = true)
```
Copies a list of files to a new root directory, optionally preserving directory sub-structures and searching alternate paths.

##### `SeparatePath`
```csharp
public static string SeparatePath (string? filePath, List<string> rootNames)
```
Given a file path, returns the partial path after one of the root names. If a root isn't found, it returns null.

#### Fields
- **`FileUtil`**: `public class FileUtil {`
  *Description:* Utility methods for file operations, alternate path searches, and file organization.

---
### Class: `SearchPaths`
**File:** [src/SyntheticShared/Infrastructure/IO/SearchPaths.cs](../src/SyntheticShared/Infrastructure/IO/SearchPaths.cs)

#### Methods
##### `SearchPaths`
```csharp
public SearchPaths()
```
Creates a new SearchPaths object without any paths or a file library.

##### `SearchPaths`
```csharp
public SearchPaths(List<string> Paths)
```
Creates a new search path object with a library of all the unique files and their paths. Only the first instance of a filename is included so the order of the Paths give priority.

##### `AddPaths`
```csharp
public SearchPaths AddPaths(List<string> paths)
```
Adds additional paths and files to an existing SearchPaths object.

##### `GetFilePath`
```csharp
public string? GetFilePath(string File)
```
Searches the paths for a file and returns if path if found, otherwise returns null.

##### `GetRelativeFilePath`
```csharp
public string? GetRelativeFilePath(string File)
```
Searches the paths for a file and returns a relative path if found, otherwise returns null.

##### `ContainsFile`
```csharp
public bool ContainsFile(string File)
```
Checks if the FileLibrary contains the file.

##### `ContainsPath`
```csharp
public bool ContainsPath(string Path)
```
Checks if the given path is a search path.

##### `CopyFiles`
```csharp
public IDictionary CopyFiles(List<string> Files, string Path, bool Overwrite = false)
```
Copies the files given that are found in the Search Path File Library to a new location.

##### `_GetFileLibrary`
```csharp
private Dictionary<string, string> _GetFileLibrary(List<string> Paths)
```
Gets all the files in the search paths. If more than one file has the same name, then only the first file found will be included. This gives priority to paths listed first in the SearchPaths.

##### `_AddFileLibrary`
```csharp
private Dictionary<string, string> _AddFileLibrary(List<string> Paths, Dictionary<string, string> fileLibrary)
```
Gets all the files in the search path and if they have a unique name, adds them to the fileLibrary Dictionary

##### `ToString`
```csharp
public override string ToString()
```
Prints the Searchpaths as a string including all the files in the library.

#### Fields
- **`SearchPaths`**: `public class SearchPaths {`
  *Description:* SearchPaths are utility functions that search recursively through a list of paths for a list of files and provides the paths of the found files and a list of files not found.
- **`Paths`**: `public List<string> Paths {`
  *Description:* List of Paths to be searched.
- **`FileLibrary`**: `public Dictionary<string, string> FileLibrary { get { return fileLibrary; } }`
  *Description:* List of files in the search path. Only one unique file name in all the paths is included.

---
## Namespace: `Synthetic.Infrastructure.Serialization`

### Class: `Json`
**File:** [src/SyntheticShared/Infrastructure/Serialization/Json.cs](../src/SyntheticShared/Infrastructure/Serialization/Json.cs)

#### Methods
##### `Encode`
```csharp
public static string Encode(System.Object @object)
```
Serializes an element into JSON.

##### `EncodeMinimal`
```csharp
public static string EncodeMinimal(System.Object @object)
```
Serializes an object to JSON string with no indentation (minimal formatting).

##### `Decode`
```csharp
public static System.Object? Decode(string json)
```
Deserializes an element from JSON.

##### `ListToJSON`
```csharp
public static string ListToJSON(List<System.Object> ListJSON)
```
Converts a list of objects to an indented JSON string.

##### `JsonToList`
```csharp
public static object? JsonToList(string JSON)
```
Deserializes a JSON string into a general object list structure.

#### Fields
- **`Json`**: `public class Json {`
  *Description:* Convert Revit Elements to and from JSON.

---
## Namespace: `Synthetic.Modules.AutoTagger.Commands`

### Class: `CmdBatchTag`
**File:** [src/SyntheticShared/Modules/AutoTagger/Commands/CmdBatchTag.cs](../src/SyntheticShared/Modules/AutoTagger/Commands/CmdBatchTag.cs)

#### Methods
##### `Execute`
```csharp
public Result Execute(ExternalCommandData commandData, ref string message, ElementSet elements)
```
Executes the batch tagging command.

#### Fields
- **`IExternalCommand`**: `public class CmdBatchTag : IExternalCommand {`
  *Description:* Revit external command to batch tag elements in the active view or a selection. Matches elements to saved tag templates and places tags accordingly.

---
### Class: `CmdManageTemplates`
**File:** [src/SyntheticShared/Modules/AutoTagger/Commands/CmdManageTemplates.cs](../src/SyntheticShared/Modules/AutoTagger/Commands/CmdManageTemplates.cs)

#### Methods
##### `Execute`
```csharp
public Result Execute(ExternalCommandData commandData, ref string message, ElementSet elements)
```
Executes the template manager command.

#### Fields
- **`IExternalCommand`**: `public class CmdManageTemplates : IExternalCommand {`
  *Description:* Revit external command to manage tag templates. Provides a user interface to view, create, edit, or delete tag templates.

---
### Class: `CmdSetTemplate`
**File:** [src/SyntheticShared/Modules/AutoTagger/Commands/CmdSetTemplate.cs](../src/SyntheticShared/Modules/AutoTagger/Commands/CmdSetTemplate.cs)

#### Methods
##### `Execute`
```csharp
public Result Execute(ExternalCommandData commandData, ref string message, ElementSet elements)
```
Executes the set template command.

#### Fields
- **`IExternalCommand`**: `public class CmdSetTemplate : IExternalCommand {`
  *Description:* Revit external command to set a tag template. Prompts the user to select a host element and its associated tag, then registers the configuration.

---
## Namespace: `Synthetic.Modules.AutoTagger.Models`

### Class: `TagTemplate`
**File:** [src/SyntheticShared/Modules/AutoTagger/Models/TagTemplate.cs](../src/SyntheticShared/Modules/AutoTagger/Models/TagTemplate.cs)

#### Methods
##### `Id`
```csharp
public Guid Id
```
Gets or sets the unique identifier of the tag template.

#### Fields
- **`TagTemplate`**: `public class TagTemplate {`
  *Description:* Data Transfer Object (DTO) representing a user-defined tag offset template.
- **`TemplateName`**: `public string TemplateName`
  *Description:* Gets or sets the name of the template.
- **`TargetCategory`**: `public string TargetCategory`
  *Description:* Gets or sets the target category name.
- **`TargetFamily`**: `public string? TargetFamily`
  *Description:* Gets or sets the target family name.
- **`TargetType`**: `public string? TargetType`
  *Description:* Gets or sets the target type name.
- **`OffsetX`**: `public double OffsetX`
  *Description:* Gets or sets the relative X offset in decimal feet.
- **`OffsetY`**: `public double OffsetY`
  *Description:* Gets or sets the relative Y offset in decimal feet.
- **`OffsetZ`**: `public double OffsetZ`
  *Description:* Gets or sets the relative Z offset in decimal feet.
- **`Orientation`**: `public TagOrientation Orientation`
  *Description:* Gets or sets the orientation of the tag.
- **`AllowOrientationChange`**: `public bool AllowOrientationChange`
  *Description:* Gets or sets a value indicating whether orientation change is allowed.
- **`HostHandX`**: `public double HostHandX`
  *Description:* Gets or sets the X component of the host's hand orientation vector.
- **`HostHandY`**: `public double HostHandY`
  *Description:* Gets or sets the Y component of the host's hand orientation vector.
- **`HostHandZ`**: `public double HostHandZ`
  *Description:* Gets or sets the Z component of the host's hand orientation vector.

---
## Namespace: `Synthetic.Modules.AutoTagger.Repositories`

### Class: `TemplateStorageRepository`
**File:** [src/SyntheticShared/Modules/AutoTagger/Repositories/TemplateStorageRepository.cs](../src/SyntheticShared/Modules/AutoTagger/Repositories/TemplateStorageRepository.cs)

#### Methods
##### `GetTemplates`
```csharp
public List<TagTemplate> GetTemplates(Document doc)
```
Retrieves all templates saved within the active Revit Document.

##### `SaveTemplate`
```csharp
public void SaveTemplate(Document doc, TagTemplate template)
```
Saves a template to the Revit Document. Creates the DataStorage element if it does not exist.

##### `ExportToJson`
```csharp
public void ExportToJson(List<TagTemplate> templates, string filePath)
```
Exports a list of templates to a local JSON file.

##### `ImportFromJson`
```csharp
public List<TagTemplate> ImportFromJson(string filePath)
```
Imports a list of templates from a local JSON file.

##### `DeleteTemplate`
```csharp
public void DeleteTemplate(Document doc, Guid templateId)
```
Deletes a specific template from the Extensible Storage.

##### `SaveAllTemplates`
```csharp
public void SaveAllTemplates(Document doc, List<TagTemplate> newTemplates)
```
Bulk saves templates, merging them with existing templates by ID.

#### Fields
- **`TemplateStorageRepository`**: `public class TemplateStorageRepository {`
  *Description:* Handles all CRUD operations for TagTemplates, abstracting Revit Extensible Storage and local JSON file I/O.

---
## Namespace: `Synthetic.Modules.AutoTagger.ViewModels`

### Class: `ConflictItem`
**File:** [src/SyntheticShared/Modules/AutoTagger/ViewModels/ResolveConflictsViewModel.cs](../src/SyntheticShared/Modules/AutoTagger/ViewModels/ResolveConflictsViewModel.cs)

#### Methods
##### `Candidates`
```csharp
public List<TagTemplate> Candidates
```
Gets or sets the list of candidates to resolve the conflict.

##### `ResolveConflictsViewModel`
```csharp
public ResolveConflictsViewModel(List<ConflictItem> conflicts)
```
Initializes a new instance of the `ResolveConflictsViewModel` class.

#### Fields
- **`ViewModelBase`**: `public class ConflictItem : ViewModelBase {`
  *Description:* Represents an item with a naming conflict and its resolution options.
- **`DisplayName`**: `public string DisplayName`
  *Description:* Gets or sets the display name of the conflicted item.
- **`UniqueKey`**: `public string UniqueKey`
  *Description:* Gets or sets the unique key of the conflicted item.
- **`SelectedCandidate`**: `public TagTemplate? SelectedCandidate {`
  *Description:* Gets or sets the selected candidate chosen to resolve the conflict.
- **`ViewModelBase`**: `public class ResolveConflictsViewModel : ViewModelBase {`
  *Description:* ViewModel for resolving template conflicts.
- **`Conflicts`**: `public ObservableCollection<ConflictItem> Conflicts`
  *Description:* Gets the collection of conflict items.
- **`Proceeded`**: `public bool Proceeded { get; private set; }`
  *Description:* Gets a value indicating whether the user proceeded with resolving the conflicts.
- **`ProceedCommand`**: `public ICommand ProceedCommand`
  *Description:* Gets the proceed command.
- **`CancelCommand`**: `public ICommand CancelCommand`
  *Description:* Gets the cancel command.
- **`CloseAction`**: `public Action? CloseAction`
  *Description:* Gets or sets the close action for the window.

---
### Class: `ManageTemplatesViewModel`
**File:** [src/SyntheticShared/Modules/AutoTagger/ViewModels/ManageTemplatesViewModel.cs](../src/SyntheticShared/Modules/AutoTagger/ViewModels/ManageTemplatesViewModel.cs)

#### Methods
##### `Templates`
```csharp
public ObservableCollection<TagTemplate> Templates
```
Gets or sets the collection of tag templates.

##### `ManageTemplatesViewModel`
```csharp
public ManageTemplatesViewModel(Document doc, IFileDialogService? fileDialogService = null, IUserPromptService? userPromptService = null)
```
Initializes a new instance of the `ManageTemplatesViewModel` class.

#### Fields
- **`ManageTemplatesAction`**: `public enum ManageTemplatesAction {`
  *Description:* Specifies the actions that can be requested when managing templates.
- **`ViewModelBase`**: `public class ManageTemplatesViewModel : ViewModelBase {`
  *Description:* ViewModel controlling the CRUD interface and state loop for managing templates.
- **`SelectedTemplate`**: `public TagTemplate? SelectedTemplate {`
  *Description:* Gets or sets the currently selected template.
- **`RequestedAction`**: `public ManageTemplatesAction RequestedAction { get; private set; }`
  *Description:* Gets the action requested by the view model.
- **`TemplateToEdit`**: `public TagTemplate? TemplateToEdit { get; private set; }`
  *Description:* Gets the template selected for editing.
- **`DeleteCommand`**: `public System.Windows.Input.ICommand DeleteCommand`
  *Description:* Gets the delete template command.
- **`ExportJsonCommand`**: `public System.Windows.Input.ICommand ExportJsonCommand`
  *Description:* Gets the export templates to JSON command.
- **`ImportJsonCommand`**: `public System.Windows.Input.ICommand ImportJsonCommand`
  *Description:* Gets the import templates from JSON command.
- **`EditCommand`**: `public System.Windows.Input.ICommand EditCommand`
  *Description:* Gets the edit selected template command.
- **`NewTemplateCommand`**: `public System.Windows.Input.ICommand NewTemplateCommand`
  *Description:* Gets the command to create a new template.
- **`CloseAction`**: `public Action? CloseAction`
  *Description:* Gets or sets the action to close the window.

---
### Class: `SetTemplateViewModel`
**File:** [src/SyntheticShared/Modules/AutoTagger/ViewModels/SetTemplateViewModel.cs](../src/SyntheticShared/Modules/AutoTagger/ViewModels/SetTemplateViewModel.cs)

#### Methods
##### `IsCategoryScope`
```csharp
public bool IsCategoryScope
```
Gets or sets a value indicating whether the scope is set to Category.

##### `IsFamilyScope`
```csharp
public bool IsFamilyScope
```
Gets or sets a value indicating whether the scope is set to Family.

##### `IsTypeScope`
```csharp
public bool IsTypeScope
```
Gets or sets a value indicating whether the scope is set to Type.

##### `SetTemplateViewModel`
```csharp
public SetTemplateViewModel(Document doc, FamilyInstance host, XYZ localOffset, TagOrientation tagOrientation, TagTemplate? editingTemplate = null)
```
Initializes a new instance of the `SetTemplateViewModel` class.

#### Fields
- **`TemplateScope`**: `public enum TemplateScope {`
  *Description:* Specifies the matching scope for a tag template (Category-level, Family-level, or Type-level).
- **`ViewModelBase`**: `public class SetTemplateViewModel : ViewModelBase {`
  *Description:* ViewModel controlling the logic for capturing and saving a new TagTemplate.
- **`TemplateName`**: `public string TemplateName {`
  *Description:* Gets or sets the name of the template.
- **`AllowOrientationChange`**: `public bool AllowOrientationChange {`
  *Description:* Gets or sets a value indicating whether orientation change is allowed.
- **`CategoryName`**: `public string CategoryName`
  *Description:* Gets the category name of the host element.
- **`FamilyName`**: `public string FamilyName`
  *Description:* Gets the family name of the host element.
- **`TypeName`**: `public string TypeName`
  *Description:* Gets the type name of the host element.
- **`SelectedScope`**: `public TemplateScope SelectedScope {`
  *Description:* Gets or sets the selected template matching scope.
- **`SaveCommand`**: `public System.Windows.Input.ICommand SaveCommand`
  *Description:* Gets the Save template command.
- **`CancelCommand`**: `public System.Windows.Input.ICommand CancelCommand`
  *Description:* Gets the Cancel command.
- **`CloseAction`**: `public Action? CloseAction`
  *Description:* Delegate assigned by the View to allow the ViewModel to request closure without violating MVVM.

---
## Namespace: `Synthetic.Modules.BatchPrint.Commands`

### Class: `PrintBatchMultiDoc`
**File:** [src/SyntheticShared/Modules/BatchPrint/Commands/PrintBatchMultiDoc.cs](../src/SyntheticShared/Modules/BatchPrint/Commands/PrintBatchMultiDoc.cs)

#### Methods
##### `Execute`
```csharp
public Result Execute( ExternalCommandData commandData, ref string message, ElementSet elements)
```
Execute a Revit Command

#### Fields
- **`IExternalCommand`**: `public class PrintBatchMultiDoc : IExternalCommand {`
  *Description:* Converts Drafting Views to Legends

---
## Namespace: `Synthetic.Modules.BatchPrint.Utilities`

### Class: `BBPrinterSettingsUtils`
**File:** [src/SyntheticShared/Modules/BatchPrint/Utilities/BBPrinterSettingsUtils.cs](../src/SyntheticShared/Modules/BatchPrint/Utilities/BBPrinterSettingsUtils.cs)

#### Methods
##### `BBPrinterSettingsUtils`
```csharp
public BBPrinterSettingsUtils ()
```
Initializes a new instance of the `BBPrinterSettingsUtils` class by reading values from the registry.

##### `BBPrinterSettingsUtils`
```csharp
public BBPrinterSettingsUtils (string openInViewer, string promptForFileName, string path)
```
Initializes a new instance of the `BBPrinterSettingsUtils` class with custom settings.

##### `SetRegistryKeys`
```csharp
public void SetRegistryKeys ()
```
Sets registry keys according to the current property values.

##### `SetDefaultRegistryKeys`
```csharp
public void SetDefaultRegistryKeys()
```
Sets registry keys to their default values.

#### Fields
- **`BBPrinterSettingsUtils`**: `public class BBPrinterSettingsUtils {`
  *Description:* Utilities for setting BlueBeam Printer settings
- **`BBPrinterRegistryPath`**: `public string BBPrinterRegistryPath`
  *Description:* The BlueBeam Printer registry path.
- **`OpenInViewer`**: `public string OpenInViewer;`
  *Description:* Gets or sets the value specifying whether to open in viewer.
- **`PromptForFileName`**: `public string PromptForFileName;`
  *Description:* Gets or sets the value specifying whether to prompt for file name.
- **`ProjectsFolder`**: `public string ProjectsFolder;`
  *Description:* Gets or sets the projects folder path.
- **`SaveAsFolder`**: `public string SaveAsFolder;`
  *Description:* Gets or sets the save as folder path.
- **`UseLastFolder`**: `public string UseLastFolder;`
  *Description:* Gets or sets the value specifying whether to use the last folder.

---
## Namespace: `Synthetic.Modules.DetailItemFactory.Commands`

### Class: `CmdDetailItemFactory`
**File:** [src/SyntheticShared/Modules/DetailItemFactory/Commands/CmdDetailItemFactory.cs](../src/SyntheticShared/Modules/DetailItemFactory/Commands/CmdDetailItemFactory.cs)

#### Methods
##### `Execute`
```csharp
public Result Execute( ExternalCommandData commandData, ref string message, ElementSet elements)
```
Executes the Detail Item Factory command, showing the WPF UI options and running the batch conversion.

#### Fields
- **`IExternalCommand`**: `public class CmdDetailItemFactory : IExternalCommand {`
  *Description:* Production external command to batch-process selected 3D model elements into 2D Detail Item family documents via temporary DWG projection and tracing.

---
## Namespace: `Synthetic.Modules.DetailItemFactory.Handlers`

### Class: `DetailItemFactoryEventHandler`
**File:** [src/SyntheticShared/Modules/DetailItemFactory/Handlers/DetailItemFactoryEventHandler.cs](../src/SyntheticShared/Modules/DetailItemFactory/Handlers/DetailItemFactoryEventHandler.cs)

#### Methods
##### `ConfiguredElements`
```csharp
public List<SelectedElementItemViewModel> ConfiguredElements
```
Gets or sets the collection of configured elements to process.

##### `Execute`
```csharp
public void Execute(UIApplication app)
```
Executes the batch processing loop asynchronously on the Revit main thread.

##### `GetName`
```csharp
public string GetName()
```
Gets the handler name.

##### `OnFamilyFound`
```csharp
public bool OnFamilyFound(bool familyInUse, out bool overwriteParameterValues)
```
Callback when a family is found during loading.

##### `OnSharedFamilyFound`
```csharp
public bool OnSharedFamilyFound(Family sharedFamily, bool familyInUse, out FamilySource source, out bool overwriteParameterValues)
```
Callback when a shared family is found during loading.

#### Fields
- **`IExternalEventHandler`**: `public class DetailItemFactoryEventHandler : IExternalEventHandler {`
  *Description:* External event handler to process selected elements into 2D Detail Items asynchronously on the Revit main thread.
- **`OutputFolder`**: `public string OutputFolder`
  *Description:* Gets or sets the target output folder path.
- **`TargetSubcategory`**: `public string TargetSubcategory`
  *Description:* Gets or sets the target subcategory name.
- **`OverwriteExisting`**: `public bool OverwriteExisting`
  *Description:* Gets or sets a value indicating whether existing family files should be overwritten on disk.
- **`IFamilyLoadOptions`**: `public class SimpleFamilyLoadOptions : IFamilyLoadOptions {`
  *Description:* Standard implementation of IFamilyLoadOptions to automatically overwrite family parameters.

---
## Namespace: `Synthetic.Modules.DetailItemFactory.Models`

### Class: `DetailItemResultItem`
**File:** [src/SyntheticShared/Modules/DetailItemFactory/Models/DetailItemResultItem.cs](../src/SyntheticShared/Modules/DetailItemFactory/Models/DetailItemResultItem.cs)

#### Fields
- **`DetailItemResultItem`**: `public class DetailItemResultItem {`
  *Description:* Represents the results of processing a single model element to a 2D Detail Item family.
- **`ElementName`**: `public string ElementName`
  *Description:* Gets or sets the name of the processed model element.
- **`ElementId`**: `public string ElementId`
  *Description:* Gets or sets the Revit Element ID of the processed element.
- **`Status`**: `public string Status`
  *Description:* Gets or sets the execution status (e.g., Success, Skipped, Failed).
- **`Message`**: `public string Message`
  *Description:* Gets or sets a description or details (such as exception messages or skip reasons).

---
## Namespace: `Synthetic.Modules.DetailItemFactory.Settings`

### Class: `DetailItemFactorySettings`
**File:** [src/SyntheticShared/Modules/DetailItemFactory/Settings/DetailItemFactorySettings.cs](../src/SyntheticShared/Modules/DetailItemFactory/Settings/DetailItemFactorySettings.cs)

#### Methods
##### `DetailItemFactorySettings`
```csharp
public DetailItemFactorySettings()
```
Initializes a new instance of the `DetailItemFactorySettings` class.

##### `Defaults`
```csharp
public DetailItemFactorySettings Defaults()
```
App default settings.

##### `IsValid`
```csharp
public bool IsValid(Document doc)
```
Validates settings module.

#### Fields
- **`ISettingModule`**: `public class DetailItemFactorySettings : ISettingModule {`
  *Description:* Configuration settings for the Detail Item Factory module. This module is serialized and saved inside the Revit Document's Extensible Storage schema.
- **`ModuleKey`**: `public string ModuleKey`
  *Description:* Gets the settings module key.
- **`Name`**: `public const string Name`
  *Description:* The name of this settings module key.
- **`OutputPath`**: `public string OutputPath`
  *Description:* Gets or sets the default output folder path.
- **`TargetSubcategory`**: `public string TargetSubcategory`
  *Description:* Gets or sets the default target subcategory name.

---
## Namespace: `Synthetic.Modules.DetailItemFactory.ViewModels`

### Class: `DetailItemFactoryResultsViewModel`
**File:** [src/SyntheticShared/Modules/DetailItemFactory/ViewModels/DetailItemFactoryResultsViewModel.cs](../src/SyntheticShared/Modules/DetailItemFactory/ViewModels/DetailItemFactoryResultsViewModel.cs)

#### Methods
##### `DetailItemFactoryResultsViewModel`
```csharp
public DetailItemFactoryResultsViewModel(string outputFolder, IEnumerable<DetailItemResultItem> results)
```
Initializes a new instance of the `DetailItemFactoryResultsViewModel` class.

#### Fields
- **`ViewModelBase`**: `public class DetailItemFactoryResultsViewModel : ViewModelBase {`
  *Description:* ViewModel for the Detail Item Factory batch results dashboard. Provides collections for list views and garbage collection commands for temporary DWG files.
- **`Results`**: `public ObservableCollection<DetailItemResultItem> Results`
  *Description:* Gets the collection of elements conversion results.
- **`DeleteTempDwgsCommand`**: `public ICommand DeleteTempDwgsCommand`
  *Description:* Gets the command to delete temporary DWG files from the output folder.
- **`CloseCommand`**: `public ICommand CloseCommand`
  *Description:* Gets the command to close the window.
- **`CloseAction`**: `public Action? CloseAction`
  *Description:* Delegate assigned by the View to handle window closure.

---
### Class: `DetailItemFactorySettingsViewModel`
**File:** [src/SyntheticShared/Modules/DetailItemFactory/ViewModels/DetailItemFactorySettingsViewModel.cs](../src/SyntheticShared/Modules/DetailItemFactory/ViewModels/DetailItemFactorySettingsViewModel.cs)

#### Methods
##### `AvailableSubcategories`
```csharp
public ObservableCollection<string> AvailableSubcategories
```
Gets the collection of available subcategory names from BuiltInCategory.OST_DetailComponents.

##### `DetailItemFactorySettingsViewModel`
```csharp
public DetailItemFactorySettingsViewModel(Document doc, IntPtr mainWindowHandle)
```
Initializes a new instance of the `DetailItemFactorySettingsViewModel` class.

#### Fields
- **`ISettingModuleViewModel`**: `public class DetailItemFactorySettingsViewModel : ViewModelBase, ISettingModuleViewModel {`
  *Description:* ViewModel for managing Detail Item Factory configurations in the Settings Dashboard.
- **`OutputPath`**: `public string OutputPath {`
  *Description:* Gets or sets the default output folder path.
- **`SelectedSubcategoryName`**: `public string SelectedSubcategoryName {`
  *Description:* Gets or sets the selected subcategory name.
- **`BrowseCommand`**: `public ICommand BrowseCommand`
  *Description:* Gets the command that allows browsing for an output folder.

---
### Class: `DetailItemFactoryViewModel`
**File:** [src/SyntheticShared/Modules/DetailItemFactory/ViewModels/DetailItemFactoryViewModel.cs](../src/SyntheticShared/Modules/DetailItemFactory/ViewModels/DetailItemFactoryViewModel.cs)

#### Methods
##### `Elements`
```csharp
public ObservableCollection<SelectedElementItemViewModel> Elements
```
Gets the collection of elements configured for conversion.

##### `Subcategories`
```csharp
public ObservableCollection<string> Subcategories
```
Gets the collection of OST_DetailComponents subcategory names available in the document.

##### `DetailItemFactoryViewModel`
```csharp
public DetailItemFactoryViewModel(Document doc, View activeView, IEnumerable<ElementId> selectedIds, IntPtr mainWindowHandle)
```
Initializes a new instance of the `DetailItemFactoryViewModel` class.

#### Fields
- **`ViewModelBase`**: `public class DetailItemFactoryViewModel : ViewModelBase {`
  *Description:* ViewModel for the Detail Item Factory WPF dialog. Provides bindings and validation logic for batch processing Revit model geometry to 2D Detail Items. Supports per-element configuration of view orientations.
- **`OutputPath`**: `public string OutputPath {`
  *Description:* Gets or sets the target folder path where family files are saved.
- **`SelectedSubcategory`**: `public string SelectedSubcategory {`
  *Description:* Gets or sets the selected subcategory name.
- **`OverwriteExisting`**: `public bool OverwriteExisting {`
  *Description:* Gets or sets a value indicating whether existing family files should be overwritten on disk.
- **`BrowseCommand`**: `public ICommand BrowseCommand`
  *Description:* Command to browse for an output folder.
- **`RemoveElementCommand`**: `public ICommand RemoveElementCommand`
  *Description:* Command to remove an element from the queue.
- **`RunCommand`**: `public ICommand RunCommand`
  *Description:* Command to trigger processing (runs validation and closes window with a success result).
- **`CancelCommand`**: `public ICommand CancelCommand`
  *Description:* Command to abort processing and close the window with a cancelled result.
- **`CloseAction`**: `public Action<bool>? CloseAction`
  *Description:* Delegate assigned by the View to request window closure, passing the dialog result.

---
### Class: `SelectedElementItemViewModel`
**File:** [src/SyntheticShared/Modules/DetailItemFactory/ViewModels/SelectedElementItemViewModel.cs](../src/SyntheticShared/Modules/DetailItemFactory/ViewModels/SelectedElementItemViewModel.cs)

#### Methods
##### `SelectedElementItemViewModel`
```csharp
public SelectedElementItemViewModel(Element element, View activeView)
```
Initializes a new instance of the `SelectedElementItemViewModel` class.

#### Fields
- **`ViewModelBase`**: `public class SelectedElementItemViewModel : ViewModelBase {`
  *Description:* ViewModel representing a single selected element for per-element configuration.
- **`ElementId`**: `public ElementId ElementId`
  *Description:* Gets the Revit Element ID.
- **`DisplayName`**: `public string DisplayName`
  *Description:* Gets the user-friendly display name.
- **`AvailableOrientations`**: `public List<string> AvailableOrientations`
  *Description:* Gets the list of available orientation options.
- **`SelectedOrientation`**: `public string SelectedOrientation {`
  *Description:* Gets or sets the selected orientation for this element.

---
## Namespace: `Synthetic.Modules.FamilyManagement.Commands`

### Class: `AuditPurgeAllFamilies`
**File:** [src/SyntheticShared/Modules/FamilyManagement/Commands/AuditPurgeAllFamilies.cs](../src/SyntheticShared/Modules/FamilyManagement/Commands/AuditPurgeAllFamilies.cs)

#### Methods
##### `Execute`
```csharp
public Result Execute( ExternalCommandData commandData, ref string message, ElementSet elements)
```
Execute a Revit Command

#### Fields
- **`IExternalCommand`**: `public class AuditPurgeAllFamilies : IExternalCommand {`
  *Description:* Revit external command to audit and purge all families loaded in the current project. Provides options to audit only, purge unused elements, and delete schema options.

---
### Class: `CmdTestAuditPurgeJournalAvailability`
**File:** [src/SyntheticShared/Modules/FamilyManagement/Commands/CmdTestAuditPurgeJournal.cs](../src/SyntheticShared/Modules/FamilyManagement/Commands/CmdTestAuditPurgeJournal.cs)

#### Methods
##### `IsCommandAvailable`
```csharp
public bool IsCommandAvailable(UIApplication applicationData, CategorySet selectedCategories)
```
Determine if the command is active.

##### `Execute`
```csharp
public Result Execute( ExternalCommandData commandData, ref string message, ElementSet elements)
```
Execute the headless test command.

#### Fields
- **`IExternalCommandAvailability`**: `public class CmdTestAuditPurgeJournalAvailability : IExternalCommandAvailability {`
  *Description:* Availability class to ensure CmdTestAuditPurgeJournal is always available, even when no document is open.
- **`IExternalCommand`**: `public class CmdTestAuditPurgeJournal : IExternalCommand {`
  *Description:* Headless integration test command for Audit &amp; Purge. Opens the test model, measures family file sizes before and after purging, checks parameter override data integrity, and writes results to commandData.JournalData.

---
### Class: `FamiliesForceReinsert`
**File:** [src/SyntheticShared/Modules/FamilyManagement/Commands/FamiliesForceReinsert.cs](../src/SyntheticShared/Modules/FamilyManagement/Commands/FamiliesForceReinsert.cs)

#### Methods
##### `Execute`
```csharp
public Result Execute( ExternalCommandData commandData, ref string message, ElementSet elements)
```
Execute a Revit Command

#### Fields
- **`IExternalCommand`**: `public class FamiliesForceReinsert : IExternalCommand {`
  *Description:* Sets the path and sheet to an Excel file with Worksets. Stores the setting in an Extensible Storage.

---
## Namespace: `Synthetic.Modules.FamilyManagement.Handlers`

### Class: `AuditPurgeEventHandler`
**File:** [src/SyntheticShared/Modules/FamilyManagement/Handlers/AuditPurgeEventHandler.cs](../src/SyntheticShared/Modules/FamilyManagement/Handlers/AuditPurgeEventHandler.cs)

#### Methods
##### `Execute`
```csharp
public void Execute(UIApplication app)
```
Executes the audit and purge loop on the Revit API thread.

##### `GetName`
```csharp
public string GetName()
```
Gets the name of the external event handler.

#### Fields
- **`IExternalEventHandler`**: `public class AuditPurgeEventHandler : IExternalEventHandler {`
  *Description:* External event handler to process Audit and Purge on all editable families in the active document.
- **`Purge`**: `public bool Purge`
  *Description:* Gets or sets whether to purge unused elements.
- **`PurgeSchema`**: `public bool PurgeSchema`
  *Description:* Gets or sets whether to purge extensible storage schemas.
- **`SchemaExceptions`**: `public List<string>? SchemaExceptions`
  *Description:* Gets or sets the list of schema names to exclude from purging.

---
## Namespace: `Synthetic.Modules.FamilyManagement.Utilities`

### Class: `PurgeFailuresPreprocessor`
**File:** [src/SyntheticShared/Modules/FamilyManagement/Utilities/PurgeFailuresPreprocessor.cs](../src/SyntheticShared/Modules/FamilyManagement/Utilities/PurgeFailuresPreprocessor.cs)

#### Methods
##### `PreprocessFailures`
```csharp
public FailureProcessingResult PreprocessFailures(FailuresAccessor failuresAccessor)
```
Preprocesses failures to delete warnings and attempt resolution of errors.

#### Fields
- **`IFailuresPreprocessor`**: `public class PurgeFailuresPreprocessor : IFailuresPreprocessor {`
  *Description:* Silently preprocesses and handles failures triggered during family audit and purge operations.

---
### Class: `SafeFamilyLoadOptions`
**File:** [src/SyntheticShared/Modules/FamilyManagement/Utilities/SafeFamilyLoadOptions.cs](../src/SyntheticShared/Modules/FamilyManagement/Utilities/SafeFamilyLoadOptions.cs)

#### Methods
##### `OnFamilyFound`
```csharp
public bool OnFamilyFound( bool familyInUse, out bool overwriteParameterValues)
```
Called when a family is found in the project.

##### `OnSharedFamilyFound`
```csharp
public bool OnSharedFamilyFound( Family sharedFamily, bool familyInUse, out FamilySource source, out bool overwriteParameterValues)
```
Called when a shared nested family is found in the project.

#### Fields
- **`IFamilyLoadOptions`**: `public class SafeFamilyLoadOptions : IFamilyLoadOptions {`
  *Description:* Safe family load options that prevent overwriting existing parameter values in the project.

---
## Namespace: `Synthetic.Modules.MaterialManagement.Commands`

### Class: `MaterialImagesPackage`
**File:** [src/SyntheticShared/Modules/MaterialManagement/Commands/MaterialImagesPackage.cs](../src/SyntheticShared/Modules/MaterialManagement/Commands/MaterialImagesPackage.cs)

#### Methods
##### `typeof`
```csharp
public static string? CommandPath = typeof(MaterialImagesPackage).FullName
```
The full class path/name of this command.

##### `Execute`
```csharp
public Result Execute( ExternalCommandData commandData, ref string message, ElementSet elements)
```
Execute a Revit Command

##### `SelectPath`
```csharp
internal string? SelectPath(IntPtr ownerHandle)
```
Allows the user to browse for a folder location

##### `SelectSearchPaths`
```csharp
internal List<string> SelectSearchPaths(List<string> defaultPaths, IntPtr mainWindowHandle)
```
Allows the user to add paths and select from a list of default paths for constructing a SearchPath

##### `SelectMaterials`
```csharp
internal List<Material> SelectMaterials(List<Material> materials, IntPtr mainWindowHandle)
```
Selects materials via checklist dialog

#### Fields
- **`IExternalCommand`**: `public class MaterialImagesPackage : IExternalCommand {`
  *Description:* Revit external command to package and transmit all material bitmap images to a new directory.
- **`CommandButton`**: `public const string CommandButton`
  *Description:* The button label text for this command in the Revit UI.
- **`CommandTooltip`**: `public const string CommandTooltip`
  *Description:* The tooltip message for this command in the Revit UI.

---
### Class: `MaterialsRepathAll`
**File:** [src/SyntheticShared/Modules/MaterialManagement/Commands/MaterialsRepathAll.cs](../src/SyntheticShared/Modules/MaterialManagement/Commands/MaterialsRepathAll.cs)

#### Methods
##### `Execute`
```csharp
public Result Execute( ExternalCommandData commandData, ref string message, ElementSet elements)
```
Execute a Revit Command

##### `SelectSearchPaths`
```csharp
internal List<string> SelectSearchPaths(List<string> defaultPaths, IntPtr mainWindowHandle)
```
Allows the user to add paths and select from a list of default paths for constructing a SearchPath

##### `SelectMaterials`
```csharp
internal List<Material> SelectMaterials(List<Material> materials, IntPtr mainWindowHandle)
```
Selects materials via checklist dialog

#### Fields
- **`IExternalCommand`**: `public class MaterialsRepathAll : IExternalCommand {`
  *Description:* Revit external command to repath material bitmap images based on search paths.

---
### Class: `PaintElements`
**File:** [src/SyntheticShared/Modules/MaterialManagement/Commands/PaintElements.cs](../src/SyntheticShared/Modules/MaterialManagement/Commands/PaintElements.cs)

#### Methods
##### `Execute`
```csharp
public Result Execute( ExternalCommandData commandData, ref string message, ElementSet elementSet)
```
Execute a Revit Command

#### Fields
- **`IExternalCommand`**: `public class PaintElements : IExternalCommand {`
  *Description:* Paints all faces of selected elements with a material

---
## Namespace: `Synthetic.Modules.MaterialManagement.Utilities`

### Class: `MaterialUtil`
**File:** [src/SyntheticShared/Modules/MaterialManagement/Utilities/MaterialUtil.cs](../src/SyntheticShared/Modules/MaterialManagement/Utilities/MaterialUtil.cs)

#### Methods
##### `Files`
```csharp
public const string AutodeskMaterialLibrary = "C:\\Program Files (x86)\\Common Files\\Autodesk Shared\\Materials\\Textures"
```
The default path to the Autodesk Shared Materials Textures directory.

##### `GetByNameDocument`
```csharp
public static revitMaterial? GetByNameDocument(string Name, revitDoc Document)
```
Gets a material given its name and document

##### `GetAllMaterials`
```csharp
public static FilteredElementCollector GetAllMaterials(revitDoc Document)
```
Retrieves all material elements in the specified document.

##### `GetMaterialBitmapPaths`
```csharp
public static List<string>? GetMaterialBitmapPaths(revitMaterial Material)
```
Gets all the connected files with paths associated with a material.

##### `ReplaceBitmapPaths`
```csharp
public static Dictionary<string, object> ReplaceBitmapPaths(List<revitMaterial> Materials, SearchPaths searchPaths, bool ReplaceRelativePaths = false, bool RunTest = false )
```
Given a material list and a SearchPaths object, the method will replace the path of any bitmaps in the material based on the SearchPaths.

##### `_ReadAssetPropertyPaths`
```csharp
private static List<string> _ReadAssetPropertyPaths(AssetProperty assetProperty)
```
Checks if a AssetProperty has connected properties and if those connected properties have a bitmap path, it returns the path.

#### Fields
- **`MaterialUtil`**: `public class MaterialUtil {`
  *Description:* Extensions of Dynamo Revit

---
## Namespace: `Synthetic.Modules.MergeDuplicates.Commands`

### Class: `CmdMergeDuplicates`
**File:** [src/SyntheticShared/Modules/MergeDuplicates/Commands/CmdMergeDuplicates.cs](../src/SyntheticShared/Modules/MergeDuplicates/Commands/CmdMergeDuplicates.cs)

#### Methods
##### `Execute`
```csharp
public Result Execute(ExternalCommandData commandData, ref string message, ElementSet elements)
```
Executes the merge duplicates command.

#### Fields
- **`IExternalCommand`**: `public class CmdMergeDuplicates : IExternalCommand {`
  *Description:* Revit command to scan model for duplicate families and display the Merge Duplicates Modeless UI.

---
## Namespace: `Synthetic.Modules.MergeDuplicates.Handlers`

### Class: `MergeExecutionReport`
**File:** [src/SyntheticShared/Modules/MergeDuplicates/Handlers/ProcessMergeEventHandler.cs](../src/SyntheticShared/Modules/MergeDuplicates/Handlers/ProcessMergeEventHandler.cs)

#### Methods
##### `OnFamilyFound`
```csharp
public bool OnFamilyFound(bool familyInUse, out bool overwriteParameterValues)
```
Called when a family is found in the target document. Overwrites parameter values.

##### `OnSharedFamilyFound`
```csharp
public bool OnSharedFamilyFound(Family sharedFamily, bool familyInUse, out FamilySource source, out bool overwriteParameterValues)
```
Called when a shared family is found in the target document. Overwrites parameter values.

##### `QueueRequest`
```csharp
public void QueueRequest(MergeQueueViewModel? queueVM, Window? window, MergeDuplicatesViewModel? mainVM, CancellationToken cancellationToken)
```
Queues a merge execution request with its corresponding UI/VM contexts.

##### `Execute`
```csharp
public void Execute(UIApplication app)
```
Executes the merge processing on the Revit API thread.

##### `GetName`
```csharp
public string GetName()
```
Gets the name of the external event handler.

#### Fields
- **`MergeExecutionReport`**: `public class MergeExecutionReport {`
  *Description:* Tracks the summary results of a merge cluster execution for Step 6.
- **`ClusterName`**: `public string ClusterName`
  *Description:* Gets or sets the name of the merge cluster.
- **`InstancesSwappedCount`**: `public int InstancesSwappedCount`
  *Description:* Gets or sets the count of instances swapped during the merge.
- **`DuplicateTypesPurged`**: `public int DuplicateTypesPurged`
  *Description:* Gets or sets the count of duplicate types purged.
- **`ExecutionSuccessStatus`**: `public bool ExecutionSuccessStatus`
  *Description:* Gets or sets a value indicating whether the merge execution succeeded.
- **`ErrorMessage`**: `public string? ErrorMessage`
  *Description:* Gets or sets the error message if the execution failed.
- **`IFamilyLoadOptions`**: `public class ProcessMergeFamilyLoadOptions : IFamilyLoadOptions {`
  *Description:* Standard family load options to automatically overwrite parameter values.
- **`IExternalEventHandler`**: `public class ProcessMergeEventHandler : IExternalEventHandler {`
  *Description:* External event handler to process the active merge queue on the Revit API thread.

---
## Namespace: `Synthetic.Modules.MergeDuplicates.Models`

### Class: `DuplicateClusterModel`
**File:** [src/SyntheticShared/Modules/MergeDuplicates/Models/DuplicateClusterModel.cs](../src/SyntheticShared/Modules/MergeDuplicates/Models/DuplicateClusterModel.cs)

#### Methods
##### `DuplicateClusterModel`
```csharp
public DuplicateClusterModel()
```
Initializes a new instance of the DuplicateClusterModel class.

##### `UpdatePrimaryItem`
```csharp
public void UpdatePrimaryItem(DuplicateItemModel newPrimary)
```
Updates the designated primary item, resetting the primary flag on other items in the cluster.

##### `OnPropertyChanged`
```csharp
protected virtual void OnPropertyChanged([CallerMemberName] string? propertyName = null)
```
Raises the PropertyChanged event.

##### `SetProperty<T>`
```csharp
protected virtual bool SetProperty<T>(ref T storage, T value, [CallerMemberName] string? propertyName = null)
```
Sets a property value and raises the PropertyChanged event if the value changed.

#### Fields
- **`INotifyPropertyChanged`**: `public class DuplicateClusterModel : ObjectModel, INotifyPropertyChanged {`
  *Description:* Model representing a cluster of duplicate elements. Manages the selection of the primary element and details about schema or origin conflicts.
- **`Items`**: `public ObservableCollection<DuplicateItemModel> Items {`
  *Description:* Gets or sets the collection of duplicate items in this cluster.
- **`ClusterName`**: `public string ClusterName {`
  *Description:* Gets or sets the name of the cluster.
- **`HasSchemaMismatch`**: `public bool HasSchemaMismatch {`
  *Description:* Gets or sets a value indicating whether there is a schema mismatch within the cluster.
- **`HasOriginMismatch`**: `public bool HasOriginMismatch {`
  *Description:* Gets or sets a value indicating whether there is a coordinate origin mismatch within the cluster.
- **`IsBlocked`**: `public bool IsBlocked {`
  *Description:* Gets or sets a value indicating whether this cluster is blocked from being merged.
- **`ParameterResolutions`**: `public Dictionary<string, ElementId> ParameterResolutions {`
  *Description:* Gets or sets the dictionary of parameter resolutions (which element's parameter value wins).
- **`TypeMappings`**: `public ObservableCollection<TypeMappingModel> TypeMappings {`
  *Description:* Gets or sets the collection of type mappings for this cluster.
- **`SelectedPrimary`**: `public DuplicateItemModel? SelectedPrimary {`
  *Description:* Gets or sets the duplicate item designated as the primary/surviving element.
- **`PropertyChanged`**: `public event PropertyChangedEventHandler? PropertyChanged;`
  *Description:* Occurs when a property value changes.

---
### Class: `DuplicateItemModel`
**File:** [src/SyntheticShared/Modules/MergeDuplicates/Models/DuplicateItemModel.cs](../src/SyntheticShared/Modules/MergeDuplicates/Models/DuplicateItemModel.cs)

#### Methods
##### `DuplicateItemModel`
```csharp
public DuplicateItemModel()
```
Initializes a new instance of the DuplicateItemModel class.

##### `OnPropertyChanged`
```csharp
protected virtual void OnPropertyChanged([CallerMemberName] string? propertyName = null)
```
Raises the PropertyChanged event.

##### `SetProperty<T>`
```csharp
protected virtual bool SetProperty<T>(ref T storage, T value, [CallerMemberName] string? propertyName = null)
```
Sets a property value and raises the PropertyChanged event if the value changed.

#### Fields
- **`INotifyPropertyChanged`**: `public class DuplicateItemModel : ObjectModel, INotifyPropertyChanged {`
  *Description:* Represents the parent container (e.g., the Family or parent element) for duplicates.
- **`RevitElementId`**: `public ElementId? RevitElementId {`
  *Description:* The Revit ElementId of this parent item.
- **`ItemName`**: `public string ItemName {`
  *Description:* The name of this item.
- **`CategoryName`**: `public string CategoryName {`
  *Description:* The Revit category name.
- **`Types`**: `public ObservableCollection<DuplicateTypeModel> Types {`
  *Description:* Nested collection of types belonging to this container.
- **`IsPrimary`**: `public bool IsPrimary {`
  *Description:* Whether this item is chosen as the primary survivor.
- **`IsIncludedForMerge`**: `public bool IsIncludedForMerge {`
  *Description:* Whether this item is included for merge processing.
- **`IsLoadableFamily`**: `public bool IsLoadableFamily {`
  *Description:* True if this item represents a loadable Family element in Revit.
- **`BoundingBox`**: `public BoundingBoxXYZ? BoundingBox {`
  *Description:* Geometric bounding box representation.
- **`Origin`**: `public XYZ? Origin {`
  *Description:* Geometric origin point (corresponds to Location).
- **`Location`**: `public XYZ? Location {`
  *Description:* Compatibility wrapper for Location mapping to Origin.
- **`Parameters`**: `public Dictionary<string, string> Parameters {`
  *Description:* Compatibility parameters dictionary.
- **`InstanceCount`**: `public int InstanceCount {`
  *Description:* Compatibility instance count.
- **`PropertyChanged`**: `public event PropertyChangedEventHandler? PropertyChanged;`
  *Description:* Occurs when a property value changes.

---
### Class: `DuplicateTypeModel`
**File:** [src/SyntheticShared/Modules/MergeDuplicates/Models/DuplicateTypeModel.cs](../src/SyntheticShared/Modules/MergeDuplicates/Models/DuplicateTypeModel.cs)

#### Methods
##### `DuplicateTypeModel`
```csharp
public DuplicateTypeModel()
```
Initializes a new instance of the DuplicateTypeModel class.

##### `OnPropertyChanged`
```csharp
protected virtual void OnPropertyChanged([CallerMemberName] string? propertyName = null)
```
Raises the PropertyChanged event.

##### `SetProperty<T>`
```csharp
protected virtual bool SetProperty<T>(ref T storage, T value, [CallerMemberName] string? propertyName = null)
```
Sets a property value and raises the PropertyChanged event if the value changed.

#### Fields
- **`INotifyPropertyChanged`**: `public class DuplicateTypeModel : ObjectModel, INotifyPropertyChanged {`
  *Description:* Represents a specific duplicate FamilySymbol, GroupType, or AssemblyType.
- **`RevitTypeId`**: `public ElementId RevitTypeId {`
  *Description:* The Revit ElementId of this type.
- **`Name`**: `public string Name {`
  *Description:* The name of this type.
- **`Parameters`**: `public Dictionary<string, string> Parameters {`
  *Description:* Dictionary of parameters mapping parameter name to its storage type or value string.
- **`PropertyChanged`**: `public event PropertyChangedEventHandler? PropertyChanged;`
  *Description:* Occurs when a property value changes.

---
### Class: `ParameterValueOption`
**File:** [src/SyntheticShared/Modules/MergeDuplicates/Models/ParameterDiffRowModel.cs](../src/SyntheticShared/Modules/MergeDuplicates/Models/ParameterDiffRowModel.cs)

#### Methods
##### `ParameterDiffRowModel`
```csharp
public ParameterDiffRowModel()
```
Initializes a new instance of the ParameterDiffRowModel class.

##### `OnPropertyChanged`
```csharp
protected virtual void OnPropertyChanged([CallerMemberName] string? propertyName = null)
```
Raises the `PropertyChanged` event.

##### `SetProperty<T>`
```csharp
protected virtual bool SetProperty<T>(ref T storage, T value, [CallerMemberName] string? propertyName = null)
```
Sets a property value and raises the `PropertyChanged` event if the value changed.

#### Fields
- **`ParameterValueOption`**: `public class ParameterValueOption {`
  *Description:* Represents a specific value option for a parameter.
- **`ElementId`**: `public ElementId? ElementId`
  *Description:* Gets or sets the ElementId of the option.
- **`DisplayText`**: `public string DisplayText`
  *Description:* Gets or sets the display text for the option.
- **`INotifyPropertyChanged`**: `public class ParameterDiffRowModel : ObjectModel, INotifyPropertyChanged {`
  *Description:* Represents a single parameter row in the Diff Tool.
- **`IsApproved`**: `public bool IsApproved {`
  *Description:* Gets or sets a value indicating whether this parameter override is approved by the user.
- **`ParameterName`**: `public string ParameterName {`
  *Description:* The name of the parameter.
- **`Values`**: `public Dictionary<ElementId, string> Values {`
  *Description:* Maps the type/symbol ElementId to the parameter value string.
- **`IsSchemaMismatch`**: `public bool IsSchemaMismatch {`
  *Description:* True if there is a schema storage type mismatch for this parameter across duplicate types.
- **`WinningValueElementId`**: `public ElementId WinningValueElementId {`
  *Description:* The ElementId of the type whose parameter value should win during merge.
- **`IsSourceWinning`**: `public bool IsSourceWinning {`
  *Description:* Gets or sets whether the source value is selected as the winning value.
- **`IsTargetWinning`**: `public bool IsTargetWinning {`
  *Description:* Gets or sets whether the target value is selected as the winning value.
- **`InjectParameter`**: `public bool InjectParameter {`
  *Description:* True if the parameter should be dynamically injected into the target family during merge.
- **`IsInjectEnabled`**: `public bool IsInjectEnabled {`
  *Description:* True if the parameter can/should be injected (i.e. it does not already exist in the primary family).
- **`ValueList`**: `public List<string> ValueList {`
  *Description:* Gets or sets the list of parameter values for compatibility.
- **`Options`**: `public List<ParameterValueOption> Options {`
  *Description:* Gets or sets the list of value options for compatibility.
- **`HasConflict`**: `public bool HasConflict {`
  *Description:* Gets or sets a value indicating whether there is a conflict in values for compatibility.
- **`PropertyChanged`**: `public event PropertyChangedEventHandler? PropertyChanged;`
  *Description:* Occurs when a property value changes.

---
### Class: `TypeMappingModel`
**File:** [src/SyntheticShared/Modules/MergeDuplicates/Models/TypeMappingModel.cs](../src/SyntheticShared/Modules/MergeDuplicates/Models/TypeMappingModel.cs)

#### Methods
##### `TypeMappingModel`
```csharp
public TypeMappingModel()
```
Initializes a new instance of the TypeMappingModel class.

##### `OnPropertyChanged`
```csharp
protected virtual void OnPropertyChanged([CallerMemberName] string? propertyName = null)
```
Raises the `PropertyChanged` event.

##### `SetProperty<T>`
```csharp
protected virtual bool SetProperty<T>(ref T storage, T value, [CallerMemberName] string? propertyName = null)
```
Sets a property value and raises the `PropertyChanged` event if the value changed.

#### Fields
- **`INotifyPropertyChanged`**: `public class TypeMappingModel : ObjectModel, INotifyPropertyChanged {`
  *Description:* Manages the resolution state between two duplicate types.
- **`SourceFamily`**: `public DuplicateItemModel? SourceFamily {`
  *Description:* The family container that this source type belongs to.
- **`TargetFamily`**: `public DuplicateItemModel? TargetFamily {`
  *Description:* The primary/target family container that the target type belongs to.
- **`SourceType`**: `public DuplicateTypeModel? SourceType {`
  *Description:* The duplicate type to be merged or migrated from.
- **`TargetType`**: `public DuplicateTypeModel? TargetType {`
  *Description:* The target type to merge or migrate into.
- **`RecommendedAction`**: `public RecommendedAction RecommendedAction {`
  *Description:* The action recommendation for this type mapping (Merge, Migrate, Exclude).
- **`ParameterResolutions`**: `public ObservableCollection<ParameterDiffRowModel> ParameterResolutions {`
  *Description:* Collection of parameter diff rows and winning value resolutions.
- **`AvailablePrimaryTypes`**: `public ObservableCollection<DuplicateTypeModel> AvailablePrimaryTypes {`
  *Description:* Collection of available types in the primary target family.
- **`MigrateRenameText`**: `public string MigrateRenameText {`
  *Description:* The new name of the type if it is migrated.
- **`PropertyChanged`**: `public event PropertyChangedEventHandler? PropertyChanged;`
  *Description:* Occurs when a property value changes.

---
## Namespace: `Synthetic.Modules.MergeDuplicates.ViewModels`

### Class: `MergeDetailedReviewViewModel`
**File:** [src/SyntheticShared/Modules/MergeDuplicates/ViewModels/MergeDetailedReviewViewModel.cs](../src/SyntheticShared/Modules/MergeDuplicates/ViewModels/MergeDetailedReviewViewModel.cs)

#### Methods
##### `MergeDetailedReviewViewModel`
```csharp
public MergeDetailedReviewViewModel( DuplicateClusterModel cluster, Action<DuplicateClusterModel>? commitCallback = null, Action<DuplicateClusterModel>? moveItemCallback = null)
```
Initializes a new instance of the `MergeDetailedReviewViewModel` class.

#### Fields
- **`ViewModelBase`**: `public class MergeDetailedReviewViewModel : ViewModelBase {`
  *Description:* ViewModel for the detailed merge review view.
- **`Cluster`**: `public DuplicateClusterModel Cluster {`
  *Description:* Gets or sets the duplicate cluster model under review.
- **`IsBlockedMessageVisible`**: `public bool IsBlockedMessageVisible {`
  *Description:* Gets a value indicating whether the blocked message is visible.
- **`BlockedReasonMessage`**: `public string BlockedReasonMessage {`
  *Description:* Gets the reason message why the cluster is blocked from being queued.
- **`SelectedTypeMapping`**: `public TypeMappingModel? SelectedTypeMapping {`
  *Description:* Gets or sets the currently selected type mapping.
- **`DiffComparisonContext`**: `public string DiffComparisonContext {`
  *Description:* Gets the text context of the parameter difference comparison.
- **`Rows`**: `public ObservableCollection<ParameterDiffRowModel>? Rows {`
  *Description:* Gets the parameter diff rows for the selected type mapping.
- **`CmdCommitToQueue`**: `public ICommand CmdCommitToQueue`
  *Description:* Gets the command to commit the cluster to the queue.
- **`CmdMoveItem`**: `public ICommand CmdMoveItem`
  *Description:* Gets the command to move the duplicate item out of this cluster.
- **`CloseAction`**: `public Action? CloseAction`
  *Description:* Gets or sets the action to close the window.

---
### Class: `MergeDuplicatesViewModel`
**File:** [src/SyntheticShared/Modules/MergeDuplicates/ViewModels/MergeDuplicatesViewModel.cs](../src/SyntheticShared/Modules/MergeDuplicates/ViewModels/MergeDuplicatesViewModel.cs)

#### Methods
##### `MergeDuplicatesViewModel`
```csharp
public MergeDuplicatesViewModel()
```
Initializes a new instance of the `MergeDuplicatesViewModel` class.

#### Fields
- **`ViewModelBase`**: `public class MergeDuplicatesViewModel : ViewModelBase {`
  *Description:* ViewModel for scanning and merging duplicate elements in the Revit database.
- **`MainWindowHandle`**: `public IntPtr MainWindowHandle {`
  *Description:* Gets or sets the main window handle.
- **`Document`**: `public Document? Document {`
  *Description:* Gets or sets the active Revit document.
- **`ScannedClusters`**: `public ObservableCollection<DuplicateClusterModel> ScannedClusters {`
  *Description:* Gets or sets the list of scanned duplicate clusters.
- **`QueueVM`**: `public MergeQueueViewModel QueueVM {`
  *Description:* Gets or sets the merge queue view model.
- **`QueuedClusters`**: `public ObservableCollection<DuplicateClusterModel>? QueuedClusters`
  *Description:* Gets the collection of duplicate clusters queued for merging.
- **`ProgressValue`**: `public double ProgressValue {`
  *Description:* Gets or sets the current progress value of the scan.
- **`ProgressMax`**: `public double ProgressMax {`
  *Description:* Gets or sets the maximum progress value of the scan.
- **`CmdScanModel`**: `public ICommand CmdScanModel`
  *Description:* Gets the command to scan the model for duplicates.
- **`CmdLoadSelection`**: `public ICommand CmdLoadSelection`
  *Description:* Gets the command to load the current Revit selection.
- **`CmdAddToQueue`**: `public ICommand CmdAddToQueue`
  *Description:* Gets the command to add a cluster to the merge queue.
- **`CmdRemoveFromQueue`**: `public ICommand CmdRemoveFromQueue`
  *Description:* Gets the command to remove a cluster from the merge queue.
- **`CmdProcessMerge`**: `public ICommand CmdProcessMerge`
  *Description:* Gets the command to execute the merge queue.
- **`CmdCancelScan`**: `public ICommand CmdCancelScan`
  *Description:* Gets the command to cancel an active scan.
- **`CmdMoveItem`**: `public ICommand CmdMoveItem`
  *Description:* Gets the command to move an item from one cluster to another.
- **`CmdDetailedReview`**: `public ICommand CmdDetailedReview`
  *Description:* Gets the command to launch the detailed review window for a cluster.
- **`CmdAddStragglers`**: `public ICommand CmdAddStragglers`
  *Description:* Gets the command to add straggler elements to the duplicate cluster.

---
### Class: `MergeQueueViewModel`
**File:** [src/SyntheticShared/Modules/MergeDuplicates/ViewModels/MergeQueueViewModel.cs](../src/SyntheticShared/Modules/MergeDuplicates/ViewModels/MergeQueueViewModel.cs)

#### Methods
##### `MergeQueueViewModel`
```csharp
public MergeQueueViewModel() : this(null)
```
Initializes a new instance of the `MergeQueueViewModel` class.

##### `MergeQueueViewModel`
```csharp
public MergeQueueViewModel(Action<DuplicateClusterModel>? onRemoved)
```
Initializes a new instance of the `MergeQueueViewModel` class.

##### `RemoveFromQueue`
```csharp
public void RemoveFromQueue(DuplicateClusterModel cluster)
```
Removes a duplicate cluster from the queue.

##### `ClearQueue`
```csharp
public void ClearQueue()
```
Clears all duplicate clusters from the queue.

#### Fields
- **`ViewModelBase`**: `public class MergeQueueViewModel : ViewModelBase {`
  *Description:* ViewModel for managing a queue of elements to merge.
- **`QueuedClusters`**: `public ObservableCollection<DuplicateClusterModel> QueuedClusters {`
  *Description:* Gets or sets the collection of duplicate clusters queued for merging.
- **`CmdRemoveFromQueue`**: `public ICommand CmdRemoveFromQueue`
  *Description:* Gets the command to remove a cluster from the merge queue.
- **`CmdClearQueue`**: `public ICommand CmdClearQueue`
  *Description:* Gets the command to clear the entire merge queue.

---
### Class: `MergeSelectionViewModel`
**File:** [src/SyntheticShared/Modules/MergeDuplicates/ViewModels/MergeSelectionViewModel.cs](../src/SyntheticShared/Modules/MergeDuplicates/ViewModels/MergeSelectionViewModel.cs)

#### Methods
##### `MergeSelectionViewModel`
```csharp
public MergeSelectionViewModel(IEnumerable<ElementTypeWrapperVM> elements)
```
Initializes a new instance of the `MergeSelectionViewModel` class.

#### Fields
- **`ViewModelBase`**: `public class MergeSelectionViewModel : ViewModelBase {`
  *Description:* ViewModel for selecting the primary element when merging multiple elements.
- **`Elements`**: `public IEnumerable<ElementTypeWrapperVM>? Elements {`
  *Description:* Gets or sets the collection of element wrappers to select from.
- **`SelectedPrimary`**: `public ElementTypeWrapperVM? SelectedPrimary {`
  *Description:* Gets or sets the selected primary element wrapper that will be the merge master.
- **`CloseAction`**: `public Action<bool>? CloseAction`
  *Description:* Gets or sets the action to close the window, passing a boolean result.
- **`OkCommand`**: `public ICommand OkCommand`
  *Description:* Gets the OK command.
- **`CancelCommand`**: `public ICommand CancelCommand`
  *Description:* Gets the Cancel command.

---
## Namespace: `Synthetic.Modules.RevitDOM`

### Class: `AppearanceAssetModel`
**File:** [src/SyntheticShared/Modules/RevitDOM/AppearanceAssetModel.cs](../src/SyntheticShared/Modules/RevitDOM/AppearanceAssetModel.cs)

#### Methods
##### `AppearanceAssetModel`
```csharp
public AppearanceAssetModel()
```
Initializes a new instance of the AppearanceAssetModel class.

#### Fields
- **`ObjectModel`**: `public class AppearanceAssetModel : ObjectModel {`
  *Description:* Represents a model for a Revit Appearance Asset (rendering data), inheriting from ObjectModel.
- **`Name`**: `public string Name`
  *Description:* Gets or sets the name of the appearance asset.
- **`Color`**: `public ColorModel? Color`
  *Description:* Gets or sets the color of the asset.
- **`Transparency`**: `public double Transparency`
  *Description:* Gets or sets the transparency value (range 0.0 to 1.0).
- **`Smoothness`**: `public double Smoothness`
  *Description:* Gets or sets the smoothness/glossiness value (range 0.0 to 1.0).

---
### Class: `BooleanModel`
**File:** [src/SyntheticShared/Modules/RevitDOM/BooleanModel.cs](../src/SyntheticShared/Modules/RevitDOM/BooleanModel.cs)

#### Methods
##### `BooleanModel`
```csharp
public BooleanModel()
```
Constructor

##### `BooleanModel`
```csharp
public BooleanModel(bool boolean)
```
Constructor

#### Fields
- **`ObjectModel`**: `public class BooleanModel : ObjectModel {`
  *Description:* Model for serializing Revit Objects
- **`boolean`**: `public bool boolean`
  *Description:* Value of the boolean

---
### Class: `BoundingBoxXYZModel`
**File:** [src/SyntheticShared/Modules/RevitDOM/BoundingBoxXYZModel.cs](../src/SyntheticShared/Modules/RevitDOM/BoundingBoxXYZModel.cs)

#### Fields
- **`ObjectModel`**: `public class BoundingBoxXYZModel : ObjectModel {`
  *Description:* Pure state container for BoundingBoxXYZ data, completely free of Revit API dependencies.

---
### Class: `BrowserOrganizationTranslator`
**File:** [src/SyntheticShared/Modules/RevitDOM/BrowserOrganizationTranslator.cs](../src/SyntheticShared/Modules/RevitDOM/BrowserOrganizationTranslator.cs)

---
### Class: `CategoryGraphicOverridesModel`
**File:** [src/SyntheticShared/Modules/RevitDOM/CategoryGraphicOverrideModel.cs](../src/SyntheticShared/Modules/RevitDOM/CategoryGraphicOverrideModel.cs)

#### Methods
##### `Category`
```csharp
public CategoryIdModel Category
```
Gets or sets the target category model.

##### `CategoryGraphicOverridesModel`
```csharp
public CategoryGraphicOverridesModel ()
```
Initializes a new instance of the CategoryGraphicOverridesModel class.

##### `IsModified`
```csharp
public bool IsModified()
```
Determines if the graphic overrides have been modified from default.

#### Fields
- **`ObjectModel`**: `public class CategoryGraphicOverridesModel : ObjectModel {`
  *Description:* Model representing graphic override settings for a specific category within a Revit view.
- **`ParentCategory`**: `public CategoryIdModel? ParentCategory`
  *Description:* Gets or sets the parent category model, if one exists.
- **`IsHidden`**: `public bool IsHidden`
  *Description:* Gets or sets a value indicating whether the category is hidden in the view.
- **`GraphicOverride`**: `public OverrideGraphicSettingsModel? GraphicOverride`
  *Description:* Gets or sets the graphic override settings for the category.

---
### Class: `CategoryIdModel`
**File:** [src/SyntheticShared/Modules/RevitDOM/CategoryIdModel.cs](../src/SyntheticShared/Modules/RevitDOM/CategoryIdModel.cs)

#### Methods
##### `ShouldSerializeId`
```csharp
public bool ShouldSerializeId()
```
If IsTemplate, don't serialize the Element Id

##### `CategoryIdModel`
```csharp
public CategoryIdModel ()
```
Initializes a new instance of the CategoryIdModel class.

#### Fields
- **`ObjectModel`**: `public class CategoryIdModel : ObjectModel {`
  *Description:* Model that acts as a wrapper for Revit Category ElementIds to facilitate JSON serialization.
- **`Name`**: `public string Name`
  *Description:* Gets or sets the name of the category.
- **`Id`**: `public long Id`
  *Description:* Gets or sets the integer ID of the category.
- **`Category`**: `public object? Category`
  *Description:* Gets or sets the associated Revit Category object. Ignored in JSON.
- **`Document`**: `public object? Document`
  *Description:* Gets or sets the associated Revit Document. Ignored in JSON.
- **`IsTemplate`**: `public bool IsTemplate`
  *Description:* If true, SerialElement is intended to be deserialized as a template for use as standards or transfer to another project. If false, SerialElement is intended to modify an element inside the project and will include ElementIds and UniqueIds.

---
### Class: `CategoryModel`
**File:** [src/SyntheticShared/Modules/RevitDOM/CategoryModel.cs](../src/SyntheticShared/Modules/RevitDOM/CategoryModel.cs)

#### Methods
##### `CategoryModel`
```csharp
public CategoryModel() : base()
```
Initializes a new instance of the CategoryModel class.

##### `PopulateParametersList`
```csharp
public void PopulateParametersList(object? doc = null)
```
Populates the model parameters list.

##### `SyncFromParameters`
```csharp
public void SyncFromParameters()
```
Synchronizes the model's property values from the internal parameters list.

#### Fields
- **`ElementModel`**: `public class CategoryModel : ElementModel {`
  *Description:* Model representation of a Revit Category, providing properties and methods to serialize and modify category settings.
- **`Class`**: `public override string Class {`
  *Description:* Gets the class grouping for categories, returning parent name or a default.
- **`Category`**: `public override string Category {`
  *Description:* Gets the category type identifier.
- **`ParentCategoryName`**: `public string? ParentCategoryName`
  *Description:* Gets or sets the name of the parent category, if applicable.
- **`CategoryGroupLevel1`**: `public string CategoryGroupLevel1 {`
  *Description:* Gets or sets the Level 1 category grouping (e.g. Line Styles, Model Categories).
- **`CategoryGroupLevel2`**: `public string CategoryGroupLevel2 {`
  *Description:* Gets or sets the Level 2 parent category grouping.
- **`IsCuttable`**: `public bool IsCuttable {`
  *Description:* Gets or sets a value indicating whether the category is cuttable in views.
- **`LineColor`**: `public ColorModel? LineColor`
  *Description:* Gets or sets the line color of the category.
- **`Material`**: `public ElementIdModel? Material`
  *Description:* Gets or sets the material model of the category.
- **`LineWeightProjection`**: `public int? LineWeightProjection`
  *Description:* Gets or sets the projection line weight.
- **`LineWeightCut`**: `public int? LineWeightCut`
  *Description:* Gets or sets the cut line weight.
- **`LinePatternProjection`**: `public ElementIdModel? LinePatternProjection`
  *Description:* Gets or sets the projection line pattern.
- **`LinePatternCut`**: `public ElementIdModel? LinePatternCut`
  *Description:* Gets or sets the cut line pattern.
- **`CategoryId`**: `public CategoryIdModel CategoryId`
  *Description:* Gets or sets the CategoryIdModel wrapper. Ignored in JSON.
- **`RevitCategory`**: `public object? RevitCategory {`
  *Description:* Gets or sets the underlying Revit Category object. Ignored in JSON.

---
### Class: `CategoryTranslator`
**File:** [src/SyntheticShared/Modules/RevitDOM/CategoryTranslator.cs](../src/SyntheticShared/Modules/RevitDOM/CategoryTranslator.cs)

---
### Class: `ColorModel`
**File:** [src/SyntheticShared/Modules/RevitDOM/ColorModel.cs](../src/SyntheticShared/Modules/RevitDOM/ColorModel.cs)

#### Methods
##### `ColorModel`
```csharp
public ColorModel()
```
Initializes a new instance of the ColorModel class as an invalid color.

##### `ColorModel`
```csharp
public ColorModel (Byte Red, Byte Green, Byte Blue)
```
Initializes a new instance of the ColorModel class with specific Red, Green, and Blue component values.

##### `ByJSON`
```csharp
public static ColorModel? ByJSON (string JSON)
```
Deserializes a JSON string into a ColorModel instance.

##### `ToJSON`
```csharp
public static string ToJSON (ColorModel color)
```
Serializes a ColorModel instance to a JSON string.

#### Fields
- **`ObjectModel`**: `public class ColorModel : ObjectModel {`
  *Description:* Model representing a Revit color value (Red, Green, Blue components).
- **`Blue`**: `public Byte Blue`
  *Description:* Gets or sets the Blue component value of the color (0-255).
- **`Green`**: `public Byte Green`
  *Description:* Gets or sets the Green component value of the color (0-255).
- **`Red`**: `public Byte Red`
  *Description:* Gets or sets the Red component value of the color (0-255).
- **`IsValid`**: `public bool IsValid`
  *Description:* Gets or sets a value indicating whether the color is valid.

---
### Class: `CompoundStructureModel`
**File:** [src/SyntheticShared/Modules/RevitDOM/CompoundStructureModel.cs](../src/SyntheticShared/Modules/RevitDOM/CompoundStructureModel.cs)

#### Methods
##### `Layers`
```csharp
public List<SerialCompoundStructureLayer> Layers
```
Gets or sets the list of layers in the compound structure.

##### `WallSweeps`
```csharp
public List<string> WallSweeps
```
Gets or sets the wall sweeps.

##### `CompoundStructureModel`
```csharp
public CompoundStructureModel ()
```
Initializes a new instance of the CompoundStructureModel class.

##### `MaterialId`
```csharp
public ElementIdModel MaterialId
```
Gets or sets the material ID model.

##### `DeckProfileId`
```csharp
public ElementIdModel DeckProfileId
```
Gets or sets the deck profile ID model.

##### `SerialCompoundStructureLayer`
```csharp
public SerialCompoundStructureLayer ()
```
Initializes a new instance of the SerialCompoundStructureLayer class.

#### Fields
- **`CompoundStructureModel`**: `public class CompoundStructureModel {`
  *Description:* Model representing a Revit CompoundStructure (e.g. wall layers).
- **`SerialCompoundStructureLayer`**: `public class SerialCompoundStructureLayer {`
  *Description:* Model representing a single layer in a CompoundStructure.
- **`Function`**: `public string Function`
  *Description:* Gets or sets the layer function name.
- **`Width`**: `public double Width`
  *Description:* Gets or sets the width of the layer in feet.
- **`DeckEmbeddingType`**: `public string DeckEmbeddingType`
  *Description:* Gets or sets the deck embedding type.
- **`LayerCapFlag`**: `public bool LayerCapFlag`
  *Description:* Gets or sets a value indicating whether the layer caps.
- **`StructuralMaterial`**: `public bool StructuralMaterial`
  *Description:* Gets or sets a value indicating whether the layer is the structural material.
- **`Priority`**: `public int Priority`
  *Description:* Gets or sets the priority of the layer (Revit 2026+).

---
### Class: `DimensionTypeModel`
**File:** [src/SyntheticShared/Modules/RevitDOM/DimensionTypeModel.cs](../src/SyntheticShared/Modules/RevitDOM/DimensionTypeModel.cs)

#### Methods
##### `DimensionTypeModel`
```csharp
public DimensionTypeModel() : base()
```
Initializes a new instance of the DimensionTypeModel class.

#### Fields
- **`ElementTypeModel`**: `public class DimensionTypeModel : ElementTypeModel {`
  *Description:* Serialize and deserialize Revit DimensionTypes
- **`DimensionStyle`**: `public string DimensionStyle`
  *Description:* Revit DimensionStyle Type Enum
- **`DimensionType`**: `public object? DimensionType`
  *Description:* Gets or sets the underlying Revit DimensionType object. Ignored in JSON.
- **`Element`**: `public override object? Element {`
  *Description:* Gets or sets the base Revit Element. Ignored in JSON.
- **`InternalDimStyleName`**: `public const string InternalDimStyleName`
  *Description:* Constant for the hidden built in linear dimension style. These styles shouldn't be serialized or used as templates to create new styles.

---
### Class: `DimensionTypeTranslator`
**File:** [src/SyntheticShared/Modules/RevitDOM/DimensionTypeTranslator.cs](../src/SyntheticShared/Modules/RevitDOM/DimensionTypeTranslator.cs)

---
### Class: `ElementIdModel`
**File:** [src/SyntheticShared/Modules/RevitDOM/ElementIdModel.cs](../src/SyntheticShared/Modules/RevitDOM/ElementIdModel.cs)

#### Methods
##### `ShouldSerializeId`
```csharp
public bool ShouldSerializeId()
```
Conditional serialization method for the Element Id. If IsTemplate is true, excludes the ID unless it is a built-in Revit element ID (represented by negative values). This ensures built-in/system IDs (e.g. built-in categories or solid line patterns) are preserved across document transfers.

##### `ShouldSerializeUniqueId`
```csharp
public bool ShouldSerializeUniqueId()
```
If IsTemplate, don't serialize the Unqiue Id

##### `ElementIdModel`
```csharp
public ElementIdModel ()
```
Initializes a new instance of the ElementIdModel class.

##### `ElementIdModel`
```csharp
public ElementIdModel(bool IsTemplate)
```
Initializes a new instance of the ElementIdModel class.

##### `ElementIdModel`
```csharp
public ElementIdModel (string Name, int ElementId, string Class, string Category)
```
Initializes a new instance of the ElementIdModel class.

##### `ByJSON`
```csharp
public static ElementIdModel? ByJSON (string JSON)
```
Deserializes a JSON string into an ElementIdModel instance.

##### `ToJSON`
```csharp
public static string ToJSON (ElementIdModel IdJSON)
```
Serializes an ElementIdModel instance to a JSON string.

#### Fields
- **`ObjectModel`**: `public class ElementIdModel : ObjectModel {`
  *Description:* Model that acts as a wrapper for Revit ElementIds to facilitate JSON serialization. Supports resolving element instances by UniqueId, Id, Name, or Aliases.
- **`Class`**: `public string Class`
  *Description:* Gets or sets the class name of the referenced Revit element.
- **`Category`**: `public string Category`
  *Description:* Gets or sets the category name of the referenced Revit element.
- **`Name`**: `public string Name`
  *Description:* Name of the element the Id belongs too.
- **`Aliases`**: `public List<string>? Aliases`
  *Description:* Gets or sets the list of name aliases to search if the primary Name is not found.
- **`Id`**: `public long Id`
  *Description:* Value of the Element Id as an int
- **`UniqueId`**: `public string? UniqueId`
  *Description:* Gets or sets the unique string identifier of the element.
- **`IsTemplate`**: `public bool IsTemplate`
  *Description:* If true, SerialElement is intended to be deserialized as a template for use as standards or transfer to another project. If false, SerialElement is intended to modify an element inside the project and will include ElementIds and UniqueIds.

---
### Class: `ElementModel`
**File:** [src/SyntheticShared/Modules/RevitDOM/ElementModel.cs](../src/SyntheticShared/Modules/RevitDOM/ElementModel.cs)

#### Methods
##### `Parameters`
```csharp
public List<ParameterModel> Parameters
```
List of the Parameters that belong to the Element.

##### `ElementId`
```csharp
public ElementIdModel ElementId
```
The Revit ElementId of the element linked to the SerialElement.

##### `ShouldSerializeId`
```csharp
public bool ShouldSerializeId()
```
If IsTemplate, don't serialize the Element Id

##### `ShouldSerializeUniqueId`
```csharp
public bool ShouldSerializeUniqueId()
```
If IsTemplate, don't serialize the Unqiue Id

##### `ElementModel`
```csharp
public ElementModel()
```
Initializes a new instance of the ElementModel class.

##### `SetAliases`
```csharp
public static ElementModel SetAliases (ElementModel serialElement, List<string> aliases)
```
Sets the SerialElement's aliases given a list of strings.

##### `ToString`
```csharp
public override string ToString()
```
Returns a string representation of the ElementModel.

##### `Clone`
```csharp
public object Clone()
```
Creates a shallow copy of the ElementModel.

##### `ByJSON`
```csharp
public static ElementModel ByJSON(string JSON)
```
Deserializes a JSON string into an ElementModel instance.

##### `ToJSON`
```csharp
public static string ToJSON(ElementModel serialElement)
```
Serializes an ElementModel instance to a JSON string.

#### Fields
- **`ObjectModel`**: `public class ElementModel : ObjectModel {`
  *Description:* Base model representing a Revit Element, providing properties and methods for serialization, parameter management, and modification.
- **`Class`**: `virtual public string Class {`
  *Description:* Gets or sets the class name of the element.
- **`Category`**: `virtual public string Category {`
  *Description:* Gets or sets the category name of the element.
- **`Name`**: `public string Name {`
  *Description:* Gets or sets the name of the element.
- **`Aliases`**: `public List<string>? Aliases {`
  *Description:* Gets or sets the list of name aliases.
- **`Id`**: `public long Id {`
  *Description:* Gets or sets the integer ID value of the element.
- **`UniqueId`**: `public string? UniqueId {`
  *Description:* Gets or sets the unique string ID of the element.
- **`Element`**: `virtual public object? Element`
  *Description:* The Revit Element that is linked to the SerialElement.
- **`Document`**: `public object? Document`
  *Description:* The Revit Document the element belongs too.
- **`IsTemplate`**: `public bool IsTemplate`
  *Description:* If true, SerialElement is intended to be deserialized as a template for use as standards or transfer to another project. If false, SerialElement is intended to modify an element inside the project and will include ElementIds and UniqueIds.
- **`DependencyOrigin`**: `public string? DependencyOrigin`
  *Description:* Gets or sets the name of the parent element that triggered the harvesting of this dependency.

---
### Class: `ElementTypeModel`
**File:** [src/SyntheticShared/Modules/RevitDOM/ElementTypeModel.cs](../src/SyntheticShared/Modules/RevitDOM/ElementTypeModel.cs)

#### Methods
##### `ElementTypeModel`
```csharp
public ElementTypeModel () : base ()
```
Initializes a new instance of the ElementTypeModel class.

#### Fields
- **`ElementModel`**: `public class ElementTypeModel : ElementModel {`
  *Description:* Model representation of a Revit ElementType, subclass of ElementModel.
- **`ElementType`**: `public object? ElementType`
  *Description:* Gets or sets the underlying Revit ElementType object. Ignored in JSON.
- **`Element`**: `public override object? Element {`
  *Description:* Gets or sets the base Revit Element. Ignored in JSON.

---
### Class: `EnumModel`
**File:** [src/SyntheticShared/Modules/RevitDOM/EnumModel.cs](../src/SyntheticShared/Modules/RevitDOM/EnumModel.cs)

#### Methods
##### `EnumModel`
```csharp
public EnumModel ()
```
Initializes a new instance of the EnumModel class.

##### `EnumModel`
```csharp
public EnumModel (Type type, Enum value)
```
Initializes a new instance of the EnumModel class using the specified type and value.

##### `EnumModel`
```csharp
public EnumModel (string type, string value)
```
Initializes a new instance of the EnumModel class using the type name and value string.

#### Fields
- **`ObjectModel`**: `public class EnumModel : ObjectModel {`
  *Description:* Represents a serialized Enum value with its type information.
- **`Type`**: `public string Type`
  *Description:* Gets or sets the full name of the Enum type.
- **`Value`**: `public string Value`
  *Description:* Gets or sets the string value of the Enum.

---
### Class: `FillGridModel`
**File:** [src/SyntheticShared/Modules/RevitDOM/FillGridModel.cs](../src/SyntheticShared/Modules/RevitDOM/FillGridModel.cs)

#### Methods
##### `FillGridModel`
```csharp
public FillGridModel()
```
Initializes a new instance of the FillGridModel class.

##### `FillGridModel`
```csharp
public FillGridModel (Double Angle, Double Offset, UVModel Origin, Double Shift)
```
Initializes a new instance of the FillGridModel class with specified angle, offset, origin, and shift.

##### `ByJSON`
```csharp
public static FillGridModel? ByJSON (string JSON)
```
Deserializes a FillGridModel from a JSON string.

##### `ToJSON`
```csharp
public static string ToJSON (FillGridModel color)
```
Serializes a FillGridModel to a JSON string.

#### Fields
- **`ObjectModel`**: `public class FillGridModel : ObjectModel {`
  *Description:* Represents a model for a Revit FillGrid.
- **`Angle`**: `public Double Angle`
  *Description:* Gets or sets the angle of the fill grid lines.
- **`Offset`**: `public Double Offset`
  *Description:* Gets or sets the offset of the fill grid lines.
- **`Origin`**: `public UVModel? Origin`
  *Description:* Gets or sets the origin point of the fill grid.
- **`Shift`**: `public Double Shift`
  *Description:* Gets or sets the shift of the fill grid lines.

---
### Class: `FillPatternModel`
**File:** [src/SyntheticShared/Modules/RevitDOM/FillPatternModel.cs](../src/SyntheticShared/Modules/RevitDOM/FillPatternModel.cs)

#### Methods
##### `FillGrids`
```csharp
public List<FillGridModel> FillGrids
```
List of Fillgrid objects

##### `FillPatternModel`
```csharp
public FillPatternModel()
```
Constructor for an empty object

##### `FillPatternModel`
```csharp
public FillPatternModel (string name, string target, string orientation, List<FillGridModel> fillgrids)
```
Constructor that takes the Name, Target, Orientation and Fillgrids

##### `ByJSON`
```csharp
public static FillPatternModel ByJSON (string JSON)
```
Create a new FillPatternModel by deserializing a JSON string

##### `ToJSON`
```csharp
public static string ToJSON (FillPatternModel fillPatternModel)
```
Serializes a FillPatternModel to a JSON string.

#### Fields
- **`ObjectModel`**: `public class FillPatternModel : ObjectModel {`
  *Description:* Represents a model for a Revit FillPattern.
- **`Name`**: `public string Name`
  *Description:* Name of the Fill Pattern
- **`Target`**: `public string Target`
  *Description:* Enum reprsenting Drafting or Model patterns
- **`HostOrientation`**: `public string HostOrientation`
  *Description:* Enum representing the orientation of the pattern relative the host or world.

---
### Class: `FillPatternTranslator`
**File:** [src/SyntheticShared/Modules/RevitDOM/FillPatternTranslator.cs](../src/SyntheticShared/Modules/RevitDOM/FillPatternTranslator.cs)

---
### Class: `FilledRegionTypeModel`
**File:** [src/SyntheticShared/Modules/RevitDOM/FilledRegionTypeModel.cs](../src/SyntheticShared/Modules/RevitDOM/FilledRegionTypeModel.cs)

#### Methods
##### `FilledRegionTypeModel`
```csharp
public FilledRegionTypeModel () : base ()
```
Initializes a new instance of the FilledRegionTypeModel class.

#### Fields
- **`ElementTypeModel`**: `public class FilledRegionTypeModel : ElementTypeModel {`
  *Description:* Represents a model for Revit FilledRegionType, inheriting from ElementTypeModel.
- **`FilledRegionType`**: `public object? FilledRegionType`
  *Description:* Gets or sets the Revit FilledRegionType associated with this model. Ignored in JSON.
- **`Element`**: `public override object? Element {`
  *Description:* Gets or sets the underlying Revit Element. Ignored in JSON.
- **`BackgroundPatternColor`**: `public ColorModel? BackgroundPatternColor`
  *Description:* Gets or sets the background pattern color.
- **`BackgroundPatternId`**: `public ElementIdModel? BackgroundPatternId`
  *Description:* Gets or sets the background pattern element ID.
- **`ForegroundPatternColor`**: `public ColorModel? ForegroundPatternColor`
  *Description:* Gets or sets the foreground pattern color.
- **`ForegroundPatternId`**: `public ElementIdModel? ForegroundPatternId`
  *Description:* Gets or sets the foreground pattern element ID.

---
### Class: `FilledRegionTypeTranslator`
**File:** [src/SyntheticShared/Modules/RevitDOM/FilledRegionTypeTranslator.cs](../src/SyntheticShared/Modules/RevitDOM/FilledRegionTypeTranslator.cs)

#### Methods
##### `ExtractSpecifics`
```csharp
public override void ExtractSpecifics(FilledRegionType revitElement, FilledRegionTypeModel model, Document doc)
```
Extracts graphic override properties from a native Revit FilledRegionType, populates the POCO properties, and purges redundant raw parameters.

##### `InjectSpecifics`
```csharp
public override FilledRegionType? InjectSpecifics(FilledRegionTypeModel model, FilledRegionType? revitElement, Document doc)
```
Injects graphic override properties from the POCO model into a native Revit FilledRegionType. Duplicates from template if it does not yet exist.

#### Fields
- **`FilledRegionTypeModel>`**: `internal class FilledRegionTypeTranslator : TemplateDuplicatingTranslator<FilledRegionType, FilledRegionTypeModel> {`
  *Description:* Translator governing the extraction and injection of FilledRegionType properties, implementing the "Elevate and Purge" strategy for graphic pattern and color overrides.

---
### Class: `FilterRuleModel`
**File:** [src/SyntheticShared/Modules/RevitDOM/FilterRuleModel.cs](../src/SyntheticShared/Modules/RevitDOM/FilterRuleModel.cs)

#### Methods
##### `InnerRules`
```csharp
public List<FilterRuleModel> InnerRules
```
Gets or sets a recursive list of inner rules (for logical filters).

#### Fields
- **`ObjectModel`**: `public class FilterRuleModel : ObjectModel {`
  *Description:* Represents a lightweight data structure to represent a single node in a filter's logical tree. Supports recursive logical nesting and parameter evaluation.
- **`RuleType`**: `public string RuleType`
  *Description:* Gets or sets the rule type (e.g., "LogicalAnd", "LogicalOr", "ParameterFilter", "HasValue").
- **`ParameterId`**: `public ElementIdModel? ParameterId`
  *Description:* Gets or sets the parameter being evaluated (if applicable).
- **`Evaluator`**: `public string? Evaluator`
  *Description:* Gets or sets the evaluator type encoded with rule type (e.g. "FilterStringRule:FilterStringEquals").
- **`RuleValue`**: `public string? RuleValue`
  *Description:* Gets or sets the threshold value to check against.

---
### Class: `GridTypeModel`
**File:** [src/SyntheticShared/Modules/RevitDOM/MissingModels.cs](../src/SyntheticShared/Modules/RevitDOM/MissingModels.cs)

#### Methods
##### `GridTypeModel`
```csharp
public GridTypeModel() : base()
```
Initializes a new instance of the GridTypeModel class.

##### `LevelTypeModel`
```csharp
public LevelTypeModel() : base()
```
Initializes a new instance of the LevelTypeModel class.

##### `FillPatternElementModel`
```csharp
public FillPatternElementModel() : base()
```
Initializes a new instance of the FillPatternElementModel class.

##### `Segments`
```csharp
public List<LinePatternSegmentModel> Segments
```
Gets or sets the list of segments defining the line pattern.

##### `LinePatternElementModel`
```csharp
public LinePatternElementModel() : base()
```
Initializes a new instance of the LinePatternElementModel class.

##### `PropertySetElementModel`
```csharp
public PropertySetElementModel() : base()
```
Initializes a new instance of the PropertySetElementModel class.

##### `CurtainSystemTypeModel`
```csharp
public CurtainSystemTypeModel() : base()
```
Initializes a new instance of the CurtainSystemTypeModel class.

##### `MullionTypeModel`
```csharp
public MullionTypeModel() : base()
```
Initializes a new instance of the MullionTypeModel class.

##### `FasciaTypeModel`
```csharp
public FasciaTypeModel() : base()
```
Initializes a new instance of the FasciaTypeModel class.

##### `GutterTypeModel`
```csharp
public GutterTypeModel() : base()
```
Initializes a new instance of the GutterTypeModel class.

##### `ViewFamilyTypeModel`
```csharp
public ViewFamilyTypeModel() : base()
```
Initializes a new instance of the ViewFamilyTypeModel class.

##### `BrowserOrganizationModel`
```csharp
public BrowserOrganizationModel() : base()
```
Initializes a new instance of the BrowserOrganizationModel class.

##### `Categories`
```csharp
public List<CategoryIdModel> Categories
```
Gets or sets the categories this parameter is bound to.

##### `ParameterElementModel`
```csharp
public ParameterElementModel() : base()
```
Initializes a new instance of the ParameterElementModel class.

#### Fields
- **`ElementTypeModel`**: `public class GridTypeModel : ElementTypeModel {`
  *Description:* Represents a model for a Revit GridType, inheriting from ElementTypeModel.
- **`ElementTypeModel`**: `public class LevelTypeModel : ElementTypeModel {`
  *Description:* Represents a model for a Revit LevelType, inheriting from ElementTypeModel.
- **`ElementModel`**: `public class FillPatternElementModel : ElementModel {`
  *Description:* Represents a model for a Revit FillPatternElement, inheriting from ElementModel.
- **`Pattern`**: `public FillPatternModel? Pattern`
  *Description:* Gets or sets the fill pattern model settings.
- **`LinePatternSegmentModel`**: `public class LinePatternSegmentModel {`
  *Description:* Represents a model segment for a Revit LinePattern.
- **`Type`**: `public string Type`
  *Description:* Gets or sets the segment type as a string (e.g. Dash, Space, Dot).
- **`Length`**: `public double Length`
  *Description:* Gets or sets the length of the segment.
- **`ElementModel`**: `public class LinePatternElementModel : ElementModel {`
  *Description:* Represents a model for a Revit LinePatternElement, inheriting from ElementModel.
- **`ElementModel`**: `public class PropertySetElementModel : ElementModel {`
  *Description:* Represents a model for a Revit PropertySetElement (Material Asset), inheriting from ElementModel.
- **`HostObjTypeModel`**: `public class CurtainSystemTypeModel : HostObjTypeModel {`
  *Description:* Represents a model for a Revit CurtainSystemType, inheriting from HostObjTypeModel.
- **`HostObjTypeModel`**: `public class MullionTypeModel : HostObjTypeModel {`
  *Description:* Represents a model for a Revit MullionType, inheriting from HostObjTypeModel.
- **`HostObjTypeModel`**: `public class FasciaTypeModel : HostObjTypeModel {`
  *Description:* Represents a model for a Revit FasciaType, inheriting from HostObjTypeModel.
- **`HostObjTypeModel`**: `public class GutterTypeModel : HostObjTypeModel {`
  *Description:* Represents a model for a Revit GutterType, inheriting from HostObjTypeModel.
- **`ElementTypeModel`**: `public class ViewFamilyTypeModel : ElementTypeModel {`
  *Description:* Represents a model for a Revit ViewFamilyType, inheriting from ElementTypeModel.
- **`ElementModel`**: `public class BrowserOrganizationModel : ElementModel {`
  *Description:* Represents a model for Revit BrowserOrganization, inheriting from ElementModel.
- **`ElementModel`**: `public class ParameterElementModel : ElementModel {`
  *Description:* Represents a model for a Revit ParameterElement, inheriting from ElementModel.
- **`IsInstanceBinding`**: `public bool IsInstanceBinding`
  *Description:* Gets or sets a value indicating whether the parameter is bound per-instance (true) or per-type (false).
- **`IsVaryByGroup`**: `public bool IsVaryByGroup`
  *Description:* Gets or sets a value indicating whether an instance parameter's values can vary across group instances.

---
### Class: `GridTypeTranslator`
**File:** [src/SyntheticShared/Modules/RevitDOM/GridTypeTranslator.cs](../src/SyntheticShared/Modules/RevitDOM/GridTypeTranslator.cs)

---
### Class: `HostObjTypeModel`
**File:** [src/SyntheticShared/Modules/RevitDOM/HostObjTypeModel.cs](../src/SyntheticShared/Modules/RevitDOM/HostObjTypeModel.cs)

#### Methods
##### `HostObjTypeModel`
```csharp
public HostObjTypeModel () : base ()
```
Initializes a new instance of the HostObjTypeModel class.

#### Fields
- **`ElementTypeModel`**: `public class HostObjTypeModel : ElementTypeModel {`
  *Description:* Represents a model for a Revit HostObjAttributes (e.g. WallType), inheriting from ElementTypeModel.
- **`Function`**: `public EnumModel? Function`
  *Description:* Gets or sets the functional classification of the host object type.
- **`Structure`**: `public CompoundStructureModel? Structure`
  *Description:* Gets or sets the compound structure of the host object type.
- **`WallType`**: `public object? WallType`
  *Description:* Gets or sets the Revit WallType / HostObjAttributes associated with this model. Ignored in JSON.
- **`Element`**: `public override object? Element {`
  *Description:* Gets or sets the underlying Revit Element. Ignored in JSON.

---
### Class: `HostObjTypeTranslator`
**File:** [src/SyntheticShared/Modules/RevitDOM/HostObjTypeTranslator.cs](../src/SyntheticShared/Modules/RevitDOM/HostObjTypeTranslator.cs)

#### Methods
##### `ExtractSpecifics`
```csharp
public override void ExtractSpecifics(ElementType revitElement, HostObjTypeModel model, Document doc)
```
Extracts CompoundStructure from the native ElementType (if it is a HostObjAttributes) and maps it to the model.

##### `InjectSpecifics`
```csharp
public override ElementType? InjectSpecifics(HostObjTypeModel model, ElementType? revitElement, Document doc)
```
Injects the CompoundStructure from the model into the native ElementType (if it supports compound structures).

##### `CreateCompoundStructure`
```csharp
private CompoundStructure CreateCompoundStructure(CompoundStructureModel structureModel, Document doc, HostObjTypeModel hostModel)
```
Reconstructs a native Revit CompoundStructure from a CompoundStructureModel, handling material resolution with graceful degradation and 2026+ layer priorities.

#### Fields
- **`HostObjTypeModel>`**: `internal class HostObjTypeTranslator : TemplateDuplicatingTranslator<ElementType, HostObjTypeModel> {`
  *Description:* Translator governing the extraction and injection of Revit system family types (derived from ElementType), managing the nested CompoundStructureModel layers with material identity resolution and priority settings.

---
### Class: `LevelTypeTranslator`
**File:** [src/SyntheticShared/Modules/RevitDOM/LevelTypeTranslator.cs](../src/SyntheticShared/Modules/RevitDOM/LevelTypeTranslator.cs)

---
### Class: `LinePatternTranslator`
**File:** [src/SyntheticShared/Modules/RevitDOM/LinePatternTranslator.cs](../src/SyntheticShared/Modules/RevitDOM/LinePatternTranslator.cs)

---
### Class: `ListModel`
**File:** [src/SyntheticShared/Modules/RevitDOM/ListModel.cs](../src/SyntheticShared/Modules/RevitDOM/ListModel.cs)

#### Methods
##### `ListModel`
```csharp
public ListModel()
```
Initializes a new instance of the ListModel class.

##### `ListModel`
```csharp
public ListModel(List<ElementModel> ElementJSONs)
```
Initializes a new instance of the ListModel class with the specified list of element models.

##### `ByJSON`
```csharp
public static ListModel ByJSON(string JSON)
```
Deserializes a ListModel from a JSON string.

##### `ToJSON`
```csharp
public static string ToJSON(ListModel ListJSON)
```
Serializes a ListModel to a JSON string.

##### `ToString`
```csharp
public override string ToString()
```
Returns a string representation of the ListModel.

##### `ListMaterialModel`
```csharp
public ListMaterialModel(List<MaterialModel> MaterialJSONs) : base()
```
Initializes a new instance of the ListMaterialModel class with a list of material models.

##### `ListMaterialModel`
```csharp
public ListMaterialModel()
```
Initializes a new instance of the ListMaterialModel class.

##### `ByJSON`
```csharp
public new static ListMaterialModel ByJSON(string JSON)
```
Deserializes a ListMaterialModel from a JSON string.

##### `ToJSON`
```csharp
public static string ToJSON(ListMaterialModel ListJSON)
```
Serializes a ListMaterialModel to a JSON string.

##### `ToString`
```csharp
public override string ToString()
```
Returns a string representation of the ListMaterialModel.

##### `Elements`
```csharp
public List<string> Elements
```
Gets or sets the list of strings.

##### `ListStringModel`
```csharp
public ListStringModel ()
```
Initializes a new instance of the ListStringModel class.

#### Fields
- **`ObjectModel`**: `public class ListModel : ObjectModel {`
  *Description:* Represents a serialized list of ElementModel objects.
- **`Elements`**: `public List<ElementModel> Elements`
  *Description:* Gets or sets the list of element models.
- **`ListModel`**: `public class ListMaterialModel : ListModel {`
  *Description:* Represents a serialized list of MaterialModel objects, inheriting from ListModel.
- **`Elements`**: `new public List<MaterialModel> Elements`
  *Description:* Gets or sets the list of material models. Hides the base list of element models.
- **`ListStringModel`**: `public class ListStringModel {`
  *Description:* Represents a serialized list of string values.

---
### Class: `MaterialModel`
**File:** [src/SyntheticShared/Modules/RevitDOM/MaterialModel.cs](../src/SyntheticShared/Modules/RevitDOM/MaterialModel.cs)

#### Methods
##### `MaterialModel`
```csharp
public MaterialModel () : base ()
```
Initializes a new instance of the MaterialModel class.

##### `ByJSON`
```csharp
public new static MaterialModel? ByJSON(string JSON)
```
Deserializes a MaterialModel from a JSON string.

##### `ToJSON`
```csharp
public static string ToJSON(MaterialModel materialJSON)
```
Serializes a MaterialModel to a JSON string.

#### Fields
- **`ElementModel`**: `public class MaterialModel : ElementModel {`
  *Description:* Represents a model for a Revit Material, inheriting from ElementModel.
- **`ClassName`**: `public const string ClassName`
  *Description:* The class name identifier for materials.
- **`AppearanceAssetId`**: `public ElementIdModel? AppearanceAssetId`
  *Description:* Gets or sets the appearance asset element ID model.
- **`AppearanceAsset`**: `public AppearanceAssetModel? AppearanceAsset`
  *Description:* Gets or sets the nested appearance asset model.
- **`StructuralAsset`**: `public StructuralAssetModel? StructuralAsset`
  *Description:* Gets or sets the nested structural physical asset model.
- **`ThermalAsset`**: `public ThermalAssetModel? ThermalAsset`
  *Description:* Gets or sets the nested thermal physical asset model.
- **`CutForegroundPatternColor`**: `public ColorModel? CutForegroundPatternColor`
  *Description:* Gets or sets the cut foreground pattern color.
- **`CutForegroundPatternId`**: `public ElementIdModel? CutForegroundPatternId`
  *Description:* Gets or sets the cut foreground pattern element ID model.
- **`CutBackgroundPatternColor`**: `public ColorModel? CutBackgroundPatternColor`
  *Description:* Gets or sets the cut background pattern color.
- **`CutBackgroundPatternId`**: `public ElementIdModel? CutBackgroundPatternId`
  *Description:* Gets or sets the cut background pattern element ID model.
- **`SurfaceForegroundPatternColor`**: `public ColorModel? SurfaceForegroundPatternColor`
  *Description:* Gets or sets the surface foreground pattern color.
- **`SurfaceForegroundPatternId`**: `public ElementIdModel? SurfaceForegroundPatternId`
  *Description:* Gets or sets the surface foreground pattern element ID model.
- **`SurfaceBackgroundPatternColor`**: `public ColorModel? SurfaceBackgroundPatternColor`
  *Description:* Gets or sets the surface background pattern color.
- **`SurfaceBackgroundPatternId`**: `public ElementIdModel? SurfaceBackgroundPatternId`
  *Description:* Gets or sets the surface background pattern element ID model.
- **`Material`**: `public object? Material`
  *Description:* Gets or sets the Revit Material object. Ignored in JSON.
- **`Element`**: `public override object? Element {`
  *Description:* Gets or sets the underlying Revit Element. Ignored in JSON.

---
### Class: `MaterialTranslator`
**File:** [src/SyntheticShared/Modules/RevitDOM/MaterialTranslator.cs](../src/SyntheticShared/Modules/RevitDOM/MaterialTranslator.cs)

---
### Class: `ModelDispatcher`
**File:** [src/SyntheticShared/Modules/RevitDOM/ModelDispatcher.cs](../src/SyntheticShared/Modules/RevitDOM/ModelDispatcher.cs)

#### Methods
##### `ModelDispatcher`
```csharp
public ModelDispatcher(IIdentityService identityService)
```
Initializes a new instance of the `ModelDispatcher` class.

##### `TTranslator>`
```csharp
public void Register<TModel, TTranslator>(TTranslator translator, params Type[] revitTypes) where TModel : ObjectModel where TTranslator : IModelTranslator
```
Registers a translator instance mapping a specific ObjectModel type and multiple Revit types.

##### `TTranslator>`
```csharp
public void Register<TModel, TTranslator>(params Type[] revitTypes) where TModel : ObjectModel where TTranslator : IModelTranslator, new()
```
Registers a translator by instantiating it with a parameterless constructor.

##### `RegisterIgnored`
```csharp
public void RegisterIgnored(params Type[] revitTypes)
```
Registers one or more Revit Element types to be explicitly ignored. Encountering these types during extraction will return null silently.

##### `RegisterPending`
```csharp
public void RegisterPending(params Type[] revitTypes)
```
Registers one or more Revit Element types as pending future support. Encountering these types during extraction will log a warning and return null.

##### `IsIgnored`
```csharp
public bool IsIgnored(Type revitType)
```
Checks if a Revit Element type is explicitly ignored.

##### `IsPending`
```csharp
public bool IsPending(Type revitType)
```
Checks if a Revit Element type is explicitly marked as pending.

##### `Extract`
```csharp
public ObjectModel? Extract(Element elem, bool isTemplate)
```
Extracts a native Revit Element into its corresponding ObjectModel.

##### `Extract`
```csharp
public ObjectModel? Extract(Category cat, Document doc, bool isTemplate)
```
Extracts a Category into its corresponding CategoryModel.

##### `GetTranslatorByModelType`
```csharp
public IModelTranslator? GetTranslatorByModelType(Type modelType)
```
Resolves the registered IModelTranslator for a given ObjectModel type.

##### `GetTranslatorByRevitType`
```csharp
public IModelTranslator? GetTranslatorByRevitType(Type revitType)
```
Resolves the registered IModelTranslator for a given native Revit Element type.

#### Fields
- **`ModelDispatcher`**: `internal class ModelDispatcher {`
  *Description:* Central dispatcher responsible for explicitly registering and routing ObjectModel types and Revit Element types to their corresponding IModelTranslator implementations. Supports One-to-Many routing configurations (e.g., mapping multiple Revit types to a single translator).

---
### Class: `ModelsToSerialize`
**File:** [src/SyntheticShared/Modules/RevitDOM/ModelsToSerialize.cs](../src/SyntheticShared/Modules/RevitDOM/ModelsToSerialize.cs)

#### Methods
##### `DeserializeByJson`
```csharp
public static IEnumerable<ElementModel> DeserializeByJson (string Json)
```
Deserializes a JSON string into a flat list of ElementModels.

##### `SerializeToJson`
```csharp
public static string SerializeToJson (List<ObjectModel> serialList)
```
Serializes a list of object models into a formatted JSON string.

##### `SerializeToJsonBySerialElementType`
```csharp
public static string SerializeToJsonBySerialElementType (List<ElementTypeModel> serialList)
```
Serializes a list of ElementTypeModel objects into a formatted JSON string.

##### `MigrateLegacyData`
```csharp
public void MigrateLegacyData()
```
Migrates legacy JSON data. Moves any TextElementType objects found in the generic ElementTypes dictionary to the new TextElementTypes dictionary.

#### Fields
- **`ModelsToSerialize`**: `public class ModelsToSerialize {`
  *Description:* Represents a container of various Revit element models to be serialized to/from JSON.
- **`FillPatternElements`**: `public Dictionary<string, FillPatternElementModel> FillPatternElements`
  *Description:* Gets or sets the dictionary of fill pattern elements.
- **`LinePatternElements`**: `public Dictionary<string, LinePatternElementModel> LinePatternElements`
  *Description:* Gets or sets the dictionary of line pattern elements.
- **`PropertySetElements`**: `public Dictionary<string, PropertySetElementModel> PropertySetElements`
  *Description:* Gets or sets the dictionary of property set elements (material assets).
- **`Materials`**: `public Dictionary<string, MaterialModel> Materials`
  *Description:* Gets or sets the dictionary of materials.
- **`ElementTypes`**: `public Dictionary<string, ElementTypeModel> ElementTypes`
  *Description:* Gets or sets the dictionary of element types.
- **`Categories`**: `public Dictionary<string, CategoryModel> Categories`
  *Description:* Gets or sets the dictionary of category override models.
- **`HostObjTypes`**: `public Dictionary<string, HostObjTypeModel> HostObjTypes`
  *Description:* Gets or sets the dictionary of host object types.
- **`FillRegionTypes`**: `public Dictionary<string, FilledRegionTypeModel> FillRegionTypes {  get; set; }`
  *Description:* Gets or sets the dictionary of filled region types.
- **`DimensionTypes`**: `public Dictionary<string, DimensionTypeModel> DimensionTypes`
  *Description:* Gets or sets the dictionary of dimension types.
- **`TextNoteTypes`**: `public Dictionary<string, ElementTypeModel> TextNoteTypes`
  *Description:* Gets or sets the dictionary of text note types.
- **`TextElementTypes`**: `public Dictionary<string, ElementTypeModel> TextElementTypes`
  *Description:* Gets or sets the dictionary of text element types (family-level labels).
- **`ModelTextTypes`**: `public Dictionary<string, ElementTypeModel> ModelTextTypes`
  *Description:* Gets or sets the dictionary of model text types.
- **`SpotDimensionTypes`**: `public Dictionary<string, ElementTypeModel> SpotDimensionTypes`
  *Description:* Gets or sets the dictionary of spot dimension types.
- **`ToposolidTypes`**: `public Dictionary<string, HostObjTypeModel> ToposolidTypes`
  *Description:* Gets or sets the dictionary of toposolid types.
- **`ViewTemplates`**: `public Dictionary<string, ViewModel> ViewTemplates`
  *Description:* Gets or sets the dictionary of view templates.
- **`Views`**: `public Dictionary<string, ViewModel> Views`
  *Description:* Gets or sets the dictionary of views.
- **`Elements`**: `public List<ElementModel> Elements`
  *Description:* Gets or sets the list of generic element models.
- **`GridTypes`**: `public Dictionary<string, GridTypeModel> GridTypes`
  *Description:* Gets or sets the dictionary of grid types.
- **`LevelTypes`**: `public Dictionary<string, LevelTypeModel> LevelTypes`
  *Description:* Gets or sets the dictionary of level types.
- **`CurtainSystemTypes`**: `public Dictionary<string, CurtainSystemTypeModel> CurtainSystemTypes`
  *Description:* Gets or sets the dictionary of curtain system types.
- **`MullionTypes`**: `public Dictionary<string, MullionTypeModel> MullionTypes`
  *Description:* Gets or sets the dictionary of mullion types.
- **`FasciaTypes`**: `public Dictionary<string, FasciaTypeModel> FasciaTypes`
  *Description:* Gets or sets the dictionary of fascia types.
- **`GutterTypes`**: `public Dictionary<string, GutterTypeModel> GutterTypes`
  *Description:* Gets or sets the dictionary of gutter types.
- **`ViewFamilyTypes`**: `public Dictionary<string, ViewFamilyTypeModel> ViewFamilyTypes`
  *Description:* Gets or sets the dictionary of view family types.
- **`BrowserOrganizations`**: `public Dictionary<string, BrowserOrganizationModel> BrowserOrganizations`
  *Description:* Gets or sets the dictionary of browser organizations.
- **`ParameterElements`**: `public Dictionary<string, ParameterElementModel> ParameterElements`
  *Description:* Gets or sets the dictionary of parameter elements.
- **`ParameterFilters`**: `public Dictionary<string, ParameterFilterElementModel> ParameterFilters`
  *Description:* Gets or sets the dictionary of parameter filter elements (view filters).

---
### Class: `ObjectModel`
**File:** [src/SyntheticShared/Modules/RevitDOM/ObjectModel.cs](../src/SyntheticShared/Modules/RevitDOM/ObjectModel.cs)

#### Methods
##### `ObjectModel`
```csharp
public ObjectModel()
```
Constructor

#### Fields
- **`ObjectModel`**: `public class ObjectModel {`
  *Description:* Basic class for generic objects. Used to inherit from.

---
### Class: `OverrideGraphicSettingsModel`
**File:** [src/SyntheticShared/Modules/RevitDOM/OverrideGraphicSettingsModel.cs](../src/SyntheticShared/Modules/RevitDOM/OverrideGraphicSettingsModel.cs)

#### Methods
##### `OverrideGraphicSettingsModel`
```csharp
public OverrideGraphicSettingsModel ()
```
Initializes a new instance of the OverrideGraphicSettingsModel class.

#### Fields
- **`ObjectModel`**: `public class OverrideGraphicSettingsModel : ObjectModel {`
  *Description:* Represents a model for Revit OverrideGraphicSettings, mapping visibility and style overrides.
- **`IsModified`**: `public bool IsModified`
  *Description:* Gets or sets a value indicating whether any settings have been modified. Ignored in JSON.
- **`IsSurfaceBackgroundPatternVisible`**: `public bool IsSurfaceBackgroundPatternVisible`
  *Description:* Gets or sets a value indicating whether surface background pattern is visible.
- **`SurfaceBackgroundPatternColor`**: `public ColorModel? SurfaceBackgroundPatternColor`
  *Description:* Gets or sets the surface background pattern color.
- **`SurfaceBackgroundPatternId`**: `public ElementIdModel? SurfaceBackgroundPatternId`
  *Description:* Gets or sets the surface background pattern element ID model.
- **`IsSurfaceForegroundPatternVisible`**: `public bool IsSurfaceForegroundPatternVisible`
  *Description:* Gets or sets a value indicating whether surface foreground pattern is visible.
- **`SurfaceForegroundPatternColor`**: `public ColorModel? SurfaceForegroundPatternColor`
  *Description:* Gets or sets the surface foreground pattern color.
- **`SurfaceForegroundPatternId`**: `public ElementIdModel? SurfaceForegroundPatternId`
  *Description:* Gets or sets the surface foreground pattern element ID model.
- **`ProjectionLineColor`**: `public ColorModel? ProjectionLineColor`
  *Description:* Gets or sets the projection line color.
- **`ProjectionLinePatternId`**: `public ElementIdModel? ProjectionLinePatternId`
  *Description:* Gets or sets the projection line pattern element ID model.
- **`ProjectionLineWeight`**: `public int ProjectionLineWeight`
  *Description:* Gets or sets the projection line weight.
- **`IsCutBackgroundPatternVisible`**: `public bool IsCutBackgroundPatternVisible`
  *Description:* Gets or sets a value indicating whether cut background pattern is visible.
- **`CutBackgroundPatternColor`**: `public ColorModel? CutBackgroundPatternColor`
  *Description:* Gets or sets the cut background pattern color.
- **`CutBackgroundPatternId`**: `public ElementIdModel? CutBackgroundPatternId`
  *Description:* Gets or sets the cut background pattern element ID model.
- **`IsCutForegroundPatternVisible`**: `public bool IsCutForegroundPatternVisible`
  *Description:* Gets or sets a value indicating whether cut foreground pattern is visible.
- **`CutForegroundPatternColor`**: `public ColorModel? CutForegroundPatternColor`
  *Description:* Gets or sets the cut foreground pattern color.
- **`CutForegroundPatternId`**: `public ElementIdModel? CutForegroundPatternId`
  *Description:* Gets or sets the cut foreground pattern element ID model.
- **`CutLineColor`**: `public ColorModel? CutLineColor`
  *Description:* Gets or sets the cut line color.
- **`CutLinePatternId`**: `public ElementIdModel? CutLinePatternId`
  *Description:* Gets or sets the cut line pattern element ID model.
- **`CutLineWeight`**: `public int CutLineWeight`
  *Description:* Gets or sets the cut line weight.
- **`Transparency`**: `public int Transparency`
  *Description:* Gets or sets the transparency percentage (0-100).
- **`Halftone`**: `public bool Halftone`
  *Description:* Gets or sets a value indicating whether halftone is enabled.
- **`DetailLevel`**: `public EnumModel? DetailLevel`
  *Description:* Gets or sets the view detail level override.

---
### Class: `ParameterElementTranslator`
**File:** [src/SyntheticShared/Modules/RevitDOM/ParameterElementTranslator.cs](../src/SyntheticShared/Modules/RevitDOM/ParameterElementTranslator.cs)

---
### Class: `ParameterFilterElementModel`
**File:** [src/SyntheticShared/Modules/RevitDOM/ParameterFilterElementModel.cs](../src/SyntheticShared/Modules/RevitDOM/ParameterFilterElementModel.cs)

#### Methods
##### `Categories`
```csharp
public List<CategoryIdModel> Categories
```
Gets or sets the list of category models associated with this filter.

##### `ParameterFilterElementModel`
```csharp
public ParameterFilterElementModel()
```
Initializes a new instance of the ParameterFilterElementModel class.

#### Fields
- **`ElementModel`**: `public class ParameterFilterElementModel : ElementModel {`
  *Description:* Represents a model representation of a Revit ParameterFilterElement (View Filter), inheriting from ElementModel. Deeply serializes categories and filter rules.
- **`RootRule`**: `public FilterRuleModel? RootRule`
  *Description:* Gets or sets the recursive root rule model representing the logic tree of the filter.

---
### Class: `ParameterFilterElementTranslator`
**File:** [src/SyntheticShared/Modules/RevitDOM/ParameterFilterElementTranslator.cs](../src/SyntheticShared/Modules/RevitDOM/ParameterFilterElementTranslator.cs)

---
### Class: `ParameterModel`
**File:** [src/SyntheticShared/Modules/RevitDOM/ParameterModel.cs](../src/SyntheticShared/Modules/RevitDOM/ParameterModel.cs)

#### Methods
##### `ParameterModel`
```csharp
public ParameterModel()
```
Initializes a new instance of the ParameterModel class.

##### `ToJSON`
```csharp
public static string ToJSON(ParameterModel parameter)
```
Serializes a ParameterModel to a JSON string.

#### Fields
- **`ObjectModel`**: `public class ParameterModel : ObjectModel {`
  *Description:* Represents a model representation of a Revit Parameter, mapping name, value, and storage type.
- **`Name`**: `public string Name`
  *Description:* Gets or sets the parameter name.
- **`Value`**: `public string? Value`
  *Description:* Gets or sets the parameter value as a string.
- **`ValueElemId`**: `public ElementIdModel? ValueElemId`
  *Description:* Gets or sets the parameter value as an ElementId model, if the parameter storage type is ElementId.
- **`StorageType`**: `public string StorageType`
  *Description:* Gets or sets the storage type of the parameter (e.g. Double, Integer, String, ElementId).
- **`Id`**: `public long Id`
  *Description:* Gets or sets the integer value of the parameter definition ID.
- **`GUID`**: `public string? GUID`
  *Description:* Gets or sets the GUID of the parameter, if it is a shared parameter.
- **`IsShared`**: `public bool IsShared`
  *Description:* Gets or sets a value indicating whether this is a shared parameter.
- **`IsReadOnly`**: `public bool IsReadOnly`
  *Description:* Gets or sets a value indicating whether this parameter is read-only.
- **`IsTemplate`**: `public bool IsTemplate`
  *Description:* If true, SerialParameter is intended to be deserialized as a template for use as standards or transfer to another project. If false, SerialParameter is intended to modify an element inside the project and will include ElementIds and UniqueIds.

---
### Class: `PlanViewRangeModel`
**File:** [src/SyntheticShared/Modules/RevitDOM/PlanViewRangeModel.cs](../src/SyntheticShared/Modules/RevitDOM/PlanViewRangeModel.cs)

#### Methods
##### `PlanViewRangeModel`
```csharp
public PlanViewRangeModel()
```
Initializes a new instance of the `PlanViewRangeModel` class.

#### Fields
- **`ObjectModel`**: `public class PlanViewRangeModel : ObjectModel {`
  *Description:* Represents a lightweight data structure mapping PlanViewRange offsets and level references.
- **`TopOffset`**: `public double TopOffset`
  *Description:* Gets or sets the top offset value of the view range.
- **`TopLevelId`**: `public ElementIdModel? TopLevelId`
  *Description:* Gets or sets the top level ID reference.
- **`CutPlaneOffset`**: `public double CutPlaneOffset`
  *Description:* Gets or sets the cut plane offset value.
- **`CutPlaneLevelId`**: `public ElementIdModel? CutPlaneLevelId`
  *Description:* Gets or sets the cut plane level ID reference.
- **`BottomOffset`**: `public double BottomOffset`
  *Description:* Gets or sets the bottom offset value.
- **`BottomLevelId`**: `public ElementIdModel? BottomLevelId`
  *Description:* Gets or sets the bottom level ID reference.
- **`ViewDepthOffset`**: `public double ViewDepthOffset`
  *Description:* Gets or sets the view depth offset value.
- **`ViewDepthLevelId`**: `public ElementIdModel? ViewDepthLevelId`
  *Description:* Gets or sets the view depth level ID reference.

---
### Class: `RevitIdentityService`
**File:** [src/SyntheticShared/Modules/RevitDOM/RevitIdentityService.cs](../src/SyntheticShared/Modules/RevitDOM/RevitIdentityService.cs)

#### Fields
- **`IIdentityService`**: `internal class RevitIdentityService : IIdentityService {`
  *Description:* Production implementation of IIdentityService that interacts with a live Revit Document.

---
### Class: `SerializationResultModel`
**File:** [src/SyntheticShared/Modules/RevitDOM/SerializationResultModel.cs](../src/SyntheticShared/Modules/RevitDOM/SerializationResultModel.cs)

#### Methods
##### `Warnings`
```csharp
public List<string> Warnings
```
Gets the warnings encountered during operation.

##### `SerializationResultModel`
```csharp
public SerializationResultModel(ObjectModel model, ElementIdModel? elementIdentity = null)
```
Initializes a new instance of the `SerializationResultModel` class for a successful operation.

##### `SerializationResultModel`
```csharp
public SerializationResultModel(ObjectModel model, string errorMessage, Exception? exception = null)
```
Initializes a new instance of the `SerializationResultModel` class for a failed operation.

#### Fields
- **`SerializationResultModel`**: `public class SerializationResultModel {`
  *Description:* Represents the outcome of a single ObjectModel serialization attempt during a batch import.
- **`Model`**: `public ObjectModel Model`
  *Description:* Gets the model that was processed.
- **`Success`**: `public bool Success`
  *Description:* Gets a value indicating whether the serialization/import succeeded.
- **`ErrorMessage`**: `public string? ErrorMessage`
  *Description:* Gets the error message if the import failed; otherwise, null.
- **`Exception`**: `public Exception? Exception`
  *Description:* Gets the Exception object if the import failed; otherwise, null.
- **`ElementIdentity`**: `public ElementIdModel? ElementIdentity`
  *Description:* Gets the element representation model (identity) of the created or modified element, if successful.
- **`OperationTarget`**: `public string OperationTarget`
  *Description:* Gets or sets the target of the operation (e.g. "Database", "File").
- **`Action`**: `public string? Action`
  *Description:* Gets or sets the action performed (e.g. "Merged Alias", "Alias Swap Failed").
- **`Message`**: `public string? Message`
  *Description:* Gets or sets a descriptive message detailing the operation's outcome.

---
### Class: `SpotDimensionTypeTranslator`
**File:** [src/SyntheticShared/Modules/RevitDOM/SpotDimensionTypeTranslator.cs](../src/SyntheticShared/Modules/RevitDOM/SpotDimensionTypeTranslator.cs)

---
### Class: `StandardSerializationEngine`
**File:** [src/SyntheticShared/Modules/RevitDOM/StandardSerializationEngine.cs](../src/SyntheticShared/Modules/RevitDOM/StandardSerializationEngine.cs)

#### Methods
##### `StandardSerializationEngine`
```csharp
public StandardSerializationEngine() : this(null)
```
Initializes a new instance of the `StandardSerializationEngine` class.

##### `StandardSerializationEngine`
```csharp
internal StandardSerializationEngine(IIdentityService? identityService)
```
Initializes a new instance of the `StandardSerializationEngine` class with a custom identity service.

##### `ByRevit`
```csharp
public IEnumerable<ObjectModel> ByRevit( IEnumerable<Element> elements, Document doc, bool isTemplate, IProgress<string>? progress = null, CancellationToken cancellationToken = default)
```
Extracts Revit elements into pure ObjectModel state containers.

##### `ToRevit`
```csharp
public IEnumerable<SerializationResultModel> ToRevit( IEnumerable<ObjectModel> models, Document doc, IProgress<string>? progress = null, CancellationToken cancellationToken = default)
```
Writes ObjectModel instances back to the Revit database, managing transaction assimilation and rollbacks.

#### Fields
- **`StandardSerializationEngine`**: `public class StandardSerializationEngine {`
  *Description:* The public interface and entry point for the RevitDOM serialization and transaction engine. Exposes methods to extract Revit elements to pure models, analyze differences, and write models back to Revit.
- **`Dispatcher`**: `internal ModelDispatcher Dispatcher`
  *Description:* Gets the internal dispatcher orchestrating the type-specific translators.
- **`_unsupportedInFamilyDoc`**: `private static readonly HashSet<Type> _unsupportedInFamilyDoc`
  *Description:* Analyzes a set of ObjectModel instances against the live Revit database and returns diff/duplicate report clusters.

---
### Class: `StructuralAssetModel`
**File:** [src/SyntheticShared/Modules/RevitDOM/StructuralAssetModel.cs](../src/SyntheticShared/Modules/RevitDOM/StructuralAssetModel.cs)

#### Methods
##### `StructuralAssetModel`
```csharp
public StructuralAssetModel()
```
Initializes a new instance of the StructuralAssetModel class.

#### Fields
- **`ObjectModel`**: `public class StructuralAssetModel : ObjectModel {`
  *Description:* Represents a model for a Revit Structural Physical Asset (e.g. concrete/metal structural properties), inheriting from ObjectModel.
- **`Name`**: `public string Name`
  *Description:* Gets or sets the name of the structural asset.
- **`Behavior`**: `public string Behavior`
  *Description:* Gets or sets the physical behavior mode (e.g. Isotropic, Orthotropic).
- **`StructuralAssetClass`**: `public string StructuralAssetClass`
  *Description:* Gets or sets the material structural asset class (e.g. Metal, Concrete).
- **`Density`**: `public double Density`
  *Description:* Gets or sets the density of the material.
- **`YoungModulus`**: `public double YoungModulus`
  *Description:* Gets or sets the Young's Modulus value (pa).
- **`PoissonRatio`**: `public double PoissonRatio`
  *Description:* Gets or sets the Poisson's Ratio value.

---
### Class: `TextElementTypeTranslator`
**File:** [src/SyntheticShared/Modules/RevitDOM/TextElementTypeTranslator.cs](../src/SyntheticShared/Modules/RevitDOM/TextElementTypeTranslator.cs)

---
### Class: `TextNoteTypeTranslator`
**File:** [src/SyntheticShared/Modules/RevitDOM/TextNoteTypeTranslator.cs](../src/SyntheticShared/Modules/RevitDOM/TextNoteTypeTranslator.cs)

---
### Class: `ThermalAssetModel`
**File:** [src/SyntheticShared/Modules/RevitDOM/ThermalAssetModel.cs](../src/SyntheticShared/Modules/RevitDOM/ThermalAssetModel.cs)

#### Methods
##### `ThermalAssetModel`
```csharp
public ThermalAssetModel()
```
Initializes a new instance of the ThermalAssetModel class.

#### Fields
- **`ObjectModel`**: `public class ThermalAssetModel : ObjectModel {`
  *Description:* Represents a model for a Revit Thermal Physical Asset, inheriting from ObjectModel.
- **`Name`**: `public string Name`
  *Description:* Gets or sets the name of the thermal asset.
- **`ThermalMaterialType`**: `public string ThermalMaterialType`
  *Description:* Gets or sets the thermal material type (e.g. Solid, Liquid, Gas).
- **`Density`**: `public double Density`
  *Description:* Gets or sets the density of the thermal asset.
- **`ThermalConductivity`**: `public double ThermalConductivity`
  *Description:* Gets or sets the thermal conductivity value (W / (m * K)).
- **`SpecificHeat`**: `public double SpecificHeat`
  *Description:* Gets or sets the specific heat value (J / (kg * K)).
- **`Emissivity`**: `public double Emissivity`
  *Description:* Gets or sets the emissivity value (dimensionless).

---
### Class: `TransformModel`
**File:** [src/SyntheticShared/Modules/RevitDOM/TransformModel.cs](../src/SyntheticShared/Modules/RevitDOM/TransformModel.cs)

#### Fields
- **`ObjectModel`**: `public class TransformModel : ObjectModel {`
  *Description:* Pure state container for Transform coordinate transformation data, completely free of Revit API dependencies.

---
### Class: `UVModel`
**File:** [src/SyntheticShared/Modules/RevitDOM/UVModel.cs](../src/SyntheticShared/Modules/RevitDOM/UVModel.cs)

#### Fields
- **`ObjectModel`**: `public class UVModel : ObjectModel {`
  *Description:* Pure state container for UV coordinate data, completely free of Revit API dependencies.

---
### Class: `ViewFilterOverrideModel`
**File:** [src/SyntheticShared/Modules/RevitDOM/ViewFilterOverrideModel.cs](../src/SyntheticShared/Modules/RevitDOM/ViewFilterOverrideModel.cs)

#### Methods
##### `ViewFilterOverrideModel`
```csharp
public ViewFilterOverrideModel ()
```
Initializes a new instance of the ViewFilterOverrideModel class.

#### Fields
- **`ObjectModel`**: `public class ViewFilterOverrideModel : ObjectModel {`
  *Description:* Represents a model for Revit View Filter overrides, wrapping filter identity, visibility, and style overrides.
- **`FilterId`**: `public ElementIdModel? FilterId`
  *Description:* Gets or sets the ElementIdModel representing the Revit Filter element.
- **`IsVisible`**: `public bool IsVisible`
  *Description:* Gets or sets a value indicating whether the filter is visible in the view.
- **`IsSurfaceBackgroundPatternVisible`**: `public bool IsSurfaceBackgroundPatternVisible`
  *Description:* Gets or sets a value indicating whether surface background pattern is visible.
- **`SurfaceBackgroundPatternColor`**: `public ColorModel? SurfaceBackgroundPatternColor`
  *Description:* Gets or sets the surface background pattern color.
- **`SurfaceBackgroundPatternId`**: `public ElementIdModel? SurfaceBackgroundPatternId`
  *Description:* Gets or sets the surface background pattern element ID model.
- **`IsSurfaceForegroundPatternVisible`**: `public bool IsSurfaceForegroundPatternVisible`
  *Description:* Gets or sets a value indicating whether surface foreground pattern is visible.
- **`SurfaceForegroundPatternColor`**: `public ColorModel? SurfaceForegroundPatternColor`
  *Description:* Gets or sets the surface foreground pattern color.
- **`SurfaceForegroundPatternId`**: `public ElementIdModel? SurfaceForegroundPatternId`
  *Description:* Gets or sets the surface foreground pattern element ID model.
- **`ProjectionLineColor`**: `public ColorModel? ProjectionLineColor`
  *Description:* Gets or sets the projection line color.
- **`ProjectionLinePatternId`**: `public ElementIdModel? ProjectionLinePatternId`
  *Description:* Gets or sets the projection line pattern element ID model.
- **`ProjectionLineWeight`**: `public int ProjectionLineWeight`
  *Description:* Gets or sets the projection line weight.
- **`IsCutBackgroundPatternVisible`**: `public bool IsCutBackgroundPatternVisible`
  *Description:* Gets or sets a value indicating whether cut background pattern is visible.
- **`CutBackgroundPatternColor`**: `public ColorModel? CutBackgroundPatternColor`
  *Description:* Gets or sets the cut background pattern color.
- **`CutBackgroundPatternId`**: `public ElementIdModel? CutBackgroundPatternId`
  *Description:* Gets or sets the cut background pattern element ID model.
- **`IsCutForegroundPatternVisible`**: `public bool IsCutForegroundPatternVisible`
  *Description:* Gets or sets a value indicating whether cut foreground pattern is visible.
- **`CutForegroundPatternColor`**: `public ColorModel? CutForegroundPatternColor`
  *Description:* Gets or sets the cut foreground pattern color.
- **`CutForegroundPatternId`**: `public ElementIdModel? CutForegroundPatternId`
  *Description:* Gets or sets the cut foreground pattern element ID model.
- **`CutLineColor`**: `public ColorModel? CutLineColor`
  *Description:* Gets or sets the cut line color.
- **`CutLinePatternId`**: `public ElementIdModel? CutLinePatternId`
  *Description:* Gets or sets the cut line pattern element ID model.
- **`CutLineWeight`**: `public int CutLineWeight`
  *Description:* Gets or sets the cut line weight.
- **`Transparency`**: `public int Transparency`
  *Description:* Gets or sets the transparency percentage (0-100).
- **`Halftone`**: `public bool Halftone`
  *Description:* Gets or sets a value indicating whether halftone is enabled.
- **`DetailLevel`**: `public EnumModel? DetailLevel`
  *Description:* Gets or sets the view detail level override.

---
### Class: `ViewModel`
**File:** [src/SyntheticShared/Modules/RevitDOM/ViewModel.cs](../src/SyntheticShared/Modules/RevitDOM/ViewModel.cs)

#### Methods
##### `NonControlledParameterIds`
```csharp
public List<int> NonControlledParameterIds
```
Gets or sets the list of parameter IDs that are not controlled by this template.

##### `ViewModel`
```csharp
public ViewModel ()
```
Initializes a new instance of the ViewModel class.

#### Fields
- **`ElementModel`**: `public class ViewModel : ElementModel {`
  *Description:* Represents a model representation of a Revit View, inheriting from ElementModel.
- **`CategoryKey`**: `public const string CategoryKey`
  *Description:* Key name for Category overrides.
- **`HiddenKey`**: `public const string HiddenKey`
  *Description:* Key name for Hidden status.
- **`OverrideKey`**: `public const string OverrideKey`
  *Description:* Key name for Override Graphic Settings.
- **`IsTemplate`**: `public new bool IsTemplate`
  *Description:* Gets or sets a value indicating whether this model is a template view/template.
- **`ViewTemplate`**: `public object? ViewTemplate {`
  *Description:* Gets or sets the associated Revit View Template object. Ignored in JSON.
- **`DisplayStyle`**: `public EnumModel? DisplayStyle`
  *Description:* Gets or sets the display style of the view.
- **`ShadowIntensity`**: `public int? ShadowIntensity`
  *Description:* Gets or sets the shadow intensity.
- **`SunlightIntensity`**: `public int? SunlightIntensity`
  *Description:* Gets or sets the sunlight intensity.
- **`Scale`**: `public int? Scale`
  *Description:* Gets or sets the view scale.
- **`DetailLevel`**: `public EnumModel? DetailLevel`
  *Description:* Gets or sets the view detail level.
- **`Discipline`**: `public EnumModel? Discipline`
  *Description:* Gets or sets the view discipline.
- **`Phase`**: `public ElementIdModel? Phase`
  *Description:* Gets or sets the view phase.
- **`PhaseFilter`**: `public ElementIdModel? PhaseFilter`
  *Description:* Gets or sets the view phase filter.
- **`ViewFilterOverrides`**: `public List<ViewFilterOverrideModel>? ViewFilterOverrides`
  *Description:* Gets or sets the list of view filter overrides applied to this view.
- **`CategoryGraphicOverrides`**: `public List<CategoryGraphicOverridesModel>? CategoryGraphicOverrides`
  *Description:* Gets or sets the list of graphic overrides applied to categories in this view.
- **`LevelId`**: `public ElementIdModel? LevelId`
  *Description:* Gets or sets the associated Level ID (only applies to plan views).
- **`ViewFamilyTypeId`**: `public ElementIdModel? ViewFamilyTypeId`
  *Description:* Gets or sets the associated ViewFamilyType ID.
- **`CropBox`**: `public BoundingBoxXYZModel? CropBox`
  *Description:* Gets or sets the view crop box (only applies to section, elevation, and plan views).
- **`ScopeBoxId`**: `public ElementIdModel? ScopeBoxId`
  *Description:* Gets or sets the associated Scope Box ID.
- **`CropBoxActive`**: `public bool? CropBoxActive`
  *Description:* Gets or sets a value indicating whether the crop box is active.
- **`CropBoxVisible`**: `public bool? CropBoxVisible`
  *Description:* Gets or sets a value indicating whether the crop box is visible.
- **`AnnotationCropActive`**: `public bool? AnnotationCropActive`
  *Description:* Gets or sets a value indicating whether the annotation crop is active.
- **`FarClipSettings`**: `public int? FarClipSettings`
  *Description:* Gets or sets far clip active status.
- **`FarClipOffset`**: `public double? FarClipOffset`
  *Description:* Gets or sets far clip offset value.
- **`PartsVisibility`**: `public EnumModel? PartsVisibility`
  *Description:* Gets or sets the parts visibility enum value.
- **`View`**: `public object? View`
  *Description:* Gets or sets the associated Revit View. Ignored in JSON.
- **`Element`**: `public override object? Element {`
  *Description:* Gets or sets the underlying Revit Element. Ignored in JSON.

---
### Class: `ViewPlanModel`
**File:** [src/SyntheticShared/Modules/RevitDOM/ViewPlanModel.cs](../src/SyntheticShared/Modules/RevitDOM/ViewPlanModel.cs)

#### Methods
##### `ViewPlanModel`
```csharp
public ViewPlanModel()
```
Initializes a new instance of the ViewPlanModel class.

#### Fields
- **`ViewModel`**: `public class ViewPlanModel : ViewModel {`
  *Description:* Represents a model representation of a Revit ViewPlan, inheriting from ViewModel. Tracks plan-specific properties: ViewRange, UnderlayId, and UnderlayOrientation.
- **`ViewRange`**: `public PlanViewRangeModel? ViewRange`
  *Description:* Gets or sets the view range (only applies to plan views).
- **`UnderlayId`**: `public ElementIdModel? UnderlayId`
  *Description:* Gets or sets the underlay view ID.
- **`UnderlayOrientation`**: `public int UnderlayOrientation`
  *Description:* Gets or sets the underlay orientation value.

---
### Class: `ViewPlanTranslator`
**File:** [src/SyntheticShared/Modules/RevitDOM/ViewPlanTranslator.cs](../src/SyntheticShared/Modules/RevitDOM/ViewPlanTranslator.cs)

#### Fields
- **`ViewPlanModel>`**: `internal class ViewPlanTranslator : ViewTranslator, IModelTranslator<ViewPlan, ViewPlanModel> {`
  *Description:* Translator governing the extraction and injection of Revit ViewPlan elements, inheriting from ViewTranslator. Manages plan-specific properties: ViewRange, UnderlayId, and UnderlayOrientation.

---
### Class: `ViewScheduleModel`
**File:** [src/SyntheticShared/Modules/RevitDOM/ViewScheduleModel.cs](../src/SyntheticShared/Modules/RevitDOM/ViewScheduleModel.cs)

#### Methods
##### `ViewScheduleModel`
```csharp
public ViewScheduleModel()
```
Initializes a new instance of the ViewScheduleModel class.

#### Fields
- **`ViewModel`**: `public class ViewScheduleModel : ViewModel {`
  *Description:* Represents a model representation of a Revit ViewSchedule, inheriting from ViewModel. Omit standard graphical properties during serialization when left null.

---
### Class: `ViewScheduleTranslator`
**File:** [src/SyntheticShared/Modules/RevitDOM/ViewScheduleTranslator.cs](../src/SyntheticShared/Modules/RevitDOM/ViewScheduleTranslator.cs)

#### Fields
- **`ViewScheduleModel>`**: `internal class ViewScheduleTranslator : ViewTranslator, IModelTranslator<ViewSchedule, ViewScheduleModel> {`
  *Description:* Translator governing the extraction and injection of Revit ViewSchedule elements, inheriting from ViewTranslator.

---
### Class: `ViewSheetModel`
**File:** [src/SyntheticShared/Modules/RevitDOM/ViewSheetModel.cs](../src/SyntheticShared/Modules/RevitDOM/ViewSheetModel.cs)

#### Methods
##### `ViewSheetModel`
```csharp
public ViewSheetModel()
```
Initializes a new instance of the ViewSheetModel class.

#### Fields
- **`ViewModel`**: `public class ViewSheetModel : ViewModel {`
  *Description:* Represents a model representation of a Revit ViewSheet, inheriting from ViewModel. Omit standard graphical properties during serialization when left null.

---
### Class: `ViewSheetTranslator`
**File:** [src/SyntheticShared/Modules/RevitDOM/ViewSheetTranslator.cs](../src/SyntheticShared/Modules/RevitDOM/ViewSheetTranslator.cs)

#### Fields
- **`ViewSheetModel>`**: `internal class ViewSheetTranslator : ViewTranslator, IModelTranslator<ViewSheet, ViewSheetModel> {`
  *Description:* Translator governing the extraction and injection of Revit ViewSheet elements, inheriting from ViewTranslator.

---
### Class: `ViewTemplateModel`
**File:** [src/SyntheticShared/Modules/RevitDOM/ViewTemplateModel.cs](../src/SyntheticShared/Modules/RevitDOM/ViewTemplateModel.cs)

#### Methods
##### `NonControlledParameterIds`
```csharp
public List<int> NonControlledParameterIds
```
Gets or sets the list of parameter IDs that are not controlled by this template.

##### `ViewTemplateModel`
```csharp
public ViewTemplateModel()
```
Initializes a new instance of the ViewTemplateModel class.

#### Fields
- **`ViewModel`**: `public class ViewTemplateModel : ViewModel {`
  *Description:* Represents a model representation of a Revit View Template, inheriting from ViewModel. Tracks which parameters are not controlled by this template.
- **`ViewTemplate`**: `public object? ViewTemplate {`
  *Description:* Gets or sets the associated Revit View Template object. Ignored in JSON.

---
### Class: `ViewTranslator`
**File:** [src/SyntheticShared/Modules/RevitDOM/ViewTranslator.cs](../src/SyntheticShared/Modules/RevitDOM/ViewTranslator.cs)

#### Fields
- **`ViewModel>`**: `internal class ViewTranslator : IModelTranslator<RevitView, ViewModel> {`
  *Description:* Translator governing the extraction and injection of Revit View elements, scoped to ViewPlan and ViewDrafting. Manages viewrange, underlays, graphical overrides, filters, crop boxes, and parameter controls.

---
### Class: `XYZModel`
**File:** [src/SyntheticShared/Modules/RevitDOM/XYZModel.cs](../src/SyntheticShared/Modules/RevitDOM/XYZModel.cs)

#### Fields
- **`ObjectModel`**: `public class XYZModel : ObjectModel {`
  *Description:* Pure state container for XYZ coordinate data, completely free of Revit API dependencies.

---
## Namespace: `Synthetic.Modules.SettingsDashboard.Commands`

### Class: `SettingsDashboardCommand`
**File:** [src/SyntheticShared/Modules/SettingsDashboard/Commands/SettingsDashboardCommand.cs](../src/SyntheticShared/Modules/SettingsDashboard/Commands/SettingsDashboardCommand.cs)

#### Methods
##### `Execute`
```csharp
public Result Execute( ExternalCommandData commandData, ref string message, ElementSet elements)
```
Executes the settings dashboard command.

#### Fields
- **`IExternalCommand`**: `public class SettingsDashboardCommand : IExternalCommand {`
  *Description:* Revit external command to display and edit the current project's configuration settings using the Unified Settings Dashboard.

---
## Namespace: `Synthetic.Modules.SettingsDashboard.Handlers`

### Class: `SyncExternalEventHandler`
**File:** [src/SyntheticShared/Modules/SettingsDashboard/Handlers/SyncExternalEventHandler.cs](../src/SyntheticShared/Modules/SettingsDashboard/Handlers/SyncExternalEventHandler.cs)

#### Methods
##### `QueueRequest`
```csharp
public void QueueRequest(SyncRequestType requestType, Document doc, string filePath)
```
Queues a sync request to be executed on the Revit API thread.

##### `Execute`
```csharp
public void Execute(UIApplication app)
```
Executes the queued sync request on the Revit API thread.

##### `GetName`
```csharp
public string GetName()
```
Gets the name of this external event handler.

#### Fields
- **`SyncRequestType`**: `public enum SyncRequestType {`
  *Description:* Request types supported by the SyncExternalEventHandler.
- **`IExternalEventHandler`**: `public class SyncExternalEventHandler : IExternalEventHandler {`
  *Description:* External event handler to marshal settings pull/push operations from modeless UI to the Revit API thread.

---
## Namespace: `Synthetic.Modules.SettingsDashboard.ViewModels`

### Class: `FileUtilitySettingsViewModel`
**File:** [src/SyntheticShared/Modules/SettingsDashboard/ViewModels/FileUtilitySettingsViewModel.cs](../src/SyntheticShared/Modules/SettingsDashboard/ViewModels/FileUtilitySettingsViewModel.cs)

#### Methods
##### `FileUtilitySettingsViewModel`
```csharp
public FileUtilitySettingsViewModel(Document doc, IntPtr mainWindowHandle)
```
Initializes a new instance of the `FileUtilitySettingsViewModel` class.

#### Fields
- **`ISettingModuleViewModel`**: `public class FileUtilitySettingsViewModel : ViewModelBase, ISettingModuleViewModel {`
  *Description:* ViewModel for managing File Utility and Network Path configuration settings in the Dashboard.

---
### Class: `MaterialLibraryViewModel`
**File:** [src/SyntheticShared/Modules/SettingsDashboard/ViewModels/MaterialLibraryViewModel.cs](../src/SyntheticShared/Modules/SettingsDashboard/ViewModels/MaterialLibraryViewModel.cs)

#### Methods
##### `MaterialLibraryViewModel`
```csharp
public MaterialLibraryViewModel(Document doc, IntPtr mainWindowHandle)
```
Initializes a new instance of the `MaterialLibraryViewModel` class.

#### Fields
- **`ISettingModuleViewModel`**: `public class MaterialLibraryViewModel : ViewModelBase, ISettingModuleViewModel {`
  *Description:* ViewModel for managing Material Library configuration settings.

---
### Class: `NetworkPathsWizardViewModel`
**File:** [src/SyntheticShared/Modules/SettingsDashboard/ViewModels/NetworkPathsWizardViewModel.cs](../src/SyntheticShared/Modules/SettingsDashboard/ViewModels/NetworkPathsWizardViewModel.cs)

#### Methods
##### `NetworkPathsWizardViewModel`
```csharp
public NetworkPathsWizardViewModel(FileUtilitySettings settings, IntPtr mainWindowHandle)
```
Initializes a new instance of the `NetworkPathsWizardViewModel` class.

#### Fields
- **`ViewModelBase`**: `public class NetworkPathsWizardViewModel : ViewModelBase {`
  *Description:* ViewModel for the Network Paths and File Utility Configuration Wizard.
- **`ArchiveDirectories`**: `public ObservableCollection<string> ArchiveDirectories`
  *Description:* Gets the collection of archive directories.
- **`AlternateMappings`**: `public ObservableCollection<PathMappingViewModel> AlternateMappings`
  *Description:* Gets the collection of alternate paths mapping views.
- **`SelectedArchiveDirectory`**: `public string? SelectedArchiveDirectory {`
  *Description:* Gets or sets the selected archive directory path.
- **`SelectedMapping`**: `public PathMappingViewModel? SelectedMapping {`
  *Description:* Gets or sets the selected path mapping item.
- **`IsValidating`**: `public bool IsValidating {`
  *Description:* Gets or sets a value indicating whether paths are currently being validated in the background.
- **`AddArchiveCommand`**: `public ICommand AddArchiveCommand`
  *Description:* Gets the command to add a new archive directory.
- **`RemoveArchiveCommand`**: `public ICommand RemoveArchiveCommand`
  *Description:* Gets the command to remove the selected archive directory.
- **`BrowseArchiveCommand`**: `public ICommand BrowseArchiveCommand`
  *Description:* Gets the command to browse and select an archive directory.
- **`AddMappingCommand`**: `public ICommand AddMappingCommand`
  *Description:* Gets the command to add a new alternate path mapping row.
- **`RemoveMappingCommand`**: `public ICommand RemoveMappingCommand`
  *Description:* Gets the command to remove the selected alternate path mapping row.
- **`BrowseMappingCommand`**: `public ICommand BrowseMappingCommand`
  *Description:* Gets the command to browse and select a local mapped path.
- **`OkCommand`**: `public ICommand OkCommand`
  *Description:* Gets the command to validate paths and close the wizard on success.
- **`CancelCommand`**: `public ICommand CancelCommand`
  *Description:* Gets the command to cancel editing and close the wizard.

---
### Class: `PathMappingViewModel`
**File:** [src/SyntheticShared/Modules/SettingsDashboard/ViewModels/PathMappingViewModel.cs](../src/SyntheticShared/Modules/SettingsDashboard/ViewModels/PathMappingViewModel.cs)

#### Methods
##### `PathMappingViewModel`
```csharp
public PathMappingViewModel()
```
Initializes a new instance of the `PathMappingViewModel` class.

##### `PathMappingViewModel`
```csharp
public PathMappingViewModel(string originalServerPath, string localMappedPath)
```
Initializes a new instance of the `PathMappingViewModel` class with initial paths.

#### Fields
- **`ViewModelBase`**: `public class PathMappingViewModel : ViewModelBase {`
  *Description:* ViewModel representing a single path mapping entry (Original -> LocalMapped).
- **`OriginalServerPath`**: `public string OriginalServerPath {`
  *Description:* Gets or sets the original server network path.
- **`LocalMappedPath`**: `public string LocalMappedPath {`
  *Description:* Gets or sets the local mapped network path.

---
### Class: `ProjectMaterialsViewModel`
**File:** [src/SyntheticShared/Modules/SettingsDashboard/ViewModels/ProjectMaterialsViewModel.cs](../src/SyntheticShared/Modules/SettingsDashboard/ViewModels/ProjectMaterialsViewModel.cs)

#### Methods
##### `ProjectMaterialsViewModel`
```csharp
public ProjectMaterialsViewModel(Document doc, IntPtr mainWindowHandle)
```
Initializes a new instance of the `ProjectMaterialsViewModel` class.

#### Fields
- **`ISettingModuleViewModel`**: `public class ProjectMaterialsViewModel : ViewModelBase, ISettingModuleViewModel {`
  *Description:* ViewModel for managing Project-specific Material paths.

---
### Class: `SettingsDashboardViewModel`
**File:** [src/SyntheticShared/Modules/SettingsDashboard/ViewModels/SettingsDashboardViewModel.cs](../src/SyntheticShared/Modules/SettingsDashboard/ViewModels/SettingsDashboardViewModel.cs)

#### Methods
##### `SettingsDashboardViewModel`
```csharp
public SettingsDashboardViewModel(Document doc, IntPtr mainWindowHandle)
```
Initializes a new instance of the `SettingsDashboardViewModel` class.

#### Fields
- **`ViewModelBase`**: `public class SettingsDashboardViewModel : ViewModelBase {`
  *Description:* Main ViewModel for the Unified Settings Dashboard.
- **`SettingModules`**: `public ObservableCollection<ISettingModuleViewModel> SettingModules`
  *Description:* Gets the list of settings modules.
- **`SelectedModule`**: `public ISettingModuleViewModel? SelectedModule {`
  *Description:* Gets or sets the selected settings module.
- **`SaveCommand`**: `public ICommand SaveCommand`
  *Description:* Gets the save command.
- **`CancelCommand`**: `public ICommand CancelCommand`
  *Description:* Gets the cancel command.

---
### Class: `StandardsSettingsViewModel`
**File:** [src/SyntheticShared/Modules/SettingsDashboard/ViewModels/StandardsSettingsViewModel.cs](../src/SyntheticShared/Modules/SettingsDashboard/ViewModels/StandardsSettingsViewModel.cs)

#### Methods
##### `StandardsSettingsViewModel`
```csharp
public StandardsSettingsViewModel(Document doc, IntPtr mainWindowHandle, IFileDialogService? fileDialogService = null)
```
Initializes a new instance of the `StandardsSettingsViewModel` class.

#### Fields
- **`ISettingModuleViewModel`**: `public class StandardsSettingsViewModel : ViewModelBase, ISettingModuleViewModel {`
  *Description:* ViewModel for managing Standards configuration settings in the Dashboard.
- **`StandardsFilePath`**: `public string StandardsFilePath {`
  *Description:* Gets or sets the path to the standards JSON file.
- **`BrowseCommand`**: `public ICommand BrowseCommand`
  *Description:* Gets the command that allows browsing for a standards JSON file.

---
### Class: `SyncSettingsViewModel`
**File:** [src/SyntheticShared/Modules/SettingsDashboard/ViewModels/SyncSettingsViewModel.cs](../src/SyntheticShared/Modules/SettingsDashboard/ViewModels/SyncSettingsViewModel.cs)

#### Methods
##### `SyncSettingsViewModel`
```csharp
public SyncSettingsViewModel(Document doc, IntPtr mainWindowHandle, IFileDialogService? fileDialogService = null, IUserPromptService? userPromptService = null)
```
Initializes a new instance of the `SyncSettingsViewModel` class.

#### Fields
- **`ISettingModuleViewModel`**: `public class SyncSettingsViewModel : ViewModelBase, ISettingModuleViewModel {`
  *Description:* ViewModel for managing Sync Configuration and Link settings.
- **`LinkedFilePath`**: `public string LinkedFilePath {`
  *Description:* Gets or sets the path to the linked settings configuration JSON file.
- **`LinkFileCommand`**: `public ICommand LinkFileCommand`
  *Description:* Gets the link file command.
- **`ExportCommand`**: `public ICommand ExportCommand`
  *Description:* Gets the export settings command.
- **`ImportCommand`**: `public ICommand ImportCommand`
  *Description:* Gets the import settings command.

---
### Class: `SyncWizardViewModel`
**File:** [src/SyntheticShared/Modules/SettingsDashboard/ViewModels/SyncWizardViewModel.cs](../src/SyntheticShared/Modules/SettingsDashboard/ViewModels/SyncWizardViewModel.cs)

#### Methods
##### `string.IsNullOrEmpty`
```csharp
public bool IsPathValid => string.IsNullOrEmpty(LinkedFilePath) || File.Exists(LinkedFilePath)
```
Gets a value indicating whether the selected linked path is valid.

##### `SyncWizardViewModel`
```csharp
public SyncWizardViewModel(SyncSettings settings, IFileDialogService? fileDialogService = null)
```
Initializes a new instance of the `SyncWizardViewModel` class.

#### Fields
- **`ViewModelBase`**: `public class SyncWizardViewModel : ViewModelBase {`
  *Description:* ViewModel for the Sync Settings Configuration Wizard.
- **`LinkedFilePath`**: `public string LinkedFilePath {`
  *Description:* Gets or sets the path to the linked settings configuration JSON file.
- **`BrowseCommand`**: `public ICommand BrowseCommand`
  *Description:* Gets the browse command.
- **`OkCommand`**: `public ICommand OkCommand`
  *Description:* Gets the OK command.
- **`CancelCommand`**: `public ICommand CancelCommand`
  *Description:* Gets the Cancel command.

---
### Class: `ViewAutoNumSettingsViewModel`
**File:** [src/SyntheticShared/Modules/SettingsDashboard/ViewModels/ViewAutoNumSettingsViewModel.cs](../src/SyntheticShared/Modules/SettingsDashboard/ViewModels/ViewAutoNumSettingsViewModel.cs)

#### Methods
##### `ViewAutoNumSettingsViewModel`
```csharp
public ViewAutoNumSettingsViewModel(Document doc, IntPtr mainWindowHandle)
```
Initializes a new instance of the `ViewAutoNumSettingsViewModel` class.

#### Fields
- **`ISettingModuleViewModel`**: `public class ViewAutoNumSettingsViewModel : ViewModelBase, ISettingModuleViewModel {`
  *Description:* ViewModel for managing View Auto-Numbering configuration settings.

---
### Class: `ViewAutoNumWizardViewModel`
**File:** [src/SyntheticShared/Modules/SettingsDashboard/ViewModels/ViewAutoNumWizardViewModel.cs](../src/SyntheticShared/Modules/SettingsDashboard/ViewModels/ViewAutoNumWizardViewModel.cs)

#### Methods
##### `AvailableFamilies`
```csharp
public ObservableCollection<string> AvailableFamilies
```
Gets the collection of available annotation families.

##### `AvailableTypes`
```csharp
public ObservableCollection<string> AvailableTypes
```
Gets the collection of available types for the selected family.

##### `ViewAutoNumWizardViewModel`
```csharp
public ViewAutoNumWizardViewModel(Document doc, ViewAutoNumSettings settings)
```
Initializes a new instance of the `ViewAutoNumWizardViewModel` class.

#### Fields
- **`ViewModelBase`**: `public class ViewAutoNumWizardViewModel : ViewModelBase {`
  *Description:* ViewModel for the View Autonumbering Configuration Wizard.
- **`SelectedFamily`**: `public string SelectedFamily {`
  *Description:* Gets or sets the selected annotation family name.
- **`SelectedType`**: `public string SelectedType {`
  *Description:* Gets or sets the selected family type name.
- **`XGridName`**: `public string XGridName {`
  *Description:* Gets or sets the X Grid parameter name.
- **`YGridName`**: `public string YGridName {`
  *Description:* Gets or sets the Y Grid parameter name.
- **`OkCommand`**: `public ICommand OkCommand`
  *Description:* Gets the OK command.
- **`CancelCommand`**: `public ICommand CancelCommand`
  *Description:* Gets the Cancel command.

---
### Class: `WorksetSettingsViewModel`
**File:** [src/SyntheticShared/Modules/SettingsDashboard/ViewModels/WorksetSettingsViewModel.cs](../src/SyntheticShared/Modules/SettingsDashboard/ViewModels/WorksetSettingsViewModel.cs)

#### Methods
##### `WorksetSettingsViewModel`
```csharp
public WorksetSettingsViewModel(Document doc, IntPtr mainWindowHandle)
```
Initializes a new instance of the `WorksetSettingsViewModel` class.

#### Fields
- **`ISettingModuleViewModel`**: `public class WorksetSettingsViewModel : ViewModelBase, ISettingModuleViewModel {`
  *Description:* ViewModel for managing Workset configuration settings.

---
### Class: `WorksetWizardViewModel`
**File:** [src/SyntheticShared/Modules/SettingsDashboard/ViewModels/WorksetWizardViewModel.cs](../src/SyntheticShared/Modules/SettingsDashboard/ViewModels/WorksetWizardViewModel.cs)

#### Methods
##### `AvailableGroups`
```csharp
public ObservableCollection<string> AvailableGroups
```
Gets the collection of available workset groups (worksheet names).

##### `WorksetWizardViewModel`
```csharp
public WorksetWizardViewModel(WorksetSettings settings, IFileDialogService? fileDialogService = null)
```
Initializes a new instance of the `WorksetWizardViewModel` class.

#### Fields
- **`ViewModelBase`**: `public class WorksetWizardViewModel : ViewModelBase {`
  *Description:* ViewModel for the Workset Configuration Wizard.
- **`WorksetFile`**: `public string? WorksetFile {`
  *Description:* Gets or sets the workset configuration Excel filename.
- **`WorksetPath`**: `public string? WorksetPath {`
  *Description:* Gets or sets the path to the workset Excel file directory.
- **`FullPath`**: `public string FullPath {`
  *Description:* Gets the full path to the workset configuration Excel file.
- **`SelectedGroup`**: `public string? SelectedGroup {`
  *Description:* Gets or sets the selected workset group.
- **`IsLoading`**: `public bool IsLoading {`
  *Description:* Gets or sets a value indicating whether background worksheet loading is in progress.
- **`BrowseCommand`**: `public ICommand BrowseCommand`
  *Description:* Gets the browse command.
- **`OkCommand`**: `public ICommand OkCommand`
  *Description:* Gets the OK command.
- **`CancelCommand`**: `public ICommand CancelCommand`
  *Description:* Gets the Cancel command.

---
## Namespace: `Synthetic.Modules.StandardsManagement.Commands`

### Class: `CmdEnforceStandards`
**File:** [src/SyntheticShared/Modules/StandardsManagement/Commands/CmdEnforceStandards.cs](../src/SyntheticShared/Modules/StandardsManagement/Commands/CmdEnforceStandards.cs)

#### Methods
##### `Execute`
```csharp
public Result Execute( ExternalCommandData commandData, ref string message, ElementSet elements)
```
Executes the element type import command.

#### Fields
- **`IExternalCommand`**: `public class CmdEnforceStandards : IExternalCommand {`
  *Description:* Revit external command to enforce project standards from a JSON configuration file. Opens the Enforce Standards dialog interface.

---
### Class: `CmdExportStandards`
**File:** [src/SyntheticShared/Modules/StandardsManagement/Commands/CmdExportStandards.cs](../src/SyntheticShared/Modules/StandardsManagement/Commands/CmdExportStandards.cs)

#### Methods
##### `Execute`
```csharp
public Result Execute( ExternalCommandData commandData, ref string message, ElementSet elements)
```
Executes the element type export command.

#### Fields
- **`IExternalCommand`**: `public class CmdExportStandards : IExternalCommand {`
  *Description:* Revit external command to serialize element types and styles to a JSON standards file.

---
### Class: `CmdProjectStandards`
**File:** [src/SyntheticShared/Modules/StandardsManagement/Commands/CmdProjectStandards.cs](../src/SyntheticShared/Modules/StandardsManagement/Commands/CmdProjectStandards.cs)

#### Methods
##### `Execute`
```csharp
public Result Execute( ExternalCommandData commandData, ref string message, ElementSet elements)
```
Executes the command.

#### Fields
- **`IExternalCommand`**: `public class CmdProjectStandards : IExternalCommand {`
  *Description:* Command to display the Modeless Project Standards Dashboard window.

---
### Class: `StandardsEditorShow`
**File:** [src/SyntheticShared/Modules/StandardsManagement/Commands/StandardsEditorShow.cs](../src/SyntheticShared/Modules/StandardsManagement/Commands/StandardsEditorShow.cs)

#### Methods
##### `Execute`
```csharp
public Result Execute( ExternalCommandData commandData, ref string message, ElementSet elements)
```
Executes the command.

#### Fields
- **`IExternalCommand`**: `public class StandardsEditorShow : IExternalCommand {`
  *Description:* Command to display the Modeless Synthetic Standards JSON Editor.

---
## Namespace: `Synthetic.Modules.StandardsManagement.Handlers`

### Class: `JsonEditorExternalEventHandler`
**File:** [src/SyntheticShared/Modules/StandardsManagement/Handlers/JsonEditorExternalEventHandler.cs](../src/SyntheticShared/Modules/StandardsManagement/Handlers/JsonEditorExternalEventHandler.cs)

#### Methods
##### `QueueRequest`
```csharp
public void QueueRequest(JsonEditorRequestType requestType, JsonEditorMainViewModel mainVM, List<ElementModel>? importPayload = null)
```
Queues a request to be executed on the Revit API thread.

##### `Execute`
```csharp
public void Execute(UIApplication app)
```
Executes the queued request on the Revit API thread.

##### `GetName`
```csharp
public string GetName()
```
Gets the name of this external event handler.

#### Fields
- **`JsonEditorRequestType`**: `public enum JsonEditorRequestType {`
  *Description:* Request types supported by the JsonEditorExternalEventHandler.
- **`IExternalEventHandler`**: `public class JsonEditorExternalEventHandler : IExternalEventHandler {`
  *Description:* External event handler to marshal calls from modeless JSON Editor UI to the Revit API thread.

---
## Namespace: `Synthetic.Modules.StandardsManagement.Models`

### Class: `ImportLogItem`
**File:** [src/SyntheticShared/Modules/StandardsManagement/Models/ImportLogItem.cs](../src/SyntheticShared/Modules/StandardsManagement/Models/ImportLogItem.cs)

#### Fields
- **`ImportLogItem`**: `public class ImportLogItem {`
  *Description:* Represents an entry in the import log, tracking actions taken on Revit elements.
- **`Action`**: `public string Action`
  *Description:* Gets or sets the action performed (e.g. Created, Updated, Renamed, Failed, Canceled).
- **`Class`**: `public string Class`
  *Description:* Gets or sets the class name of the Revit element.
- **`ElementName`**: `public string ElementName`
  *Description:* Gets or sets the name of the element.
- **`Message`**: `public string Message`
  *Description:* Gets or sets any details or error messages associated with the action.

---
## Namespace: `Synthetic.Modules.StandardsManagement.ViewModels`

### Class: `CategorySelectionViewModel`
**File:** [src/SyntheticShared/Modules/StandardsManagement/ViewModels/CategorySelectionViewModel.cs](../src/SyntheticShared/Modules/StandardsManagement/ViewModels/CategorySelectionViewModel.cs)

#### Methods
##### `CategoryFilters`
```csharp
public ObservableCollection<CategorySelectionItem> CategoryFilters
```
Gets the list of category filter options.

##### `CategorySelectionViewModel`
```csharp
public CategorySelectionViewModel(Document doc)
```
Initializes a new instance of the `CategorySelectionViewModel` class.

#### Fields
- **`ViewModelBase`**: `public class CategorySelectionViewModel : ViewModelBase {`
  *Description:* ViewModel that handles Revit category filtering.
- **`SelectedCategoryFilter`**: `public CategorySelectionItem? SelectedCategoryFilter {`
  *Description:* Gets or sets the selected category filter.
- **`ViewModelBase`**: `public class CategorySelectionItem : ViewModelBase {`
  *Description:* Represents a category that can be checked or unchecked for export.
- **`IsChecked`**: `public bool IsChecked {`
  *Description:* Gets or sets a value indicating whether the category is selected.
- **`Name`**: `public string Name`
  *Description:* Gets or sets the name of the category.
- **`Id`**: `public ElementId Id`
  *Description:* Gets or sets the ElementId of the category.

---
### Class: `ElementTypeWrapperVM`
**File:** [src/SyntheticShared/Modules/StandardsManagement/ViewModels/ElementTypeWrapperVM.cs](../src/SyntheticShared/Modules/StandardsManagement/ViewModels/ElementTypeWrapperVM.cs)

#### Methods
##### `ElementTypeWrapperVM`
```csharp
public ElementTypeWrapperVM(ElementModel model, ElementModel? baselineModel = null)
```
Initializes a new instance of the `ElementTypeWrapperVM` class.

##### `GetUpdatedModel`
```csharp
public ElementModel GetUpdatedModel()
```
Synchronizes and returns the updated ElementTypeModel POCO for future serialization.

##### `_errors.Any`
```csharp
public bool HasErrors => _errors.Any(kvp => kvp.Value != null && kvp.Value.Count > 0)
```
Gets whether there are any validation errors.

##### `GetErrors`
```csharp
public IEnumerable GetErrors(string? propertyName)
```
Gets the validation errors for a specific property.

##### `OnErrorsChanged`
```csharp
protected virtual void OnErrorsChanged(string propertyName)
```
Raises the ErrorsChanged event and notifies that HasErrors has changed.

##### `ValidateName`
```csharp
public void ValidateName()
```
Validates the Name property to check for duplicate names within the same category.

##### `AddError`
```csharp
private void AddError(string propertyName, string error)
```
Adds a validation error to the dictionary and triggers ErrorsChanged.

##### `HarvestPocoProperties`
```csharp
public List<ParameterWrapperVM> HarvestPocoProperties()
```
Reflection sweep to harvest all public properties on the underlying POCO, excluding Name, Class, Aliases, and [JsonIgnore] properties.

#### Fields
- **`INotifyDataErrorInfo`**: `public class ElementTypeWrapperVM : ViewModelBase, INotifyDataErrorInfo {`
  *Description:* ViewModel wrapper for the ElementTypeModel POCO. Provides change notification, data binding, and editable/read-only field policies.
- **`ErrorsChanged`**: `public event EventHandler<DataErrorsChangedEventArgs>? ErrorsChanged;`
  *Description:* Event raised when validation errors change for a property.
- **`Group`**: `public string Group`
  *Description:* Gets or sets the group name for categorization in the UI.
- **`IsSelected`**: `public bool IsSelected {`
  *Description:* Gets or sets whether this element is selected in the TreeView.
- **`IsDirty`**: `public bool IsDirty {`
  *Description:* Gets a value indicating whether this element or any of its parameters has unsaved changes.
- **`Parameters`**: `public ObservableCollection<ParameterWrapperVM> Parameters`
  *Description:* Gets the collection of parameter ViewModels.
- **`IsNameDuplicateCallback`**: `public Func<ElementTypeWrapperVM, string, bool>? IsNameDuplicateCallback`
  *Description:* Callback function to check if this name is a duplicate.
- **`IsNameEditable`**: `public bool IsNameEditable {`
  *Description:* Gets whether the element's name is editable. Root/built-in categories and subcategories cannot be renamed.
- **`Name`**: `public string Name {`
  *Description:* Gets or sets the name of the Element Type. Updates the underlying POCO and raises PropertyChanged.
- **`AliasesString`**: `public string AliasesString {`
  *Description:* Gets or sets the element aliases as a comma-separated string.
- **`Class`**: `public string Class`
  *Description:* Gets the class of the Element Type. This field is read-only.
- **`Category`**: `public string Category`
  *Description:* Gets the category of the Element Type. This field is read-only.
- **`CategoryGroupLevel1`**: `public string CategoryGroupLevel1 {`
  *Description:* Gets the Level 1 category/type grouping.
- **`CategoryGroupLevel2`**: `public string CategoryGroupLevel2 {`
  *Description:* Gets the Level 2 parent category/type grouping.

---
### Class: `EnforceStandardsViewModel`
**File:** [src/SyntheticShared/Modules/StandardsManagement/ViewModels/EnforceStandardsViewModel.cs](../src/SyntheticShared/Modules/StandardsManagement/ViewModels/EnforceStandardsViewModel.cs)

#### Methods
##### `EnforceStandardsViewModel`
```csharp
public EnforceStandardsViewModel(UIApplication uiapp) : this(uiapp, new WindowsFileDialogService())
```
Initializes a new instance of the `EnforceStandardsViewModel` class.

##### `EnforceStandardsViewModel`
```csharp
public EnforceStandardsViewModel(UIApplication uiapp, IFileDialogService dialogService) : this(ExtractDocument(uiapp), dialogService)
```
Initializes a new instance of the `EnforceStandardsViewModel` class with custom file dialog service.

##### `EnforceStandardsViewModel`
```csharp
public EnforceStandardsViewModel(UIApplication uiapp, IEnumerable<ElementModel> inMemoryStyles) : this(uiapp)
```
Initializes a new instance of the `EnforceStandardsViewModel` class with in-memory style data.

##### `FilterStyles`
```csharp
private List<ElementModel> FilterStyles(List<ElementModel> styles)
```
Filters incoming standard models based on checked category check boxes.

#### Fields
- **`ViewModelBase`**: `public class EnforceStandardsViewModel : ViewModelBase {`
  *Description:* ViewModel for enforcing standards from a JSON file or memory. Supports deep-scan and silent override paths.
- **`FilePath`**: `public string FilePath {`
  *Description:* Gets or sets the path of the styles JSON file to import.
- **`FileSelectionVisibility`**: `public Visibility FileSelectionVisibility`
  *Description:* Gets the visibility status of the file selection control.
- **`MergeAliases`**: `public bool MergeAliases {`
  *Description:* Gets or sets a value indicating whether to merge alias style names during import.
- **`IsTemplate`**: `public bool IsTemplate {`
  *Description:* Gets or sets a value indicating whether elements should be enforced as templates (omitting Element IDs).
- **`UpdateFamilies`**: `public bool UpdateFamilies {`
  *Description:* Gets or sets a value indicating whether loaded families should also be updated with the imported styles.
- **`ProcessNested`**: `public bool ProcessNested {`
  *Description:* Gets or sets a value indicating whether nested families should also be updated recursively.
- **`PurgeUnused`**: `public bool PurgeUnused {`
  *Description:* Gets or sets a value indicating whether unused elements should be purged from families.
- **`CategoryFilters`**: `public List<string> CategoryFilters`
  *Description:* Gets the list of category filter options.
- **`SelectedCategoryFilter`**: `public string SelectedCategoryFilter {`
  *Description:* Gets or sets the selected category filter option.
- **`StatusMessage`**: `public string StatusMessage {`
  *Description:* Gets or sets the current status message.
- **`ProgressValue`**: `public int ProgressValue {`
  *Description:* Gets or sets the current progress value.
- **`ProgressMax`**: `public int ProgressMax {`
  *Description:* Gets or sets the maximum progress value.
- **`ProgressVisibility`**: `public Visibility ProgressVisibility {`
  *Description:* Gets or sets the visibility of the progress indicator.
- **`IsIndeterminate`**: `public bool IsIndeterminate {`
  *Description:* Gets or sets a value indicating whether the progress indicator is indeterminate.
- **`IsProcessing`**: `public bool IsProcessing {`
  *Description:* Gets or sets a value indicating whether an import operation is running.
- **`ClassSelection`**: `public StandardsClassSelectionViewModel ClassSelection`
  *Description:* Gets the class selection ViewModel containing modular standard filter check boxes.
- **`BrowseCommand`**: `public ICommand BrowseCommand`
  *Description:* Gets the command to browse for the JSON file.
- **`EnforceCommand`**: `public ICommand EnforceCommand`
  *Description:* Gets the command to enforce standards silently (direct import).
- **`AnalyzeCommand`**: `public ICommand AnalyzeCommand`
  *Description:* Gets the command to perform deep scan analysis and review overrides.
- **`CancelCommand`**: `public ICommand CancelCommand`
  *Description:* Gets the command to cancel the operation.
- **`CloseAction`**: `public Action? CloseAction`
  *Description:* Gets or sets the action to close the window.

---
### Class: `ExportStylesViewModel`
**File:** [src/SyntheticShared/Modules/StandardsManagement/ViewModels/ExportStylesViewModel.cs](../src/SyntheticShared/Modules/StandardsManagement/ViewModels/ExportStylesViewModel.cs)

#### Methods
##### `CartItems`
```csharp
public ObservableCollection<ElementTypeWrapperVM> CartItems
```
Gets the collection of elements currently in the export cart.

##### `ExportStylesViewModel`
```csharp
public ExportStylesViewModel(UIApplication uiapp) : this(uiapp, false, new WindowsFileDialogService())
```
Initializes a new instance of the `ExportStylesViewModel` class with default in-memory mode set to false.

##### `ExportStylesViewModel`
```csharp
public ExportStylesViewModel(UIApplication uiapp, bool isInMemoryMode) : this(uiapp, isInMemoryMode, new WindowsFileDialogService())
```
Initializes a new instance of the `ExportStylesViewModel` class.

##### `ExportStylesViewModel`
```csharp
public ExportStylesViewModel(UIApplication uiapp, bool isInMemoryMode, IFileDialogService fileDialogService) : this(ExtractDocument(uiapp), fileDialogService)
```
Initializes a new instance of the `ExportStylesViewModel` class with custom dialog service.

##### `ExportStylesViewModel`
```csharp
internal ExportStylesViewModel(Document doc, IFileDialogService fileDialogService)
```
Internal constructor for headless unit testing.

##### `FilterStyles`
```csharp
internal List<ObjectModel> FilterStyles(IEnumerable<ObjectModel> styles)
```
Filters incoming standard models based on active checkbox configuration.

#### Fields
- **`ViewModelBase`**: `public class ExportStylesViewModel : ViewModelBase {`
  *Description:* ViewModel for exporting styles and configurations from Revit.
- **`HideViewRequested`**: `public event EventHandler? HideViewRequested;`
  *Description:* Event raised when the view is requested to hide.
- **`ShowViewRequested`**: `public event EventHandler? ShowViewRequested;`
  *Description:* Event raised when the view is requested to show.
- **`SelectedExportItems`**: `public ObservableCollection<ElementId> SelectedExportItems {`
  *Description:* Gets or sets the collection of ElementIds selected for export.
- **`GroupedCartView`**: `public System.ComponentModel.ICollectionView GroupedCartView`
  *Description:* Gets the grouped collection view of the cart items.
- **`ExportFilePath`**: `public string ExportFilePath {`
  *Description:* Gets or sets the destination file path for the exported styles.
- **`GeneratedStyles`**: `public IEnumerable<ObjectModel> GeneratedStyles`
  *Description:* Gets the exported models in memory when in in-memory mode.
- **`FileSelectionVisibility`**: `public Visibility FileSelectionVisibility`
  *Description:* Gets the visibility status of the file selection control.
- **`ExportButtonText`**: `public string ExportButtonText`
  *Description:* Gets the display text for the export/generate button.
- **`ScanFamilies`**: `public bool ScanFamilies {`
  *Description:* Gets or sets a value indicating whether families should be scanned for styles.
- **`IsTemplate`**: `public bool IsTemplate {`
  *Description:* Gets or sets a value indicating whether elements should be exported as templates (omitting Element IDs).
- **`ProcessNested`**: `public bool ProcessNested {`
  *Description:* Gets or sets a value indicating whether nested families/elements should be processed.
- **`CategorySelection`**: `public CategorySelectionViewModel CategorySelection`
  *Description:* Gets the Category Selection viewmodel.
- **`StatusMessage`**: `public string StatusMessage {`
  *Description:* Gets or sets the current status message shown during processing.
- **`ProgressValue`**: `public int ProgressValue {`
  *Description:* Gets or sets the current progress value.
- **`ProgressMax`**: `public int ProgressMax {`
  *Description:* Gets or sets the maximum progress value.
- **`ProgressVisibility`**: `public Visibility ProgressVisibility {`
  *Description:* Gets or sets the visibility of the progress indicator.
- **`IsIndeterminate`**: `public bool IsIndeterminate {`
  *Description:* Gets or sets a value indicating whether the progress indicator is indeterminate.
- **`IsProcessing`**: `public bool IsProcessing {`
  *Description:* Gets or sets a value indicating whether an operation is currently processing.
- **`ClassSelection`**: `public StandardsClassSelectionViewModel ClassSelection`
  *Description:* Gets the class selection ViewModel containing modular standard filter check boxes.
- **`BrowseCommand`**: `public ICommand BrowseCommand`
  *Description:* Gets the command to browse for file paths.
- **`ExportCommand`**: `public ICommand ExportCommand`
  *Description:* Gets the command to perform the export operation.
- **`CancelCommand`**: `public ICommand CancelCommand`
  *Description:* Gets the command to cancel the operation.
- **`AddAnnotationsCommand`**: `public ICommand AddAnnotationsCommand`
  *Description:* Gets the command to add annotations to the export cart.
- **`AddViewsCommand`**: `public ICommand AddViewsCommand`
  *Description:* Gets the command to add views to the export cart.
- **`AddModelSystemTypesCommand`**: `public ICommand AddModelSystemTypesCommand`
  *Description:* Gets the command to add model/system types to the export cart.
- **`AddFamilyTypesCommand`**: `public ICommand AddFamilyTypesCommand`
  *Description:* Gets the command to add family types to the export cart.
- **`AddMaterialsCommand`**: `public ICommand AddMaterialsCommand`
  *Description:* Gets the command to add materials to the export cart.
- **`AddCategoriesCommand`**: `public ICommand AddCategoriesCommand`
  *Description:* Gets the command to add categories to the export cart.
- **`AddElementsCommand`**: `public ICommand AddElementsCommand`
  *Description:* Gets the command to add elements to the export cart.
- **`AddParameterFiltersCommand`**: `public ICommand AddParameterFiltersCommand`
  *Description:* Gets the command to add parameter filters to the export cart.
- **`RemoveCartItemCommand`**: `public ICommand RemoveCartItemCommand`
  *Description:* Gets the command to remove an item from the cart.
- **`CloseAction`**: `public Action? CloseAction`
  *Description:* Gets or sets the close action for the window.

---
### Class: `FindReplaceViewModel`
**File:** [src/SyntheticShared/Modules/StandardsManagement/ViewModels/FindReplaceViewModel.cs](../src/SyntheticShared/Modules/StandardsManagement/ViewModels/FindReplaceViewModel.cs)

#### Methods
##### `FindReplaceViewModel`
```csharp
public FindReplaceViewModel(JsonEditorMainViewModel mainVM)
```
Initializes a new instance of the `FindReplaceViewModel` class.

##### `FindNext`
```csharp
public bool FindNext()
```
Searches forward in the dataset for FindText.

##### `FindPrevious`
```csharp
public bool FindPrevious()
```
Searches backward in the dataset for FindText.

##### `Replace`
```csharp
public void Replace()
```
Replaces the current match and finds the next match.

##### `ReplaceAll`
```csharp
public void ReplaceAll()
```
Replaces all occurrences in all editable fields in the dataset.

#### Fields
- **`SearchScope`**: `public enum SearchScope {`
  *Description:* Specifies the scope of a find and replace operation.
- **`ViewModelBase`**: `public class FindReplaceViewModel : ViewModelBase {`
  *Description:* ViewModel for the Find and Replace dialog. Handles searching and replacing text in Element Names and editable parameter values.
- **`Scope`**: `public SearchScope Scope {`
  *Description:* Gets or sets the search scope.
- **`IsElementNamesScope`**: `public bool IsElementNamesScope {`
  *Description:* Gets or sets whether to search Element Names.
- **`IsParameterValuesScope`**: `public bool IsParameterValuesScope {`
  *Description:* Gets or sets whether to search Parameter Values.
- **`FindText`**: `public string FindText {`
  *Description:* Gets or sets the text to search for.
- **`ReplaceText`**: `public string ReplaceText {`
  *Description:* Gets or sets the replacement text.
- **`FindNextCommand`**: `public ICommand FindNextCommand`
  *Description:* Gets the command to find the next match.
- **`FindPreviousCommand`**: `public ICommand FindPreviousCommand`
  *Description:* Gets the command to find the previous match.
- **`ReplaceCommand`**: `public ICommand ReplaceCommand`
  *Description:* Gets the command to replace the current match and find next.
- **`ReplaceAllCommand`**: `public ICommand ReplaceAllCommand`
  *Description:* Gets the command to replace all occurrences.
- **`CloseCommand`**: `public ICommand CloseCommand`
  *Description:* Gets the command to close the window.

---
### Class: `ImportSummaryViewModel`
**File:** [src/SyntheticShared/Modules/StandardsManagement/ViewModels/ImportSummaryViewModel.cs](../src/SyntheticShared/Modules/StandardsManagement/ViewModels/ImportSummaryViewModel.cs)

#### Methods
##### `LogItems.Count`
```csharp
public int CreatedCount => LogItems.Count(item => item.Action == "Created")
```
Gets the count of items that were created during import.

##### `LogItems.Count`
```csharp
public int UpdatedCount => LogItems.Count(item => item.Action == "Updated")
```
Gets the count of items that were updated during import.

##### `LogItems.Count`
```csharp
public int RenamedCount => LogItems.Count(item => item.Action == "Renamed")
```
Gets the count of items that were renamed during import.

##### `LogItems.Count`
```csharp
public int ErrorsCount => LogItems.Count(item => item.Action == "Failed")
```
Gets the count of failed import operations.

##### `ImportSummaryViewModel`
```csharp
public ImportSummaryViewModel(ObservableCollection<ImportLogItem> logItems, IFileDialogService? dialogService = null)
```
Initializes a new instance of the `ImportSummaryViewModel` class.

##### `GenerateMarkdown`
```csharp
public string GenerateMarkdown()
```
Generates a formatted markdown report from the summary log items.

#### Fields
- **`ViewModelBase`**: `public class ImportSummaryViewModel : ViewModelBase {`
  *Description:* ViewModel for the import summary view, summarizing the results of an import operation.
- **`LogItems`**: `public ObservableCollection<ImportLogItem> LogItems`
  *Description:* Gets the collection of import log items.
- **`ExportLogCommand`**: `public System.Windows.Input.ICommand ExportLogCommand`
  *Description:* Gets the command to export the execution log to a markdown file.

---
### Class: `JsonEditorMainViewModel`
**File:** [src/SyntheticShared/Modules/StandardsManagement/ViewModels/JsonEditorMainViewModel.cs](../src/SyntheticShared/Modules/StandardsManagement/ViewModels/JsonEditorMainViewModel.cs)

#### Methods
##### `SetExternalEvent`
```csharp
public void SetExternalEvent(ExternalEvent? externalEvent, JsonEditorExternalEventHandler? eventHandler)
```
Sets the Revit external event and handler for modeless API execution.

##### `JsonEditorMainViewModel`
```csharp
public JsonEditorMainViewModel(UIApplication? uiapp)
```
Initializes a new instance of the `JsonEditorMainViewModel` class with Revit context.

##### `JsonEditorMainViewModel`
```csharp
public JsonEditorMainViewModel() : this(null)
```
Initializes a new instance of the `JsonEditorMainViewModel` class.

##### `WrappedElements`
```csharp
public ObservableCollection<ElementTypeWrapperVM> WrappedElements
```
Gets the collection of wrapped ElementType ViewModels.

##### `SelectedElements`
```csharp
public ObservableCollection<ElementTypeWrapperVM> SelectedElements
```
Gets the collection of currently selected elements in the TreeView.

##### `GroupedElements`
```csharp
public ObservableCollection<ElementGroup> GroupedElements
```
Gets the grouped elements for TreeView binding.

##### `DisplayParameters`
```csharp
public ObservableCollection<ParameterWrapperVM> DisplayParameters
```
Gets the parameters representing the intersection of all selected elements.

##### `LoadJson`
```csharp
public void LoadJson(string filePath)
```
Loads the JSON from the specified file path, deserializes it, wraps the ElementTypeModels, and populates WrappedElements.

##### `SaveJson`
```csharp
public void SaveJson(string filePath)
```
Saves the wrapped and preserved elements back to the specified JSON file path.

##### `ApplyFilter`
```csharp
public void ApplyFilter()
```
Applies search filtering on the CollectionView.

##### `UpdateSelectedElements`
```csharp
public void UpdateSelectedElements()
```
Synchronizes the collection of SelectedElements and triggers parameter intersection recalculation.

##### `UpdateSelectedElementsFromListBox`
```csharp
public void UpdateSelectedElementsFromListBox()
```
Synchronizes the collection of SelectedElements from ListBox and triggers parameter intersection recalculation.

##### `CalculateParameterIntersection`
```csharp
private void CalculateParameterIntersection()
```
Calculates the parameter intersection of all selected elements and builds DisplayParameters.

##### `DisplayParam_PropertyChanged`
```csharp
private void DisplayParam_PropertyChanged(object? sender, System.ComponentModel.PropertyChangedEventArgs e)
```
Handles changes to intersecting display parameter values and pushes them to all selected elements.

##### `PushBulkValue`
```csharp
private void PushBulkValue(string paramName, string newValue)
```
Pushes a bulk edit to the matching parameter in all currently selected elements.

##### `IsNameDuplicate`
```csharp
public bool IsNameDuplicate(ElementTypeWrapperVM element, string name)
```
Checks if an element name is a duplicate within the same category.

##### `IsElementReferenced`
```csharp
public bool IsElementReferenced(ElementTypeWrapperVM element, out List<ElementTypeWrapperVM> referencingElements)
```
Checks if a given element is referenced by other elements in the JSON workspace.

##### `ReplaceReferences`
```csharp
public void ReplaceReferences(ElementTypeWrapperVM oldElement, string newName)
```
Replaces all references to an old element with references to a new element name.

##### `IngestGeneratedStyles`
```csharp
public void IngestGeneratedStyles(IEnumerable<ObjectModel> styles)
```
Safely ingests generated ElementModel and other ObjectModel elements into the editor.

##### `Elements`
```csharp
public ObservableCollection<ElementTypeWrapperVM> Elements
```
Gets the collection of elements belonging to this group.

##### `ElementGroup`
```csharp
public ElementGroup(string name)
```
Initializes a new instance of the `ElementGroup` class.

##### `Compare`
```csharp
public int Compare(object? x, object? y)
```
Compares two elements.

#### Fields
- **`ViewModelBase`**: `public class JsonEditorMainViewModel : ViewModelBase {`
  *Description:* Main orchestrator ViewModel for the JSON Editor. Handles loading, saving, and wrapping ElementTypeModel POCOs.
- **`Instance`**: `public static JsonEditorMainViewModel? Instance`
  *Description:* Gets or sets the singleton instance of the `JsonEditorMainViewModel`.
- **`GroupedElementsView`**: `public ICollectionView GroupedElementsView`
  *Description:* Gets the grouped CollectionView for ListBox binding.
- **`DuplicateCommand`**: `public ICommand DuplicateCommand`
  *Description:* Gets the command to duplicate selected items.
- **`DeleteCommand`**: `public ICommand DeleteCommand`
  *Description:* Gets the command to delete selected items.
- **`MergeElementsCommand`**: `public ICommand MergeElementsCommand`
  *Description:* Gets the command to merge selected elements.
- **`SelectAllCommand`**: `public ICommand SelectAllCommand`
  *Description:* Gets the command to select all visible items.
- **`SelectNoneCommand`**: `public ICommand SelectNoneCommand`
  *Description:* Gets the command to clear selection.
- **`ShowFindReplaceCommand`**: `public ICommand ShowFindReplaceCommand`
  *Description:* Gets the command to open the Find and Replace dialog.
- **`LoadCommand`**: `public ICommand LoadCommand`
  *Description:* Gets the command to load a JSON file.
- **`SaveCommand`**: `public ICommand SaveCommand`
  *Description:* Gets the command to save the current file.
- **`SaveAsCommand`**: `public ICommand SaveAsCommand`
  *Description:* Gets the command to save the current file as a new file.
- **`ImportSelectedCommand`**: `public ICommand ImportSelectedCommand`
  *Description:* Gets the command to import selected elements into Revit.
- **`GenerateCommand`**: `public ICommand GenerateCommand`
  *Description:* Gets the command to generate/extract styles from Revit.
- **`CurrentFilePath`**: `public string CurrentFilePath {`
  *Description:* Gets or sets the path of the active JSON file.
- **`SearchText`**: `public string SearchText {`
  *Description:* Gets or sets the search/filter text.
- **`SelectedElement`**: `public ElementTypeWrapperVM? SelectedElement {`
  *Description:* Gets or sets the currently selected element (for single select or primary element context).
- **`MainWindowHandle`**: `public IntPtr MainWindowHandle`
  *Description:* Gets the parent Revit main window handle.
- **`UIApplication`**: `public UIApplication? UIApplication`
  *Description:* Gets the UIApplication instance.
- **`SelectedName`**: `public string SelectedName {`
  *Description:* Gets the display name for the selected element(s) in the header.
- **`SelectedDisplayClass`**: `public string SelectedDisplayClass {`
  *Description:* Gets the display class for the selected element(s).
- **`SelectedDisplayCategory`**: `public string SelectedDisplayCategory {`
  *Description:* Gets the display category for the selected element(s).
- **`SelectedAliasesString`**: `public string SelectedAliasesString {`
  *Description:* Gets or sets the comma-separated aliases for the selected element(s).
- **`IsSingleElementSelected`**: `public bool IsSingleElementSelected`
  *Description:* Gets whether exactly one element is selected (enabling single-element edits like renaming).
- **`IsNameEditingAllowed`**: `public bool IsNameEditingAllowed`
  *Description:* Gets whether name editing is allowed on the selected element.
- **`IsEditorVisible`**: `public bool IsEditorVisible`
  *Description:* Gets whether the parameter editor should be visible.
- **`ViewModelBase`**: `public class ElementGroup : ViewModelBase {`
  *Description:* Represents a group of elements categorized by their JSON root key (e.g. "FillRegionTypes").
- **`Name`**: `public string Name {`
  *Description:* Gets or sets the group name.
- **`IsSelected`**: `public bool IsSelected {`
  *Description:* Gets or sets whether this group node is selected in the TreeView.
- **`System.Collections.IComparer`**: `public class ElementTypeWrapperComparer : System.Collections.IComparer {`
  *Description:* Custom comparer to sort ElementTypeWrapperVM items in the Left TreeView. Sorts by Group (using critical order), then Class (alphabetically), then Name (alphabetically).

---
### Class: `NestedDataEditorViewModel`
**File:** [src/SyntheticShared/Modules/StandardsManagement/ViewModels/NestedDataEditorViewModel.cs](../src/SyntheticShared/Modules/StandardsManagement/ViewModels/NestedDataEditorViewModel.cs)

#### Methods
##### `Layers`
```csharp
public ObservableCollection<CompoundLayerRowVM> Layers
```
Gets the collection of compound structure layer rows.

##### `Overrides`
```csharp
public ObservableCollection<VisibilityOverrideRowVM> Overrides
```
Gets the collection of visibility override rows.

##### `PropertyGridProperties`
```csharp
public ObservableCollection<ParameterWrapperVM> PropertyGridProperties
```
Gets the collection of properties shown in the property grid mode.

##### `NestedDataEditorViewModel`
```csharp
public NestedDataEditorViewModel(object nestedData, ObservableCollection<ElementTypeWrapperVM> wrappedElements)
```
Initializes a new instance of the `NestedDataEditorViewModel` class.

##### `CompoundLayerRowVM`
```csharp
public CompoundLayerRowVM(SerialCompoundStructureLayer layer, List<string> materialsList)
```
Initializes a new instance of the `CompoundLayerRowVM` class.

##### `GetUpdatedLayer`
```csharp
public SerialCompoundStructureLayer GetUpdatedLayer()
```
Gets the updated compound structure layer model with modified properties synced back.

##### `string.IsNullOrEmpty`
```csharp
public string GroupName => string.IsNullOrEmpty(ParentCategoryName) ? CategoryName : ParentCategoryName!
```
Gets the group name, which is either the parent category name or the category name if it has no parent.

##### `!string.IsNullOrEmpty`
```csharp
public bool IsSubcategory => !string.IsNullOrEmpty(ParentCategoryName)
```
Gets a value indicating whether this category is a subcategory.

##### `VisibilityOverrideRowVM`
```csharp
public VisibilityOverrideRowVM(CategoryGraphicOverridesModel overrideModel, List<string> patternsList, List<string> lineStylesList)
```
Initializes a new instance of the `VisibilityOverrideRowVM` class.

##### `IsModelModified`
```csharp
public bool IsModelModified()
```
Checks if the model has been modified.

##### `GetOverrideModel`
```csharp
public CategoryGraphicOverridesModel GetOverrideModel()
```
Gets the override model.

##### `SyncBackToOverride`
```csharp
public void SyncBackToOverride()
```
Synchronizes settings from the VM back to the override model.

#### Fields
- **`ViewModelBase`**: `public class NestedDataEditorViewModel : ViewModelBase {`
  *Description:* ViewModel for the nested data editor window, which handles editing compound layers, visibility overrides, or object properties.
- **`IsLayersMode`**: `public bool IsLayersMode`
  *Description:* Gets a value indicating whether the editor is in compound layers mode.
- **`IsOverridesMode`**: `public bool IsOverridesMode`
  *Description:* Gets a value indicating whether the editor is in visibility overrides mode.
- **`IsPropertyGridMode`**: `public bool IsPropertyGridMode`
  *Description:* Gets a value indicating whether the editor is in property grid mode.
- **`AvailableMaterials`**: `public List<string> AvailableMaterials`
  *Description:* Gets the list of available materials.
- **`AvailablePatterns`**: `public List<string> AvailablePatterns`
  *Description:* Gets the list of available patterns.
- **`AvailableLineStyles`**: `public List<string> AvailableLineStyles`
  *Description:* Gets the list of available line styles.
- **`SaveCommand`**: `public ICommand SaveCommand`
  *Description:* Gets the Save command.
- **`CancelCommand`**: `public ICommand CancelCommand`
  *Description:* Gets the Cancel command.
- **`CloseAction`**: `public Action? CloseAction`
  *Description:* Gets or sets the close action for the window.
- **`DialogResult`**: `public bool DialogResult { get; private set; }`
  *Description:* Gets a value indicating the dialog result status when the window is closed.
- **`ViewModelBase`**: `public class CompoundLayerRowVM : ViewModelBase {`
  *Description:* Wrapper for compound structure layers.
- **`Function`**: `public string Function`
  *Description:* Gets or sets the function of the layer.
- **`Width`**: `public double Width`
  *Description:* Gets or sets the width of the layer.
- **`LayerCapFlag`**: `public bool LayerCapFlag`
  *Description:* Gets or sets a value indicating whether the layer cap flag is set.
- **`StructuralMaterial`**: `public bool StructuralMaterial`
  *Description:* Gets or sets a value indicating whether the structural material flag is set.
- **`Priority`**: `public int Priority {`
  *Description:* Gets or sets the priority of the layer.
- **`SelectedMaterial`**: `public string SelectedMaterial {`
  *Description:* Gets or sets the name of the selected material for this layer.
- **`MaterialsList`**: `public List<string> MaterialsList`
  *Description:* Gets the list of available materials.
- **`ViewModelBase`**: `public class VisibilityOverrideRowVM : ViewModelBase {`
  *Description:* Wrapper for category graphic overrides.
- **`CategoryName`**: `public string CategoryName`
  *Description:* Gets the name of the category.
- **`ParentCategoryName`**: `public string? ParentCategoryName`
  *Description:* Gets the name of the parent category.
- **`IsVisible`**: `public bool IsVisible {`
  *Description:* Gets or sets a value indicating whether the category is visible.
- **`Halftone`**: `public bool Halftone {`
  *Description:* Gets or sets a value indicating whether the category is drawn in halftone.
- **`Transparency`**: `public int Transparency {`
  *Description:* Gets or sets the transparency of the category.
- **`ProjectionLineWeight`**: `public int ProjectionLineWeight {`
  *Description:* Gets or sets the projection line weight.
- **`ProjectionLineColor`**: `public string ProjectionLineColor {`
  *Description:* Gets or sets the projection line color.
- **`SelectedProjectionLinePattern`**: `public string SelectedProjectionLinePattern {`
  *Description:* Gets or sets the selected projection line pattern.
- **`CutLineWeight`**: `public int CutLineWeight {`
  *Description:* Gets or sets the cut line weight.
- **`CutLineColor`**: `public string CutLineColor {`
  *Description:* Gets or sets the cut line color.
- **`SelectedCutLinePattern`**: `public string SelectedCutLinePattern {`
  *Description:* Gets or sets the selected cut line pattern.
- **`DetailLevel`**: `public string DetailLevel {`
  *Description:* Gets or sets the detail level.
- **`PatternsList`**: `public List<string> PatternsList`
  *Description:* Gets the list of available patterns.
- **`LineStylesList`**: `public List<string> LineStylesList`
  *Description:* Gets the list of available line styles.
- **`DetailLevels`**: `public List<string> DetailLevels`
  *Description:* Gets the list of detail levels.

---
### Class: `ParameterWrapperVM`
**File:** [src/SyntheticShared/Modules/StandardsManagement/ViewModels/ParameterWrapperVM.cs](../src/SyntheticShared/Modules/StandardsManagement/ViewModels/ParameterWrapperVM.cs)

#### Methods
##### `ParameterWrapperVM`
```csharp
public ParameterWrapperVM(ParameterModel model, ElementModel? parentModel = null, ParameterModel? baselineParamModel = null, IUserPromptService? userPromptService = null)
```
Initializes a new instance of the `ParameterWrapperVM` class.

##### `ParameterWrapperVM`
```csharp
public ParameterWrapperVM(string propertyName, object nestedDataTarget, IUserPromptService? userPromptService = null)
```
Initializes a new instance of the `ParameterWrapperVM` class for complex nested data.

##### `GetModel`
```csharp
public ParameterModel GetModel()
```
Gets the underlying ParameterModel POCO reference.

##### `_errors.Any`
```csharp
public bool HasErrors => _errors.Any(kvp => kvp.Value != null && kvp.Value.Count > 0)
```
Gets whether there are any validation errors.

##### `GetErrors`
```csharp
public IEnumerable GetErrors(string? propertyName)
```
Gets the validation errors for a specific property.

##### `OnErrorsChanged`
```csharp
protected virtual void OnErrorsChanged(string propertyName)
```
Raises the ErrorsChanged event and notifies that HasErrors has changed.

##### `ValidateValue`
```csharp
private void ValidateValue()
```
Validates the Value property based on StorageType.

##### `AddError`
```csharp
private void AddError(string propertyName, string error)
```
Adds a validation error to the dictionary and triggers ErrorsChanged.

#### Fields
- **`INotifyDataErrorInfo`**: `public class ParameterWrapperVM : ViewModelBase, INotifyDataErrorInfo {`
  *Description:* ViewModel wrapper for the ParameterModel POCO. Supports two-way data binding, field-locking, &lt;Varies&gt; state, and validation.
- **`ErrorsChanged`**: `public event EventHandler<DataErrorsChangedEventArgs>? ErrorsChanged;`
  *Description:* Event raised when validation errors change for a property.
- **`Name`**: `public string Name`
  *Description:* Gets the parameter Name. (Read-Only)
- **`StorageType`**: `public string StorageType`
  *Description:* Gets the parameter StorageType (e.g. Integer, Double, ElementId, String). (Read-Only)
- **`Id`**: `public long Id`
  *Description:* Gets the parameter Id. (Read-Only)
- **`GUID`**: `public string? GUID`
  *Description:* Gets the parameter GUID if it is a shared parameter. (Read-Only)
- **`IsShared`**: `public bool IsShared`
  *Description:* Gets whether the parameter is a shared parameter. (Read-Only)
- **`IsDirty`**: `public bool IsDirty {`
  *Description:* Gets or sets a value indicating whether this parameter has unsaved changes.
- **`IsReadOnly`**: `public bool IsReadOnly {`
  *Description:* Gets whether the parameter is read-only. (Read-Only)
- **`ValueElemId`**: `public ElementIdModel? ValueElemId`
  *Description:* Gets the full ElementIdModel for reference element parameters. (Read-Only)
- **`ValueElemIdClass`**: `public string? ValueElemIdClass`
  *Description:* Gets the Class of the reference element parameter. (Read-Only)
- **`ValueElemIdCategory`**: `public string? ValueElemIdCategory`
  *Description:* Gets the Category of the reference element parameter. (Read-Only)
- **`ValueElemIdName`**: `public string? ValueElemIdName`
  *Description:* Gets the Name of the reference element parameter. (Read-Only)
- **`Value`**: `public string Value {`
  *Description:* Gets or sets the value of the parameter. If `IsMixedValue` is true, this returns "&lt;Varies&gt;". If set to a different value, `IsMixedValue` is automatically set to false.
- **`IsMixedValue`**: `public bool IsMixedValue {`
  *Description:* Gets or sets whether this parameter represents multiple varying values (mixed value state). If true, the `Value` property returns "&lt;Varies&gt;".
- **`AvailableReferences`**: `public IEnumerable<string> AvailableReferences {`
  *Description:* Gets the available references matching the parameter spec class/category.
- **`EditNestedDataCommand`**: `public ICommand EditNestedDataCommand {`
  *Description:* Command to edit complex nested data structures.

---
### Class: `ProjectStandardsDashboardViewModel`
**File:** [src/SyntheticShared/Modules/StandardsManagement/ViewModels/ProjectStandardsDashboardViewModel.cs](../src/SyntheticShared/Modules/StandardsManagement/ViewModels/ProjectStandardsDashboardViewModel.cs)

#### Methods
##### `ActionQueue.Any`
```csharp
public bool IsSavePathActive => ActionQueue.Any(item => item.WillSave)
```
Gets whether the save path panel should be active/visible in the UI.

##### `Enum.GetValues`
```csharp
public IEnumerable<SearchScope> AvailableSearchScopes => Enum.GetValues(typeof(SearchScope)).Cast<SearchScope>()
```
Gets the available search scopes for binding in the UI.

##### `SelectedQueueItems`
```csharp
public ObservableCollection<QueueItemModel> SelectedQueueItems
```
Gets the collection of staged elements currently selected for editing/diffing.

##### `ActiveDiffClusters`
```csharp
public ObservableCollection<DuplicateClusterModel> ActiveDiffClusters
```
Gets the collection of duplicate/diff clusters populated by the comparison engine.

##### `LastExecutionResults`
```csharp
public List<SerializationResultModel> LastExecutionResults
```
Gets the combined list of results from the last Run Queue execution.

##### `ProjectStandardsDashboardViewModel`
```csharp
public ProjectStandardsDashboardViewModel( UIApplication uiapp, IFileDialogService dialogService, IStandardsExportService? exportService = null, StandardsSettings? settings = null, IUserPromptService? userPromptService = null, IFindReplaceService? findReplaceService = null)
```
Initializes a new instance of the `ProjectStandardsDashboardViewModel` class for live Revit environment.

##### `ProjectStandardsDashboardViewModel`
```csharp
public ProjectStandardsDashboardViewModel( UIApplication uiapp, IFileDialogService dialogService, IStandardsExportService? exportService) : this(uiapp, dialogService, exportService, null)
```
Initializes a new instance of the `ProjectStandardsDashboardViewModel` class with 3 parameters for reflection compatibility.

##### `ProjectStandardsDashboardViewModel`
```csharp
public ProjectStandardsDashboardViewModel( Document doc, IFileDialogService dialogService, IStandardsExportService? exportService = null, StandardsSettings? settings = null, IUserPromptService? userPromptService = null, IFindReplaceService? findReplaceService = null)
```
Initializes a new instance of the `ProjectStandardsDashboardViewModel` class for headless testing.

##### `ProjectStandardsDashboardViewModel`
```csharp
public ProjectStandardsDashboardViewModel( Document doc, IFileDialogService dialogService, IStandardsExportService? exportService) : this(doc, dialogService, exportService, null)
```
Initializes a new instance of the `ProjectStandardsDashboardViewModel` class with 3 parameters for reflection compatibility.

##### `ProjectStandardsDashboardViewModel`
```csharp
public ProjectStandardsDashboardViewModel( UIApplication uiapp, IFileDialogService dialogService, IGuardrailPromptService? guardrailService, StandardsSettings? settings = null) : this(uiapp, dialogService, new StandardsExportService(guardrailService ?? new WindowsGuardrailPromptService(), dialogService), settings)
```
Compatibility constructor that adapts a legacy `IGuardrailPromptService` to the new export service.

##### `ProjectStandardsDashboardViewModel`
```csharp
public ProjectStandardsDashboardViewModel( Document doc, IFileDialogService dialogService, IGuardrailPromptService? guardrailService, StandardsSettings? settings = null) : this(doc, dialogService, new StandardsExportService(guardrailService ?? new WindowsGuardrailPromptService(), dialogService), settings)
```
Compatibility constructor that adapts a legacy `IGuardrailPromptService` to the new export service.

##### `SetExternalEvent`
```csharp
public void SetExternalEvent(ExternalEvent externalEvent, ProjectStandardsExternalEventHandler eventHandler)
```
Registers the external event and handler to run database transactions on the Revit API thread.

##### `LoadFileSource`
```csharp
public void LoadFileSource(string path, string displayName)
```
Loads a JSON standard file and appends it as a new source tab.

##### `MergeStandardsLists`
```csharp
public static List<ElementModel> MergeStandardsLists(List<ElementModel> existingElements, List<ElementModel> newElements, bool overwriteDuplicates)
```
Merges a list of existing standard elements with new standard elements, identifying duplicates strictly by Class + Name.

##### `SetViewModel`
```csharp
public void SetViewModel(ProjectStandardsDashboardViewModel viewModel)
```
Sets the view model instance.

##### `Execute`
```csharp
public void Execute(UIApplication app)
```
Executes the queued run-queue logic.

##### `GetName`
```csharp
public string GetName()
```
Gets the handler name.

#### Fields
- **`WorkspaceMode`**: `public enum WorkspaceMode {`
  *Description:* Specifies the active panel mode in the right column sub-workspace.
- **`SearchScope`**: `public enum SearchScope {`
  *Description:* Specifies the scope of a find and replace operation.
- **`ViewModelBase`**: `public class ProjectStandardsDashboardViewModel : ViewModelBase {`
  *Description:* ViewModel that manages the Project Standards Dashboard modeless window. Orchestrates multiple source tabs, hierarchical trees, search filtering, and staging.
- **`ActionQueue`**: `public ObservableCollection<QueueItemModel> ActionQueue`
  *Description:* Gets the staging action queue collection.
- **`ActionQueueView`**: `public ICollectionView ActionQueueView`
  *Description:* Gets the grouped collection view of the action queue.
- **`SelectedElement`**: `public ElementTypeWrapperVM? SelectedElement {`
  *Description:* Gets the single selected element wrapper when exactly one element is selected.
- **`IsSingleElementSelected`**: `public bool IsSingleElementSelected`
  *Description:* Gets whether exactly one element is currently selected.
- **`SelectedNameOrCount`**: `public string SelectedNameOrCount {`
  *Description:* Gets or sets the name of the selected element, or a count description if multiple elements are selected.
- **`SelectedDisplayClass`**: `public string SelectedDisplayClass {`
  *Description:* Gets a comma-separated concatenated list of stripped classes for the selected elements.
- **`SelectedAliasesString`**: `public string SelectedAliasesString {`
  *Description:* Gets or sets the aliases string of the selected element, or &lt;Varies&gt; if multiple elements are selected.
- **`ActiveWorkspace`**: `public WorkspaceMode ActiveWorkspace {`
  *Description:* Gets or sets the active right pane sub-workspace mode.
- **`SaveFilePath`**: `public string? SaveFilePath {`
  *Description:* Gets or sets the target file path for save actions.
- **`FindText`**: `public string FindText {`
  *Description:* Gets or sets the search string for batch find-and-replace edits.
- **`ReplaceText`**: `public string ReplaceText {`
  *Description:* Gets or sets the replacement string for batch find-and-replace edits.
- **`FindReplaceScope`**: `public SearchScope FindReplaceScope {`
  *Description:* Gets or sets the search scope for batch find-and-replace edits.
- **`SelectedItemName`**: `public string SelectedItemName {`
  *Description:* Gets or sets the name of the selected item in the queue.
- **`SelectedItemErrorMessage`**: `public string? SelectedItemErrorMessage {`
  *Description:* Gets the error message of the currently selected queue item if it has an error.
- **`AvailableSources`**: `public ObservableCollection<ProjectStandardsSourceViewModel> AvailableSources {`
  *Description:* Gets or sets the collection of loaded standard sources (tabs).
- **`SelectedSource`**: `public ProjectStandardsSourceViewModel? SelectedSource {`
  *Description:* Gets or sets the currently active source tab.
- **`UpdateFamilies`**: `public bool UpdateFamilies {`
  *Description:* Gets or sets whether to update loaded families during queue execution.
- **`ProcessNestedRecursive`**: `public bool ProcessNestedRecursive {`
  *Description:* Gets or sets whether to process nested families recursively.
- **`PurgeUnusedStyleTypes`**: `public bool PurgeUnusedStyleTypes {`
  *Description:* Gets or sets whether to purge unused style types in family documents.
- **`CategoryFilter`**: `public string CategoryFilter {`
  *Description:* Gets or sets the category filter for family updates.
- **`AvailableCategoryFilters`**: `public List<string> AvailableCategoryFilters`
  *Description:* Gets the list of available category filters.
- **`IExternalEventHandler`**: `public class ProjectStandardsExternalEventHandler : IExternalEventHandler {`
  *Description:* Event handler to execute Revit database writes on the Revit API thread from the modeless window.

---
### Class: `ProjectStandardsSourceViewModel`
**File:** [src/SyntheticShared/Modules/StandardsManagement/ViewModels/ProjectStandardsSourceViewModel.cs](../src/SyntheticShared/Modules/StandardsManagement/ViewModels/ProjectStandardsSourceViewModel.cs)

#### Methods
##### `SourceHierarchy`
```csharp
public ObservableCollection<StandardGroupModel> SourceHierarchy
```
Gets the hierarchical tree view dataset for this source.

##### `ProjectStandardsSourceViewModel`
```csharp
public ProjectStandardsSourceViewModel()
```
Initializes a new instance of the `ProjectStandardsSourceViewModel` class.

#### Fields
- **`ViewModelBase`**: `public class ProjectStandardsSourceViewModel : ViewModelBase {`
  *Description:* Represents a tabbed source standard file or Revit model context loaded inside the Project Standards workspace.
- **`DisplayName`**: `public string DisplayName {`
  *Description:* Gets or sets the display name shown on the source tab.
- **`SourcePath`**: `public string SourcePath {`
  *Description:* Gets or sets the absolute path or identifier of the source data.
- **`IsRevitSource`**: `public bool IsRevitSource {`
  *Description:* Gets or sets a value indicating whether this source is an active Revit model.
- **`SelectAllCommand`**: `public System.Windows.Input.ICommand SelectAllCommand`
  *Description:* Gets the command to select all elements in the source hierarchy.
- **`SelectNoneCommand`**: `public System.Windows.Input.ICommand SelectNoneCommand`
  *Description:* Gets the command to deselect all elements in the source hierarchy.

---
### Class: `QueueItemModel`
**File:** [src/SyntheticShared/Modules/StandardsManagement/ViewModels/QueueItemModel.cs](../src/SyntheticShared/Modules/StandardsManagement/ViewModels/QueueItemModel.cs)

#### Methods
##### `!string.IsNullOrEmpty`
```csharp
public bool HasError => !string.IsNullOrEmpty(ErrorMessage)
```
Gets a value indicating whether this item has an execution error.

##### `!string.IsNullOrEmpty`
```csharp
public bool IsDependency => !string.IsNullOrEmpty(DependencyOrigin)
```
Gets a value indicating whether this item was harvested as a dependency.

##### `QueueItemModel`
```csharp
public QueueItemModel(ObjectModel model, bool willEnforce, bool willSave)
```
Initializes a new instance of the `QueueItemModel` class with explicit execution flags.

##### `GetWrapper`
```csharp
public ElementTypeWrapperVM GetWrapper()
```
Generates a fresh ElementTypeWrapperVM on-the-fly.

##### `Revert`
```csharp
public void Revert()
```
Reverts the mutable TargetModel to match the immutable BaselineModel.

##### `RaisePropertyChanged`
```csharp
public void RaisePropertyChanged(string propertyName)
```
Raises a property changed notification for a given property.

#### Fields
- **`ViewModelBase`**: `public class QueueItemModel : ViewModelBase {`
  *Description:* Wrapper ViewModel for deep-copied elements staged in the Action Queue.
- **`BaselineModel`**: `public ObjectModel BaselineModel`
  *Description:* Gets the immutable historical snapshot at the moment of queuing.
- **`TargetModel`**: `public ObjectModel TargetModel { get; private set; }`
  *Description:* Gets the active, mutable state of the staged element.
- **`Model`**: `public ObjectModel Model`
  *Description:* Gets the underlying cloned standard object (redirects to TargetModel).
- **`Name`**: `public string Name {`
  *Description:* Gets the display name of the staged element.
- **`ClassName`**: `public string ClassName {`
  *Description:* Gets the class name / category of the element used for UI grouping.
- **`Category`**: `public string Category {`
  *Description:* Gets the category of the element.
- **`WillEnforce`**: `public bool WillEnforce {`
  *Description:* Gets or sets whether this item will be enforced in the Revit database.
- **`WillSave`**: `public bool WillSave {`
  *Description:* Gets or sets whether this item will be saved to a JSON standards file.
- **`IsEdited`**: `public bool IsEdited {`
  *Description:* Gets or sets whether this item has been edited.
- **`IsDiffed`**: `public bool IsDiffed {`
  *Description:* Gets or sets whether this item has been diffed.
- **`ErrorMessage`**: `public string? ErrorMessage {`
  *Description:* Gets or sets the execution error message if this item failed database or file operations.
- **`DependencyOrigin`**: `public string? DependencyOrigin {`
  *Description:* Gets or sets the name of the parent element that triggered the harvesting of this dependency.

---
### Class: `RevitDocumentItem`
**File:** [src/SyntheticShared/Modules/StandardsManagement/ViewModels/SelectRevitDocumentViewModel.cs](../src/SyntheticShared/Modules/StandardsManagement/ViewModels/SelectRevitDocumentViewModel.cs)

#### Methods
##### `.SelectMany`
```csharp
public List<string> SelectedFamilyGroupings => FilterHierarchy .SelectMany(g => g.Children.OfType<StandardClassModel>()) .Where(c => c.IsChecked == true) .Select(c => c.Name) .ToList()
```
Gets the list of names of all checked classes in the filter hierarchy.

#### Fields
- **`FilterHierarchy`**: `public ObservableCollection<StandardGroupModel> FilterHierarchy`
  *Description:* Gets the hierarchical checkable standards filter tree.
- **`ScanFamilies`**: `public bool ScanFamilies {`
  *Description:* Gets or sets a value indicating whether families should be scanned.
- **`IncludeNestedFamilies`**: `public bool IncludeNestedFamilies {`
  *Description:* Gets or sets a value indicating whether nested families should be included in family scanning.

---
### Class: `StandardClassModel`
**File:** [src/SyntheticShared/Modules/StandardsManagement/ViewModels/StandardClassModel.cs](../src/SyntheticShared/Modules/StandardsManagement/ViewModels/StandardClassModel.cs)

#### Fields
- **`SourceTreeItemViewModel`**: `public class StandardClassModel : SourceTreeItemViewModel {`
  *Description:* TreeView node representing a standard class (e.g. Text Note Types, Wall Types).

---
### Class: `StandardElementModel`
**File:** [src/SyntheticShared/Modules/StandardsManagement/ViewModels/StandardElementModel.cs](../src/SyntheticShared/Modules/StandardsManagement/ViewModels/StandardElementModel.cs)

#### Methods
##### `StandardElementModel`
```csharp
public StandardElementModel(ElementModel element)
```
Initializes a new instance of the `StandardElementModel` class.

#### Fields
- **`SourceTreeItemViewModel`**: `public class StandardElementModel : SourceTreeItemViewModel {`
  *Description:* TreeView leaf node wrapping a specific ElementModel POCO representation.
- **`Element`**: `public ElementModel Element`
  *Description:* Gets the underlying ElementModel.

---
### Class: `StandardGroupModel`
**File:** [src/SyntheticShared/Modules/StandardsManagement/ViewModels/StandardGroupModel.cs](../src/SyntheticShared/Modules/StandardsManagement/ViewModels/StandardGroupModel.cs)

#### Fields
- **`SourceTreeItemViewModel`**: `public class StandardGroupModel : SourceTreeItemViewModel {`
  *Description:* TreeView node representing a standard group (e.g. Annotations, Materials & Assets, System Types).

---
### Class: `StandardsClassSelectionViewModel`
**File:** [src/SyntheticShared/Modules/StandardsManagement/ViewModels/StandardsClassSelectionViewModel.cs](../src/SyntheticShared/Modules/StandardsManagement/ViewModels/StandardsClassSelectionViewModel.cs)

#### Methods
##### `ExportTextNoteTypes`
```csharp
public bool ExportTextNoteTypes
```
Gets or sets a value indicating whether to export TextNoteTypes.

##### `ExportLabelTypes`
```csharp
public bool ExportLabelTypes
```
Gets or sets a value indicating whether to export LabelTypes.

##### `ExportDimensionTypes`
```csharp
public bool ExportDimensionTypes
```
Gets or sets a value indicating whether to export DimensionTypes.

##### `ExportFilledRegionTypes`
```csharp
public bool ExportFilledRegionTypes
```
Gets or sets a value indicating whether to export FilledRegionTypes.

##### `ExportMaterials`
```csharp
public bool ExportMaterials
```
Gets or sets a value indicating whether to export Materials.

##### `ExportWallTypes`
```csharp
public bool ExportWallTypes
```
Gets or sets a value indicating whether to export WallTypes.

##### `ExportToposolidTypes`
```csharp
public bool ExportToposolidTypes
```
Gets or sets a value indicating whether to export ToposolidTypes.

##### `ExportFloorTypes`
```csharp
public bool ExportFloorTypes
```
Gets or sets a value indicating whether to export FloorTypes.

##### `ExportRoofTypes`
```csharp
public bool ExportRoofTypes
```
Gets or sets a value indicating whether to export RoofTypes.

##### `ExportCeilingTypes`
```csharp
public bool ExportCeilingTypes
```
Gets or sets a value indicating whether to export CeilingTypes.

##### `ExportRailingTypes`
```csharp
public bool ExportRailingTypes
```
Gets or sets a value indicating whether to export RailingTypes.

##### `ExportStairsTypes`
```csharp
public bool ExportStairsTypes
```
Gets or sets a value indicating whether to export StairsTypes.

##### `ExportStandardViews`
```csharp
public bool ExportStandardViews
```
Gets or sets a value indicating whether to export StandardViews.

##### `ExportViewTemplates`
```csharp
public bool ExportViewTemplates
```
Gets or sets a value indicating whether to export ViewTemplates.

##### `ExportLineStyles`
```csharp
public bool ExportLineStyles
```
Gets or sets a value indicating whether to export LineStyles.

##### `ExportModelCategories`
```csharp
public bool ExportModelCategories
```
Gets or sets a value indicating whether to export ModelCategories.

##### `ExportAnnotationCategories`
```csharp
public bool ExportAnnotationCategories
```
Gets or sets a value indicating whether to export AnnotationCategories.

##### `ExportAnalyticalCategories`
```csharp
public bool ExportAnalyticalCategories
```
Gets or sets a value indicating whether to export AnalyticalCategories.

##### `ExportImportCategories`
```csharp
public bool ExportImportCategories
```
Gets or sets a value indicating whether to export ImportCategories.

##### `ExportGridTypes`
```csharp
public bool ExportGridTypes
```
Gets or sets a value indicating whether to export GridTypes.

##### `ExportLevelTypes`
```csharp
public bool ExportLevelTypes
```
Gets or sets a value indicating whether to export LevelTypes.

##### `ExportFillPatterns`
```csharp
public bool ExportFillPatterns
```
Gets or sets a value indicating whether to export FillPatterns.

##### `ExportLinePatterns`
```csharp
public bool ExportLinePatterns
```
Gets or sets a value indicating whether to export LinePatterns.

##### `ExportAppearanceAssets`
```csharp
public bool ExportAppearanceAssets
```
Gets or sets a value indicating whether to export AppearanceAssets.

##### `ExportCurtainSystemTypes`
```csharp
public bool ExportCurtainSystemTypes
```
Gets or sets a value indicating whether to export CurtainSystemTypes.

##### `ExportMullionTypes`
```csharp
public bool ExportMullionTypes
```
Gets or sets a value indicating whether to export MullionTypes.

##### `ExportFasciaTypes`
```csharp
public bool ExportFasciaTypes
```
Gets or sets a value indicating whether to export FasciaTypes.

##### `ExportGutterTypes`
```csharp
public bool ExportGutterTypes
```
Gets or sets a value indicating whether to export GutterTypes.

##### `ExportTitleBlockTypes`
```csharp
public bool ExportTitleBlockTypes
```
Gets or sets a value indicating whether to export TitleBlockTypes.

##### `ExportViewFamilyTypes`
```csharp
public bool ExportViewFamilyTypes
```
Gets or sets a value indicating whether to export ViewFamilyTypes.

##### `ExportBrowserOrganizations`
```csharp
public bool ExportBrowserOrganizations
```
Gets or sets a value indicating whether to export BrowserOrganizations.

##### `ExportParameterElements`
```csharp
public bool ExportParameterElements
```
Gets or sets a value indicating whether to export ParameterElements.

##### `ExportParameterFilters`
```csharp
public bool ExportParameterFilters
```
Gets or sets a value indicating whether to export ParameterFilters.

##### `IsSystemFamiliesEnabled`
```csharp
public bool IsSystemFamiliesEnabled
```
Gets or sets a value indicating whether system families export is enabled.

##### `IsViewsEnabled`
```csharp
public bool IsViewsEnabled
```
Gets or sets a value indicating whether views export is enabled.

##### `LoadableFamilyCategories`
```csharp
public ObservableCollection<CategorySelectionItem> LoadableFamilyCategories
```
Gets the collection of loadable family categories.

##### `StandardsClassSelectionViewModel`
```csharp
public StandardsClassSelectionViewModel(Document doc)
```
Initializes a new instance of the `StandardsClassSelectionViewModel` class.

##### `GetCheckedCategoryIds`
```csharp
public List<ElementId> GetCheckedCategoryIds()
```
Gets a list of currently checked category ElementIds.

#### Fields
- **`ViewModelBase`**: `public class StandardsClassSelectionViewModel : ViewModelBase {`
  *Description:* ViewModel managing the modular class-level selection settings.
- **`SelectAllAnnotationsCommand`**: `public ICommand SelectAllAnnotationsCommand`
  *Description:* Command to select all annotation categories.
- **`SelectNoneAnnotationsCommand`**: `public ICommand SelectNoneAnnotationsCommand`
  *Description:* Command to deselect all annotation categories.
- **`SelectAllMaterialsCommand`**: `public ICommand SelectAllMaterialsCommand`
  *Description:* Command to select all materials, patterns, and assets.
- **`SelectNoneMaterialsCommand`**: `public ICommand SelectNoneMaterialsCommand`
  *Description:* Command to deselect all materials, patterns, and assets.
- **`SelectAllSystemTypesCommand`**: `public ICommand SelectAllSystemTypesCommand`
  *Description:* Command to select all system type classes.
- **`SelectNoneSystemTypesCommand`**: `public ICommand SelectNoneSystemTypesCommand`
  *Description:* Command to deselect all system type classes.
- **`SelectAllViewsCommand`**: `public ICommand SelectAllViewsCommand`
  *Description:* Command to select all views and view template options.
- **`SelectNoneViewsCommand`**: `public ICommand SelectNoneViewsCommand`
  *Description:* Command to deselect all views and view template options.
- **`SelectAllStandardsCommand`**: `public ICommand SelectAllStandardsCommand`
  *Description:* Command to select all standards categories (line styles, parameter elements, etc.).
- **`SelectNoneStandardsCommand`**: `public ICommand SelectNoneStandardsCommand`
  *Description:* Command to deselect all standards categories.
- **`SelectAllCategoriesCommand`**: `public ICommand SelectAllCategoriesCommand`
  *Description:* Command to select all category list items.
- **`SelectNoneCategoriesCommand`**: `public ICommand SelectNoneCategoriesCommand`
  *Description:* Command to deselect all category list items.

---
### Class: `StandardsReviewViewModel`
**File:** [src/SyntheticShared/Modules/StandardsManagement/ViewModels/StandardsReviewViewModel.cs](../src/SyntheticShared/Modules/StandardsManagement/ViewModels/StandardsReviewViewModel.cs)

#### Methods
##### `StandardsReviewViewModel`
```csharp
public StandardsReviewViewModel(Document doc, ObservableCollection<DuplicateClusterModel> clusters)
```
Initializes a new instance of the `StandardsReviewViewModel` class.

#### Fields
- **`ViewModelBase`**: `public class StandardsReviewViewModel : ViewModelBase {`
  *Description:* ViewModel for the standards review window. Manages the resolution and enforcement of style conflicts between JSON standards and the document.
- **`Clusters`**: `public ObservableCollection<DuplicateClusterModel> Clusters {`
  *Description:* Gets or sets the collection of standard comparison clusters.
- **`SelectedCluster`**: `public DuplicateClusterModel? SelectedCluster {`
  *Description:* Gets or sets the currently selected cluster (category) in the sidebar.
- **`SelectedClusterTypeMappings`**: `public ObservableCollection<TypeMappingModel>? SelectedClusterTypeMappings`
  *Description:* Gets the collection of type mappings for the selected cluster.
- **`SelectedTypeMapping`**: `public TypeMappingModel? SelectedTypeMapping {`
  *Description:* Gets or sets the currently selected type mapping in the element grid.
- **`Rows`**: `public ObservableCollection<ParameterDiffRowModel>? Rows`
  *Description:* Gets the collection of parameter resolutions for the selected type mapping.
- **`EnforceApprovedCommand`**: `public ICommand EnforceApprovedCommand`
  *Description:* Gets the command to enforce all approved overrides.
- **`CancelCommand`**: `public ICommand CancelCommand`
  *Description:* Gets the command to cancel the operation.
- **`CloseAction`**: `public Action? CloseAction {`
  *Description:* Gets or sets the action to close the window.

---
## Namespace: `Synthetic.Modules.ViewManagement.Commands`

### Class: `ConvertDraftingToLegend`
**File:** [src/SyntheticShared/Modules/ViewManagement/Commands/ConvertDraftingToLegend.cs](../src/SyntheticShared/Modules/ViewManagement/Commands/ConvertDraftingToLegend.cs)

#### Methods
##### `Execute`
```csharp
public Result Execute( ExternalCommandData commandData, ref string message, ElementSet elements)
```
Execute a Revit Command

##### `SelectViews`
```csharp
internal IList<View> SelectViews(IList<View> views, IntPtr mainWindowHandle)
```
Displays a checkbox selection dialog allowing the user to choose views for batch conversion.

#### Fields
- **`IExternalCommand`**: `public class ConvertDraftingToLegend : IExternalCommand {`
  *Description:* Converts Drafting Views to Legends

---
### Class: `ConvertLegendToDrafting`
**File:** [src/SyntheticShared/Modules/ViewManagement/Commands/ConvertLegendToDrafting.cs](../src/SyntheticShared/Modules/ViewManagement/Commands/ConvertLegendToDrafting.cs)

#### Methods
##### `Execute`
```csharp
public Result Execute( ExternalCommandData commandData, ref string message, ElementSet elements)
```
Execute a Revit Command

#### Fields
- **`IExternalCommand`**: `public class ConvertLegendToDrafting : IExternalCommand {`
  *Description:* Converts Legend Views to Drafting Views

---
### Class: `ViewAutoNumberConfig`
**File:** [src/SyntheticShared/Modules/ViewManagement/Commands/ViewAutoNumberConfig.cs](../src/SyntheticShared/Modules/ViewManagement/Commands/ViewAutoNumberConfig.cs)

#### Methods
##### `Execute`
```csharp
public Result Execute( ExternalCommandData commandData, ref string message, ElementSet elements)
```
Execute a Revit Command

##### `internal`
```csharp
internal (bool result, string? selection) SelectFamily(List<string> itemList, IntPtr mainWindowHandle)
```
Opens dialog to select the family to use for the origin and parameters for the view renumbering

##### `internal`
```csharp
internal (bool result, string? selection) SelectGrid(List<string> itemList, bool IsXGrid, IntPtr mainWindowHandle)
```
Opens dialog box to select the Grid Parameters with options for X Grid or Y Grid

#### Fields
- **`IExternalCommand`**: `public class ViewAutoNumberConfig : IExternalCommand {`
  *Description:* Revit Command to configure the View Autonumber command.

---
### Class: `ViewsAutoNumber`
**File:** [src/SyntheticShared/Modules/ViewManagement/Commands/ViewsAutoNumber.cs](../src/SyntheticShared/Modules/ViewManagement/Commands/ViewsAutoNumber.cs)

#### Methods
##### `Execute`
```csharp
public Result Execute( ExternalCommandData commandData, ref string message, ElementSet elements)
```
Execute a Revit Command

#### Fields
- **`IExternalCommand`**: `public class ViewsAutoNumber : IExternalCommand {`
  *Description:* Revit Command to automatically number views on the active sheet based on a family that determines the origin point and the grid spacing.

---
## Namespace: `Synthetic.Modules.ViewManagement.Models`

### Class: `ViewAutoNumModel`
**File:** [src/SyntheticShared/Modules/ViewManagement/Models/ViewAutoNumModel.cs](../src/SyntheticShared/Modules/ViewManagement/Models/ViewAutoNumModel.cs)

#### Methods
##### `ViewAutoNumModel`
```csharp
public ViewAutoNumModel(Document doc, string familyName, string symbolName, string xSpacingParameter, string ySpacingParameter)
```
Constructor

##### `AutoNumberOnSheet`
```csharp
public List<Viewport>? AutoNumberOnSheet (ViewSheet sheet)
```
Renumbers the views on the given sheet based on the views location in a grid

##### `_tempRenumberViewports`
```csharp
internal static List<Viewport> _tempRenumberViewports(List<ElementId> viewPortIds, Document doc)
```
Given a list of viewport element IDs, the function will get the viewport from the document and give each viewport a temporary sheet number. Function will ignore legends.

##### `_renumberViewports`
```csharp
internal static List<Viewport> _renumberViewports(List<Viewport> viewports, double gridX, double gridY, double originX, double originY)
```
Given a list of viewports, grid spacing and an origin point, function will renumber the viewports based on grid location.

##### `_IntToLetters`
```csharp
internal static string _IntToLetters(int value)
```
Given an Int, returns an equivalent letter from the alphabet.

#### Fields
- **`ViewAutoNumModel`**: `public class ViewAutoNumModel {`
  *Description:* Class used to automatically renumber views based on a grid. Uses a family on the sheet to determine the origin point and the spacing of the grid.
- **`Document`**: `public Document Document`
  *Description:* A Revit Document
- **`Family`**: `public FamilySymbol? Family`
  *Description:* A Revit Family used as the Origin Point
- **`XSpacingParameter`**: `public string XSpacingParameter`
  *Description:* Name of the parameter in the Revit Family that specifies the X grid spacing
- **`YSpacingParameter`**: `public string YSpacingParameter`
  *Description:* Name of the parameter in the Revit Family that specifies the X grid spacing
- **`XSpacing`**: `public double XSpacing`
  *Description:* The X grid spacing used to number views on relative the origin point.
- **`YSpacing`**: `public double YSpacing`
  *Description:* The Y grid spacing used to number views on relative the origin point.

---
## Namespace: `Synthetic.Modules.ViewManagement.Utilities`

### Class: `LegendsUtil`
**File:** [src/SyntheticShared/Modules/ViewManagement/Utilities/LegendsUtil.cs](../src/SyntheticShared/Modules/ViewManagement/Utilities/LegendsUtil.cs)

#### Methods
##### `ConvertFromDrafting`
```csharp
public static View? ConvertFromDrafting(View drafting, ref bool warningTripped)
```
Convert a Drafting view to a Legend

##### `ConvertToDrafting`
```csharp
public static View? ConvertToDrafting(View legend)
```
Convert a Legend view to a Drafting view

##### `PreprocessFailures`
```csharp
public FailureProcessingResult PreprocessFailures(FailuresAccessor failuresAccessor)
```
Preprocesses failures to delete warnings.

#### Fields
- **`LegendsUtil`**: `public class LegendsUtil {`
  *Description:* Utility functions for dealing with legends
- **`IDuplicateTypeNamesHandler`**: `public class CopyUseDestination : IDuplicateTypeNamesHandler {`
  *Description:* Overrides IDuplicateTypeNamsHandler for use in converting Drafting Views to Legends.
- **`IFailuresPreprocessor`**: `public class SuppressConstraintsPreprocessor : IFailuresPreprocessor {`
  *Description:* Suppresses constraint deletion warnings when grouping reference planes.
- **`WarningTripped`**: `public bool WarningTripped { get; private set; }`
  *Description:* Gets a value indicating whether a warning was tripped and suppressed.

---
### Class: `ViewUtil`
**File:** [src/SyntheticShared/Modules/ViewManagement/Utilities/ViewUtil.cs](../src/SyntheticShared/Modules/ViewManagement/Utilities/ViewUtil.cs)

#### Methods
##### `AutoNumber`
```csharp
public static List<revitViewport>? AutoNumber(RevitDoc doc, revitFamilySymbol familyType, string xGridName, string yGridName)
```
Renumbers the views on the Active Sheet

##### `SetWorksetVisibilityInView`
```csharp
public static bool SetWorksetVisibilityInView(revitView view, revitDB.Workset workset)
```
Sets a workset to visible in a specific view.

##### `_tempRenumberViewports`
```csharp
internal static List<revitViewport> _tempRenumberViewports(List<revitElemId> viewPortIds, RevitDoc doc)
```
Given a list of viewport element IDs, the function will get the viewport from the document and give each viewport a temporary sheet number. Function will ignore legends.

##### `_renumberViewports`
```csharp
internal static List<revitViewport> _renumberViewports(List<revitViewport> viewports, double gridX, double gridY, double originX, double originY)
```
Given a list of viewports, grid spacing and an origin point, function will renumber the viewports based on grid location.

#### Fields
- **`ViewUtil`**: `public class ViewUtil {`
  *Description:* Utility methods for managing and auto-numbering Revit Views.

---
## Namespace: `Synthetic.Modules.Worksets.Commands`

### Class: `ElementsOnWorksetRecord`
**File:** [src/SyntheticShared/Modules/Worksets/Commands/ElementsOnWorksetRecord.cs](../src/SyntheticShared/Modules/Worksets/Commands/ElementsOnWorksetRecord.cs)

#### Methods
##### `Execute`
```csharp
public Result Execute( ExternalCommandData commandData, ref string message, ElementSet elements)
```
Execute a Revit Command

#### Fields
- **`IExternalCommand`**: `public class ElementsOnWorksetRecord : IExternalCommand {`
  *Description:* Revit external command to record the elements associated with a user-selected workset to a JSON file.

---
### Class: `ElementsOnWorksetReload`
**File:** [src/SyntheticShared/Modules/Worksets/Commands/ElementsOnWorksetReload.cs](../src/SyntheticShared/Modules/Worksets/Commands/ElementsOnWorksetReload.cs)

#### Methods
##### `Execute`
```csharp
public Result Execute( ExternalCommandData commandData, ref string message, ElementSet elements)
```
Execute a Revit Command

#### Fields
- **`IExternalCommand`**: `public class ElementsOnWorksetReload : IExternalCommand {`
  *Description:* Revit external command to reload recorded elements from a JSON file and move them back to their recorded workset.

---
### Class: `ScopeBoxesMoveToWorkset`
**File:** [src/SyntheticShared/Modules/Worksets/Commands/ScopeBoxesMoveToWorkset.cs](../src/SyntheticShared/Modules/Worksets/Commands/ScopeBoxesMoveToWorkset.cs)

#### Methods
##### `Execute`
```csharp
public Result Execute( ExternalCommandData commandData, ref string message, ElementSet elements)
```
Executes the command to move scope boxes.

#### Fields
- **`IExternalCommand`**: `public class ScopeBoxesMoveToWorkset : IExternalCommand {`
  *Description:* Revit external command to move all scope boxes in the project to a selected workset and optionally update their visibility in views.

---
### Class: `WorksetSetFile`
**File:** [src/SyntheticShared/Modules/Worksets/Commands/WorksetSetFile.cs](../src/SyntheticShared/Modules/Worksets/Commands/WorksetSetFile.cs)

#### Methods
##### `Execute`
```csharp
public Result Execute( ExternalCommandData commandData, ref string message, ElementSet elements)
```
Execute a Revit Command

#### Fields
- **`IExternalCommand`**: `public class WorksetSetFile : IExternalCommand {`
  *Description:* Sets the path and sheet to an Excel file with Worksets. Stores the setting in an Extensible Storage.

---
### Class: `WorksetSettingsShow`
**File:** [src/SyntheticShared/Modules/Worksets/Commands/WorksetSettingsShow.cs](../src/SyntheticShared/Modules/Worksets/Commands/WorksetSettingsShow.cs)

#### Methods
##### `Execute`
```csharp
public Result Execute( ExternalCommandData commandData, ref string message, ElementSet elements)
```
Execute a Revit Command

#### Fields
- **`IExternalCommand`**: `public class WorksetSettingsShow : IExternalCommand {`
  *Description:* Displays the current path, worksheet and list of Worksets based on the current settings.

---
### Class: `WorksetStartView`
**File:** [src/SyntheticShared/Modules/Worksets/Commands/WorksetStartView.cs](../src/SyntheticShared/Modules/Worksets/Commands/WorksetStartView.cs)

#### Methods
##### `Execute`
```csharp
public Result Execute( ExternalCommandData commandData, ref string message, ElementSet elements)
```
Execute a Revit Command

#### Fields
- **`IExternalCommand`**: `public class WorksetStartView : IExternalCommand {`
  *Description:* Revit external command to set project info (such as starting view) based on workset Excel configuration settings.

---
### Class: `WorksetsImport`
**File:** [src/SyntheticShared/Modules/Worksets/Commands/WorksetsImport.cs](../src/SyntheticShared/Modules/Worksets/Commands/WorksetsImport.cs)

#### Methods
##### `Execute`
```csharp
public Result Execute( ExternalCommandData commandData, ref string message, ElementSet elements)
```
Execute a Revit Command

#### Fields
- **`IExternalCommand`**: `public class WorksetsImport : IExternalCommand {`
  *Description:* Revit Command to import Worksets based on an Excel file.

---
## Namespace: `Synthetic.Modules.Worksets.Models`

### Class: `ElementsOnWorkset`
**File:** [src/SyntheticShared/Modules/Worksets/Models/ElementsOnWorkset.cs](../src/SyntheticShared/Modules/Worksets/Models/ElementsOnWorkset.cs)

#### Methods
##### `ElementsOnWorkset`
```csharp
public ElementsOnWorkset()
```
Initializes a new instance of the ElementsOnWorkset class.

##### `ElementsOnWorkset`
```csharp
public ElementsOnWorkset(Workset workset, Document document)
```
Initializes a new instance of the ElementsOnWorkset class from a Revit workset and document.

##### `ElementsOnWorkset`
```csharp
public ElementsOnWorkset(string worksetName, Guid worksetUniqueId, List<string> elementUniqueIds)
```
Initializes a new instance of the ElementsOnWorkset class with specific properties.

##### `Add`
```csharp
public ElementsOnWorkset Add (string UniqueId)
```
Add a new element's UniqueId to the collection.

##### `MoveToWorkset`
```csharp
public List<Element>? MoveToWorkset(Document document)
```
Moves the elements with UniqueIds in the ElementsOnWorkset object to the Workset. If the workset doesn't exist, it will be created.

##### `ToJSON`
```csharp
public string ToJSON()
```
Serializes the object to JSON

##### `ByJSON`
```csharp
public static ElementsOnWorkset ByJSON(string JSON)
```
Deserializes the object from a JSON string.

##### `IfLog`
```csharp
public bool IfLog()
```
Checks if there are any results to log

##### `GetLog`
```csharp
public string GetLog()
```
Complies the log of results

#### Fields
- **`ElementsOnWorkset`**: `public class ElementsOnWorkset {`
  *Description:* Model that tracks elements associated with a specific workset. Used for exporting elements on a workset and recreating them back.
- **`WorksetName`**: `public string WorksetName`
  *Description:* Gets or sets the name of the workset.
- **`WorksetUniqueId`**: `public System.Guid WorksetUniqueId;`
  *Description:* Gets or sets the unique identifier of the workset.
- **`ElementUniqueIds`**: `public List<string> ElementUniqueIds;`
  *Description:* Gets or sets the list of unique element IDs.

---
### Class: `WorksetModel`
**File:** [src/SyntheticShared/Modules/Worksets/Models/WorksetModel.cs](../src/SyntheticShared/Modules/Worksets/Models/WorksetModel.cs)

#### Methods
##### `WorksetModel`
```csharp
public WorksetModel(string name, bool visibility = true, string? alias = null, string? description = null)
```
Initializes a new instance of the WorksetModel class.

#### Fields
- **`ObjectModel`**: `public class WorksetModel : ObjectModel {`
  *Description:* Represents a model representation of a Revit Workset, mapping its name, visibility, alias, and description.
- **`Name`**: `public string Name`
  *Description:* Gets or sets the name of the workset.
- **`Visibility`**: `public bool Visibility`
  *Description:* Gets or sets a value indicating whether the workset is visible.
- **`Alias`**: `public string? Alias`
  *Description:* Gets or sets the alias name of the workset.
- **`Description`**: `public string? Description`
  *Description:* Gets or sets the description of the workset.

---
## Namespace: `Synthetic.Modules.Worksets.Utilities`

### Class: `WorksetUtil`
**File:** [src/SyntheticShared/Modules/Worksets/Utilities/WorksetUtil.cs](../src/SyntheticShared/Modules/Worksets/Utilities/WorksetUtil.cs)

#### Methods
##### `WorksetUtil`
```csharp
public WorksetUtil()
```
Constructor that creates an empty WorksetUtil object

##### `WorksetUtil`
```csharp
public WorksetUtil(List<List<object>> cells)
```
Creates a list of WorksetModels from Excel cells.

##### `Add`
```csharp
public WorksetUtil Add (WorksetModel workset)
```
Adds a Workset to the WorksetUtil object.

##### `Names`
```csharp
public List<string> Names()
```
Gets a list of Workset names

##### `WorksetNames`
```csharp
public string WorksetNames()
```
Return a string Workset names with line breaks between

##### `WorksetVisibilities`
```csharp
public string WorksetVisibilities()
```
Returns a string of Workset visibilities with line breaks between

##### `WorksetDescriptions`
```csharp
public string WorksetDescriptions()
```
Returns a string of Workset descriptions with line breaks between

##### `Filter`
```csharp
public WorksetUtil Filter(List<string> filters)
```
Given a list of WorksetModel names, updates the WorksetUtil object to only include those WorksetModels

##### `SetProjectInfo`
```csharp
public ProjectInfo SetProjectInfo (RevitDoc doc)
```
Sets the "Workset Names", "Workset Visibility", and "Workset Descriptions" parameters on the ProjectInfo element.

##### `Create`
```csharp
public static Dictionary<string, revitWorkset>? Create (RevitDoc doc, WorksetUtil worksets)
```
Creates Worksets from a WorksetUtil object. Worksets that already exist are modified. Worksets are renamed if they have Aliases.

##### `Create`
```csharp
public static Dictionary<string, revitWorkset>? Create (RevitDoc doc, string name, bool visible = true, string alias = "", string description = "")
```
Creates a Workset given a Revit Document and workset information. Will modify an existing Workset. Worksets named as the alias, will be renamed.

##### `GetByName`
```csharp
public static revitWorkset? GetByName (string name, RevitDoc doc)
```
Retrieves the workset with the given name.

##### `GetByWorksetId`
```csharp
public static revitWorkset GetByWorksetId (WorksetId worksetId, RevitDoc doc)
```
Retrieves the workset with the given WorksetId.

##### `GetByWorksetUniqueId`
```csharp
public static revitWorkset GetByWorksetUniqueId(System.Guid worksetUniqueId, RevitDoc doc)
```
Retrieves the workset with the given Workset's UniqueId.

##### `GetElementsOnWorkset`
```csharp
public static IList<Element> GetElementsOnWorkset(Workset workset, RevitDoc document)
```
Retrieves all elements belonging to a specific workset in the document.

##### `GetUserWorksets`
```csharp
public static List<revitWorkset> GetUserWorksets (RevitDoc doc)
```
Retrieves all the user worksets from a document. Excludes view and family worksets.

##### `Rename`
```csharp
public static revitWorkset? Rename (revitWorkset workset, string name, RevitDoc doc)
```
Renames a workset.

##### `SetDefaultVisibility`
```csharp
public static revitWorkset SetDefaultVisibility (revitWorkset workset, bool visible, RevitDoc doc)
```
Sets the Default Visibility of a workset within a document.

#### Fields
- **`WorksetUtil`**: `public class WorksetUtil {`
  *Description:* Utilities for dealing with Worksets, WorksetModels and WorksetSettings.
- **`Worksets`**: `public Dictionary<string, WorksetModel> Worksets`
  *Description:* Dictionary of WorksetModels with Workset Name as Key.

---
## Namespace: `Synthetic.Properties`

### Class: `Resources`
**File:** [src/Synthetic2025/Properties/Resources.Designer.cs](../src/Synthetic2025/Properties/Resources.Designer.cs)

#### Fields
- **`Resources`**: `// This class was auto-generated by the StronglyTypedResourceBuilder // class via a tool like ResGen or Visual Studio. // To add or remove a member, edit your .ResX file then rerun ResGen // with the /str option, or rebuild your VS project. internal class Resources {`
  *Description:* A strongly-typed resource class, for looking up localized strings, etc.
- **`ResourceManager`**: `internal static global::System.Resources.ResourceManager ResourceManager {`
  *Description:* Returns the cached ResourceManager instance used by this class.
- **`Culture`**: `internal static global::System.Globalization.CultureInfo Culture {`
  *Description:* Overrides the current thread's CurrentUICulture property for all resource lookups using this strongly typed resource class.

---
## Namespace: `Synthetic.Settings`

### Class: `Config`
**File:** [src/SyntheticShared/Settings/Config.cs](../src/SyntheticShared/Settings/Config.cs)

#### Methods
##### `addinPath`
```csharp
public static string addinPath
```
Gets the folder path where the executing add-in assembly is located. Ignored in JSON.

##### `defaultSettingsPath`
```csharp
public static string defaultSettingsPath
```
Gets the default full file path to the settings file. Ignored in JSON.

##### `Config`
```csharp
public Config ()
```
Initializes a new instance of the Config class.

##### `Config`
```csharp
public Config (string name, object? settingsValue) : base()
```
Initializes a new instance of the Config class with a single setting option.

##### `SetSettings<T>`
```csharp
public void SetSettings<T>(string name, T value)
```
Sets a specific setting value by name.

##### `GetSettings<T>`
```csharp
public T GetSettings<T>(string name) where T : new()
```
Gets a specific setting value by name, deserializing it if necessary.

##### `Defaults`
```csharp
public Config Defaults ()
```
Populates the configuration with default settings.

##### `FilePath`
```csharp
public string FilePath (string filename)
```
Gets the full path for a file relative to the add-in directory.

##### `Contains`
```csharp
public bool Contains(string key)
```
Determines whether the configuration contains a setting with the specified key.

##### `ToString`
```csharp
public override string ToString ()
```
Returns a string representation of all settings.

##### `FilePathDefaults`
```csharp
public static string FilePathDefaults ()
```
Gets the default settings file path.

##### `ByJson`
```csharp
public static Config ByJson(string json)
```
Deserializes a Config instance from a JSON string.

##### `ReadFromFile`
```csharp
public static Config? ReadFromFile(string path)
```
Reads a Config instance from a JSON file.

##### `WriteToFile`
```csharp
public static void WriteToFile(string path, Config settings)
```
Writes a Config instance to a JSON file.

##### `ReadAppConfig`
```csharp
public static Config? ReadAppConfig()
```
Reads the application level configuration.

##### `WriteAppConfig`
```csharp
public static void WriteAppConfig(Config settings)
```
Writes the application level configuration.

##### `ReadProjectConfig`
```csharp
public static Config? ReadProjectConfig(Document doc)
```
Reads the project specific configuration.

##### `WriteProjectConfig`
```csharp
public static void WriteProjectConfig(Document doc)
```
Writes the current project configuration to the project specific file. This will overwrite the existing file.

#### Fields
- **`Config`**: `public class Config {`
  *Description:* Represents configuration settings for the add-in.
- **`defaultSettingsFile`**: `public static string defaultSettingsFile { get { return "SyntheticSettings.json"; } }`
  *Description:* Gets the default filename for settings. Ignored in JSON.
- **`settings`**: `public Dictionary<string, object?> settings`
  *Description:* Gets or sets the collection of configuration setting key-value pairs.

---
### Class: `ConfigCollection`
**File:** [src/SyntheticShared/Settings/ConfigCollection.cs](../src/SyntheticShared/Settings/ConfigCollection.cs)

#### Methods
##### `ConfigCollection`
```csharp
public ConfigCollection()
```
Initializes a new instance of the ConfigCollection class.

##### `GetAppConfig`
```csharp
public Config GetAppConfig()
```
Gets the application configuration.

##### `GetProjectConfig`
```csharp
public Config GetProjectConfig(string path)
```
Gets the project configuration associated with the specified file path.

##### `GetProjectConfig`
```csharp
public Config GetProjectConfig(RevitDoc doc)
```
Gets the project configuration associated with the specified Revit document.

##### `GetSettings<T>`
```csharp
public T GetSettings<T>(string path, string settingsName) where T : class, new()
```
Retrieves the requested settings from project configuration first, falling back to app configuration.

##### `GetSettings<T>`
```csharp
public T GetSettings<T>(RevitDoc doc, string settingsName) where T : class, new()
```
Retrieves the requested settings for the specified Revit document, falling back to app configuration.

##### `AddAppConfig`
```csharp
public ConfigCollection AddAppConfig (Config? configApp)
```
Sets/adds the application configuration.

##### `AddProjectConfig`
```csharp
public ConfigCollection AddProjectConfig(string path, Config value)
```
Sets/adds a project configuration associated with a file path.

##### `AddProjectConfig`
```csharp
public ConfigCollection AddProjectConfig(RevitDoc doc, Config value)
```
Sets/adds a project configuration associated with a Revit document.

##### `RemoveProjectConfig`
```csharp
public ConfigCollection RemoveProjectConfig(string path)
```
Removes the project configuration associated with the specified file path.

##### `RemoveProjectConfig`
```csharp
public ConfigCollection RemoveProjectConfig(RevitDoc doc)
```
Removes the project configuration associated with the specified Revit document.

##### `ContainsProjectConfig`
```csharp
public bool ContainsProjectConfig (string path)
```
Determines whether a project configuration exists for the specified file path.

##### `ContainsProjectConfig`
```csharp
public bool ContainsProjectConfig(RevitDoc doc)
```
Determines whether a project configuration exists for the specified Revit document.

#### Fields
- **`ConfigCollection`**: `public class ConfigCollection {`
  *Description:* Manages application-level and project-level configurations.

---
### Class: `FileUtilitySettings`
**File:** [src/SyntheticShared/Settings/FileUtilitySettings.cs](../src/SyntheticShared/Settings/FileUtilitySettings.cs)

#### Methods
##### `FileUtilitySettings`
```csharp
public FileUtilitySettings()
```
Initializes a new instance of the `FileUtilitySettings` class.

##### `Defaults`
```csharp
public FileUtilitySettings Defaults()
```
Populates default values.

##### `IsValid`
```csharp
public bool IsValid(Document doc)
```
Validates settings module.

#### Fields
- **`ISettingModule`**: `public class FileUtilitySettings : ISettingModule {`
  *Description:* Configuration settings for file utility operations, including archive and alternate paths.
- **`ModuleKey`**: `public string ModuleKey`
  *Description:* Gets the settings module key.
- **`Name`**: `public const string Name`
  *Description:* The name of this settings module key.
- **`ArchiveDirectories`**: `public List<string> ArchiveDirectories`
  *Description:* Gets or sets the list of archive directories.
- **`AlternatePaths`**: `public Dictionary<string, List<string>> AlternatePaths`
  *Description:* Gets or sets the dictionary of alternate paths and their replacements.

---
### Class: `MaterialLibrarySettings`
**File:** [src/SyntheticShared/Settings/MaterialLibrarySettings.cs](../src/SyntheticShared/Settings/MaterialLibrarySettings.cs)

#### Methods
##### `MaterialLibrarySettings`
```csharp
public MaterialLibrarySettings()
```
Initializes a new instance of the `MaterialLibrarySettings` class.

##### `Defaults`
```csharp
public MaterialLibrarySettings Defaults()
```
Populates default values.

##### `IsValid`
```csharp
public bool IsValid(Document doc)
```
Validates that the folder path exists.

#### Fields
- **`ISettingModule`**: `public class MaterialLibrarySettings : ISettingModule {`
  *Description:* Configuration settings for the material library path.
- **`ModuleKey`**: `public string ModuleKey`
  *Description:* Gets the settings module key.
- **`Name`**: `public const string Name`
  *Description:* The name of this settings module key.
- **`LibraryFolderPath`**: `public string LibraryFolderPath`
  *Description:* Gets or sets the folder path to the material library.

---
### Class: `ProjectMaterialSettings`
**File:** [src/SyntheticShared/Settings/ProjectMaterialSettings.cs](../src/SyntheticShared/Settings/ProjectMaterialSettings.cs)

#### Methods
##### `ProjectMaterialSettings`
```csharp
public ProjectMaterialSettings()
```
Initializes a new instance of the `ProjectMaterialSettings` class.

##### `Defaults`
```csharp
public ProjectMaterialSettings Defaults()
```
Populates default values.

##### `GetResolvedPath`
```csharp
public string? GetResolvedPath(Document doc)
```
Resolves the absolute directory path of the materials.

##### `IsValid`
```csharp
public bool IsValid(Document doc)
```
Validates that the resolved folder path exists.

#### Fields
- **`ISettingModule`**: `public class ProjectMaterialSettings : ISettingModule {`
  *Description:* Configuration settings for project-specific material paths.
- **`ModuleKey`**: `public string ModuleKey`
  *Description:* Gets the settings module key.
- **`Name`**: `public const string Name`
  *Description:* The name of this settings module key.
- **`DefaultRelativePath`**: `public string DefaultRelativePath`
  *Description:* Gets or sets the default relative path.
- **`OverrideFolderPath`**: `public string OverrideFolderPath`
  *Description:* Gets or sets the override folder path.

---
### Class: `StandardsSettings`
**File:** [src/SyntheticShared/Settings/StandardsSettings.cs](../src/SyntheticShared/Settings/StandardsSettings.cs)

#### Methods
##### `StandardsSettings`
```csharp
public StandardsSettings()
```
Initializes a new instance of the `StandardsSettings` class.

##### `IsValid`
```csharp
public bool IsValid(Document doc)
```
Validates settings module.

#### Fields
- **`ISettingModule`**: `public class StandardsSettings : ISettingModule {`
  *Description:* Configuration settings for the standards workflow, storing the path to the standards JSON file. This module is serialized and saved inside the Revit Document's Extensible Storage schema to persist the firmwide or project-specific standards path across user sessions.
- **`ModuleKey`**: `public string ModuleKey`
  *Description:* Gets the settings module key.
- **`Name`**: `public const string Name`
  *Description:* The name of this settings module key.
- **`StandardsFilePath`**: `public string StandardsFilePath`
  *Description:* Gets or sets the path to the standards JSON file.

---
### Class: `SyncSettings`
**File:** [src/SyntheticShared/Settings/SyncSettings.cs](../src/SyntheticShared/Settings/SyncSettings.cs)

#### Methods
##### `SyncSettings`
```csharp
public SyncSettings()
```
Initializes a new instance of the `SyncSettings` class.

##### `IsValid`
```csharp
public bool IsValid(Document doc)
```
Validates the settings module within the document context.

#### Fields
- **`ISettingModule`**: `public class SyncSettings : ISettingModule {`
  *Description:* Configuration settings for project configuration synchronization.
- **`Name`**: `public const string Name`
  *Description:* The setting configuration name.
- **`ModuleKey`**: `public string ModuleKey`
  *Description:* Gets the settings module key.
- **`LinkedFilePath`**: `public string LinkedFilePath`
  *Description:* Gets or sets the path to the linked external settings JSON file.

---
### Class: `ViewAutoNumSettings`
**File:** [src/SyntheticShared/Settings/ViewAutoNumSettings.cs](../src/SyntheticShared/Settings/ViewAutoNumSettings.cs)

#### Methods
##### `ViewAutoNumSettings`
```csharp
public ViewAutoNumSettings()
```
Constructor

##### `ViewAutoNumSettings`
```csharp
public ViewAutoNumSettings(string family, string familytype, string xGridName, string yGridName)
```
Constructor

##### `Defaults`
```csharp
public ViewAutoNumSettings Defaults()
```
Reset the ViewAutoNumSettings properties to the Defaults

##### `IsValid`
```csharp
public bool IsValid (Document doc)
```
Checks if all the setting values exist and the family and type are loaded in the project.

#### Fields
- **`ISettingModule`**: `public class ViewAutoNumSettings : ISettingModule {`
  *Description:* Settings for Autonumbering views.
- **`ModuleKey`**: `public string ModuleKey { get { return Name; } }`
  *Description:* Gets the settings module key.
- **`Name`**: `public const string Name`
  *Description:* The setting configuration name.
- **`ViewAutoNumFamily`**: `public string ViewAutoNumFamily`
  *Description:* Gets or sets the family name used for view autonumbering.
- **`ViewAutoNumFamilyType`**: `public string ViewAutoNumFamilyType`
  *Description:* Gets or sets the family type name used for view autonumbering.
- **`ViewAutoNumXGridName`**: `public string ViewAutoNumXGridName`
  *Description:* Gets or sets the X grid spacing parameter name.
- **`ViewAutoNumYGridName`**: `public string ViewAutoNumYGridName`
  *Description:* Gets or sets the Y grid spacing parameter name.

---
### Class: `WorksetSettings`
**File:** [src/SyntheticShared/Settings/WorksetSettings.cs](../src/SyntheticShared/Settings/WorksetSettings.cs)

#### Methods
##### `WorksetSettings`
```csharp
public WorksetSettings ()
```
Initializes a new instance of the `WorksetSettings` class.

##### `WorksetSettings`
```csharp
public WorksetSettings (string? file, string? path = null, string? group = null)
```
Constructor using individual parameters to create

##### `Defaults`
```csharp
public WorksetSettings Defaults ()
```
App default WorksetSettings object

##### `PathOrDefault`
```csharp
public string PathOrDefault()
```
If the WorksetPath setting is a path, return the path, otherwise return the addin's path.

##### `FullPath`
```csharp
public string? FullPath()
```
Takes the WorksetPath and WorksetFile to create a path to the file. If the WorksetPath is empty, uses the addin's path.

##### `IsValid`
```csharp
public bool IsValid (Document doc)
```
Validates the settings module within the document context.

##### `IsSettingsEmpty`
```csharp
public bool IsSettingsEmpty()
```
Checks if the settings are empty (i.e. no WorksetFile is defined).

##### `FileExists`
```csharp
public bool FileExists ()
```
Checks if the Workset File exists at the path

##### `WorksetsByExcel`
```csharp
public WorksetUtil? WorksetsByExcel ()
```
Loads workset configurations from the Excel file specified in the settings.

#### Fields
- **`ISettingModule`**: `public class WorksetSettings : ISettingModule {`
  *Description:* Configuration settings for worksets, including excel mapping files and group configurations.
- **`ModuleKey`**: `public string ModuleKey { get { return Name; } }`
  *Description:* Gets the settings module key.
- **`Name`**: `public const string Name`
  *Description:* Name of the WorksetSettings type
- **`WorksetFile`**: `public string? WorksetFile`
  *Description:* Gets or sets the workset configuration Excel filename.
- **`WorksetPath`**: `public string? WorksetPath`
  *Description:* Gets or sets the path to the workset Excel file.
- **`WorksetGroup`**: `public string? WorksetGroup`
  *Description:* Gets or sets the workset group name.

---
## Namespace: `Synthetic.Shared`

### Class: `EnumUtil`
**File:** [src/SyntheticShared/Shared/EnumUtil.cs](../src/SyntheticShared/Shared/EnumUtil.cs)

#### Methods
##### `Parse`
```csharp
public static System.Object? Parse(string enumTypeName, string name)
```
Retrieves a enum of the given type and name.

##### `GetNames`
```csharp
public static List<string> GetNames(string enumTypeName)
```
Retrieves a list of the names of the constants in a specified enumeration.

##### `GetName`
```csharp
public static string GetName(Enum enumeration)
```
Retrieves the name as a string of the enumeration.

##### `GetEnums`
```csharp
public static List<System.Object> GetEnums(string enumTypeName)
```
Retrieves all enums of a given type.

##### `GetValue`
```csharp
public static object GetValue(object enumeration)
```
Evaluates a enum and returns its value.

##### `GetEnumType`
```csharp
public static Type? GetEnumType(string enumTypeName)
```
Retrieves the Enum Type from a string name.

##### `IsDefined`
```csharp
public static bool IsDefined(string enumTypeName, string name)
```
Returns an indication whether a constant with a specified value exists in a specified enumeration.

#### Fields
- **`EnumUtil`**: `public class EnumUtil {`
  *Description:* Wrapper for using enumerations

---
## Namespace: `Synthetic.Shared.RevitAPI`

### Class: `CommandUtil`
**File:** [src/SyntheticShared/Shared/RevitAPI/CommandUtil.cs](../src/SyntheticShared/Shared/RevitAPI/CommandUtil.cs)

#### Methods
##### `SaveResults`
```csharp
public static string? SaveResults(object results, Document document, string fileName = "Results", string? path = null)
```
Saves a command results to a json file in the same location as the document.

##### `SaveAsResults`
```csharp
public static string? SaveAsResults(object results)
```
Displays a save file dialog and saves the command results to the chosen JSON file path.

##### `SaveAsJSON`
```csharp
public static string? SaveAsJSON(string? initialFileName = null)
```
Opens a FileSaveDialog to select a path and file name for a JSON file.

##### `OpenJSON`
```csharp
public static string? OpenJSON()
```
Selects a JSON file to open

#### Fields
- **`CommandUtil`**: `public class CommandUtil {`
  *Description:* Utility methods for executing Revit commands and saving/loading results.

---
### Class: `DocumentUtil`
**File:** [src/SyntheticShared/Shared/RevitAPI/DocumentUtil.cs](../src/SyntheticShared/Shared/RevitAPI/DocumentUtil.cs)

#### Methods
##### `OpenRvtDetached`
```csharp
public static Document? OpenRvtDetached(ModelPath path, UIApplication uiApp)
```
Opens an RVT Revit file as detached.

##### `GetFilePath`
```csharp
public static string? GetFilePath(Document document)
```
Retrieves the file path of the document depending on if the file is a cloud model, workshared or just a regular project.

##### `GetFolderPath`
```csharp
public static string? GetFolderPath(Document document)
```
Retrieves the folder path of the document depending on if the file is a cloud model, workshared or just a regular project.

##### `Purge`
```csharp
public static bool Purge(Application app, Document doc)
```
Using etransmit, purges the model.

#### Fields
- **`DocumentUtil`**: `public class DocumentUtil {`
  *Description:* Utility methods for interacting with Revit Document objects.

---
### Class: `ElementUtil`
**File:** [src/SyntheticShared/Shared/RevitAPI/ElementUtil.cs](../src/SyntheticShared/Shared/RevitAPI/ElementUtil.cs)

#### Methods
##### `ElementUtil`
```csharp
internal ElementUtil()
```
Dummy constructor for the class. Not used.

##### `Document`
```csharp
public static RevitDoc Document(RevitElem Element)
```
Gets an elements document

##### `SetParamterByDictionary`
```csharp
public static RevitElem SetParamterByDictionary(RevitElem element, Dictionary<string, RevitDB.Parameter> dictionary)
```
Sets an element's parameters based on a Dictionary object with the Key being the parameter name and the Value being the parameter value.

##### `GetParamterToDictionary`
```csharp
public static Dictionary<string, RevitDB.Parameter> GetParamterToDictionary(RevitElem element, List<string> parameterNames)
```
Gets the listed parameters of an element and returns a Dictionary with the Key being the parameter name and the Value being the parameter value.

##### `TransferParameters`
```csharp
public static RevitElem TransferParameters(RevitElem Element, RevitElem SourceElement)
```
Overwrite an elements parameters with the parameter values from a source element.

##### `CopyElements`
```csharp
public static List<RevitElemId> CopyElements(RevitDoc sourceDoc, List<int> elementIds, RevitDoc destinationDoc)
```
Copy elements to the same location between documents. Can be used to copy system types or view templates between documents. Model elements are copied in the same location. If the elements already exist, Revit will give you an option to either duplicate the types or cancel the operation. Please note that documents are to be a Autodesk.Revit.DB.Document objects, not a Dynamo wrapped Revit Document.

##### `TransferElements`
```csharp
public static RevitElem TransferElements( RevitElem Element, RevitElem SourceElement)
```
Overwrites the parameters of an element with the parameters of an element from a different document. Associated elements such as materials may be duplicated in the document.

##### `SetCategory`
```csharp
public static IDictionary SetCategory(RevitElem element, RevitDB.Category category)
```
Changes an element's subcategory. Only works inside of Family documents

##### `SetWorkset`
```csharp
public static List<RevitElem> SetWorkset(List<RevitElem> elements, Workset workset, RevitDoc document)
```
Given a list of elements, sets their worksets to the given workset.

##### `MergeElementTypes`
```csharp
public static IDictionary MergeElementTypes(RevitElem FromType, RevitElem ToType)
```
Merges ElementType FromType into ToType. FromType will be deleted if all instances of the Type are successfully changed. Elements in groups will not be changed.

##### `MergeElementTypesRevit`
```csharp
public static IDictionary MergeElementTypesRevit(RevitElem FromType, RevitElem ToType)
```
Merges ElementType FromType into ToType. FromType will be deleted if all instances of the Type are successfully changed. Elements in groups will not be changed.

##### `//[MultiReturn`
```csharp
//[MultiReturn(new[]
```
Tests whether the element has a parameter of a given value. Returns true if the parameter has an equal value and false otherwise. A list of parameters names can be given to test against values in elements within parameters. For example, one can test for a type description from a instance by creating a list of each parameter. Please note that the comparision is done using the string representation of each parameter.

##### `GetByElementId`
```csharp
public static RevitElem GetByElementId(RevitElemId elementId, RevitDoc document)
```
Gets an element given the ElementId

##### `GetByUniqueId`
```csharp
public static RevitElem GetByUniqueId(string UniqueId, RevitDoc document)
```
Gets an element given its UniqueId

##### `Id`
```csharp
public static RevitElemId Id(System.Object Element)
```
Gets a Element's ElementId

##### `Name`
```csharp
public static string Name(System.Object Element)
```
Gets a Element's name

##### `UniqueId`
```csharp
public static string UniqueId(System.Object Element)
```
Gets a Element's UniqueId

##### `CastRevitElement`
```csharp
public static RevitElem CastRevitElement(System.Object Object)
```
If the object

##### `PaintElement`
```csharp
public static RevitElem PaintElement(RevitElem Element, RevitElemId MaterialId)
```
Paints every face in an element with a material

##### `RemovePaintElement`
```csharp
public static RevitElem RemovePaintElement(RevitElem Element)
```
Removes all painted faces on an elementl

#### Fields
- **`ElementUtil`**: `public class ElementUtil {`
  *Description:* Manipulation and modification of Dynamo wrapped Revit elements.

---
### Class: `FamilySymbolUtil`
**File:** [src/SyntheticShared/Shared/RevitAPI/FamilySymbolUtil.cs](../src/SyntheticShared/Shared/RevitAPI/FamilySymbolUtil.cs)

#### Methods
##### `GetByName`
```csharp
public static FamilySymbol? GetByName (RevitDoc doc, string familyName, string symbolName)
```
Retrieves a FamilySymbol by its family name and symbol/type name.

##### `GetFamiliesOfCategory`
```csharp
public static IList<Element> GetFamiliesOfCategory (RevitDoc doc, BuiltInCategory category)
```
Gets all family symbols/types in the document belonging to the specified BuiltInCategory.

##### `IsLoaded`
```csharp
public static bool IsLoaded (RevitDoc doc, string familyName, string symbolName)
```
Checks if a FamilySymbol with the specified family name and symbol name is loaded in the document.

##### `GetOrigin`
```csharp
public static XYZ? GetOrigin(FamilySymbol? family)
```
Gets the location point origin of a FamilySymbol.

##### `GetInstancesInView`
```csharp
public static FilteredElementCollector GetInstancesInView (Document doc, FamilySymbol family, View view)
```
Gets all instances of a FamilySymbol that exist within a specific View.

#### Fields
- **`FamilySymbolUtil`**: `public class FamilySymbolUtil {`
  *Description:* Utility methods for managing and query Revit FamilySymbol elements.

---
### Class: `FamilyUtil`
**File:** [src/SyntheticShared/Shared/RevitAPI/FamilyUtil.cs](../src/SyntheticShared/Shared/RevitAPI/FamilyUtil.cs)

#### Methods
##### `AuditProjectFamilies`
```csharp
public static Dictionary<string, object> AuditProjectFamilies(Document document, bool purge = false, bool purgeSchema = false, List<string>? schemaExceptions = null)
```
Opens all the Families in a project to test if any have been corrupted.

##### `LoadStandards`
```csharp
public static void LoadStandards(Document doc, IEnumerable<ElementModel> standards)
```
Opens every Annotation family in the project and creates the standard element types.

##### `ForceReinsertAnnotation`
```csharp
public static void ForceReinsertAnnotation(Document doc)
```
Forces all annotation families in the project to be reinserted/reloaded to ensure they are up to date.

##### `SetIsChanged`
```csharp
public static void SetIsChanged (Document doc, Document familyDoc)
```
Marks a family document as changed/dirty by creating and deleting a temporary text note.

##### `ResolveWarnings`
```csharp
private static void ResolveWarnings(object? sender, FailuresProcessingEventArgs e)
```
Catches warners and resolves them so they are not displayed to the user.

#### Fields
- **`FamilyUtil`**: `public class FamilyUtil {`
  *Description:* Utility methods for auditing, loading, and modifying Revit Families.

---
### Class: `Select`
**File:** [src/SyntheticShared/Shared/RevitAPI/Select.cs](../src/SyntheticShared/Shared/RevitAPI/Select.cs)

#### Methods
##### `AllElementsOfType`
```csharp
//public static IList<RevitElem> AllElementsOfType(Type type, // bool inverted, // RevitDoc document) //
```
// Selects all elements of a type. Works with documents other than the active document. //

##### `AllElementsOfCategoryDynamo`
```csharp
//public static IList<RevitElem> AllElementsOfCategoryDynamo(RevitDB.Category categoryDynamo, // bool inverted, // RevitDoc document) //
```
// Selects all instance elements in a category, excludes element types. Use a Dynamo Category wrapper //

##### `AllFamilyTypesOfCategory`
```csharp
//public static IList<RevitElem> AllFamilyTypesOfCategory(RevitDB.Category category, // bool inverted, // RevitDoc document) //
```
// Selects all Family Symbol types in a category, but excludes instances of those elements. The node does not work with System familes because System Families do not have a Family Sybmol. //

##### `AllMaterials`
```csharp
public static IEnumerable<RevitDB.Material> AllMaterials(RevitDoc document)
```
Retrieve all materials in the document.

##### `GetMaterialByName`
```csharp
public static RevitDB.Material? GetMaterialByName(IEnumerable<RevitDB.Material> materials, string materialName)
```
Given a list of materials, returns the material that matches the given name.

##### `ElementByNameClass`
```csharp
public static RevitElem? ElementByNameClass(string Name, Type Class, RevitDoc document)
```
Retrieves a Revit element of the specified class/type that matches the given name.

##### `RevitClassByString`
```csharp
public static Type? RevitClassByString(string typeName)
```
Get the Type of a Revit Class from RevitAPI.dll given its name.

##### `InstanceClassFromTypeClass`
```csharp
public static Type? InstanceClassFromTypeClass(Type elementType)
```
Given the an ElementType Type retrieves the corresponding Instance Type. For example Type WallType returns Type Wall or Type TextNoteType returns Type TextNote.

##### `GetInstancesFromElemType`
```csharp
public static IEnumerable<RevitElem> GetInstancesFromElemType(RevitElem ElemType, RevitDoc document)
```
Retrieves all instances in the document that use the specified element type.

##### `GetElementTypeByName`
```csharp
public static RevitElem? GetElementTypeByName(string Name, RevitDoc document)
```
Retrieves an ElementType by its name.

#### Fields
- **`Select`**: `public class Select {`
  *Description:* Nodes that certain sets of elements using pre-configured Collectors and filters.

---
### Class: `StorageUtil`
**File:** [src/SyntheticShared/Shared/RevitAPI/StorageUtil.cs](../src/SyntheticShared/Shared/RevitAPI/StorageUtil.cs)

#### Methods
##### `DoesAnyStorageExist`
```csharp
public static bool DoesAnyStorageExist(Document doc)
```
Returns true if any extensible storage exists in the document, false otherwise.

##### `PurgeSchema`
```csharp
public static List<string>? PurgeSchema(List<Schema> schemas, Document document)
```
Purges the given list of Schema from all open documents.

##### `GetDocumentSchemas`
```csharp
public static List<Schema>? GetDocumentSchemas(Document doc)
```
Gets the schemas that have elements in the given document.

##### `GetElementStringWithAllSchemas`
```csharp
public static string GetElementStringWithAllSchemas(Document doc)
```
Returns a formatted string containing schema guids and element info for all elements containing extensible storage.

##### `GetElementStringWithSchema`
```csharp
private static string GetElementStringWithSchema(Document doc, Schema schema)
```
Returns a formatted string containing a schema guid and element info for all elements containing extensible storage of a given schema.

##### `ElementsWithStorage`
```csharp
private static List<ElementId> ElementsWithStorage(Document doc, Schema schema)
```
Returns a list of ElementIds that contain extensible storage of a given schema using the ExtensibleStorageFilter ElementQuickFilter.

##### `PrintElementInfo`
```csharp
private static string PrintElementInfo(ElementId id, Document document)
```
Writes basic element info to a string.

#### Fields
- **`StorageUtil`**: `public class StorageUtil {`
  *Description:* Utility methods for interacting with Revit Extensible Storage.

---
## Namespace: `Synthetic.Shared.UI`

### Class: `DropdownSelectionViewModel`
**File:** [src/SyntheticShared/Shared/UI/DropdownSelectionViewModel.cs](../src/SyntheticShared/Shared/UI/DropdownSelectionViewModel.cs)

#### Methods
##### `DropdownSelectionViewModel`
```csharp
public DropdownSelectionViewModel()
```
Initializes a new instance of the `DropdownSelectionViewModel` class.

#### Fields
- **`ViewModelBase`**: `public class DropdownSelectionViewModel : ViewModelBase {`
  *Description:* ViewModel for dropdown selection views.
- **`Title`**: `public string Title {`
  *Description:* Gets or sets the title of the view.
- **`Instruction`**: `public string Instruction {`
  *Description:* Gets or sets the instructions shown to the user.
- **`ItemLabel`**: `public string ItemLabel {`
  *Description:* Gets or sets the label for the dropdown.
- **`Items`**: `public IEnumerable<string> Items {`
  *Description:* Gets or sets the items in the dropdown list.
- **`SelectedItem`**: `public string? SelectedItem {`
  *Description:* Gets or sets the selected item from the dropdown list.
- **`IsSorted`**: `public bool IsSorted {`
  *Description:* Gets or sets a value indicating whether items are sorted alphabetically.
- **`CloseAction`**: `public Action<bool>? CloseAction`
  *Description:* Gets or sets the action to close the window, passing a boolean result.
- **`OkCommand`**: `public ICommand OkCommand`
  *Description:* Gets the OK command.
- **`CancelCommand`**: `public ICommand CancelCommand`
  *Description:* Gets the Cancel command.

---
### Class: `EnumToBooleanConverter`
**File:** [src/SyntheticShared/Shared/UI/EnumToBooleanConverter.cs](../src/SyntheticShared/Shared/UI/EnumToBooleanConverter.cs)

#### Methods
##### `Convert`
```csharp
public object Convert(object value, Type targetType, object parameter, CultureInfo culture)
```
Converts an enum value to a boolean. Returns true if the value equals the parameter.

##### `ConvertBack`
```csharp
public object ConvertBack(object value, Type targetType, object parameter, CultureInfo culture)
```
Converts a boolean back to the enum value. Returns the parameter if value is true.

#### Fields
- **`IValueConverter`**: `public class EnumToBooleanConverter : IValueConverter {`
  *Description:* Converts an enum value to a boolean for binding RadioButtons to Enum properties.

---
### Class: `ListByCheckboxViewModel`
**File:** [src/SyntheticShared/Shared/UI/ListByCheckboxViewModel.cs](../src/SyntheticShared/Shared/UI/ListByCheckboxViewModel.cs)

#### Methods
##### `ListByCheckboxViewModel`
```csharp
public ListByCheckboxViewModel()
```
Initializes a new instance of the `ListByCheckboxViewModel` class.

##### `string.IsNullOrEmpty`
```csharp
public System.Windows.Visibility InstructionVisibility => string.IsNullOrEmpty(Instruction) ? System.Windows.Visibility.Collapsed : System.Windows.Visibility.Visible
```
Gets the visibility status of the instructions.

##### `SetItems`
```csharp
public void SetItems(IEnumerable<string> itemNames, bool checkAll = false)
```
Sets the checkable items from a list of item names.

##### `SetItems`
```csharp
public void SetItems(IEnumerable<Tuple<string, ElementId>> items, bool checkAll = false)
```
Sets the checkable items from a list of tuples containing item names and ElementIds.

##### `CheckableItem`
```csharp
public CheckableItem(string name, bool isChecked = false, Action<CheckableItem>? onCheckChanged = null)
```
Initializes a new instance of the `CheckableItem` class.

##### `CheckableItem`
```csharp
public CheckableItem(string name, ElementId? value, bool isChecked = false, Action<CheckableItem>? onCheckChanged = null)
```
Initializes a new instance of the `CheckableItem` class.

#### Fields
- **`ViewModelBase`**: `public class ListByCheckboxViewModel : ViewModelBase {`
  *Description:* ViewModel for lists where items can be selected using checkboxes.
- **`Title`**: `public string Title {`
  *Description:* Gets or sets the title of the view.
- **`Instruction`**: `public string Instruction {`
  *Description:* Gets or sets the instructions shown to the user.
- **`IsSorted`**: `public bool IsSorted {`
  *Description:* Gets or sets a value indicating whether items are sorted alphabetically.
- **`IsSingleSelection`**: `public bool IsSingleSelection {`
  *Description:* Gets or sets a value indicating whether only one item can be selected at a time.
- **`SelectAllVisibility`**: `public System.Windows.Visibility SelectAllVisibility`
  *Description:* Gets the visibility status of the select all command.
- **`Items`**: `public ObservableCollection<CheckableItem> Items {`
  *Description:* Gets or sets the observable collection of checkable items.
- **`CheckedItems`**: `public List<string> CheckedItems {`
  *Description:* Gets the list of names of items that are checked.
- **`CheckedElementIds`**: `public List<ElementId> CheckedElementIds {`
  *Description:* Gets the list of ElementIds of items that are checked.
- **`CloseAction`**: `public Action<bool>? CloseAction`
  *Description:* Gets or sets the action to close the window, passing a boolean result.
- **`OkCommand`**: `public ICommand OkCommand`
  *Description:* Gets the OK command.
- **`CancelCommand`**: `public ICommand CancelCommand`
  *Description:* Gets the Cancel command.
- **`SelectAllCommand`**: `public ICommand SelectAllCommand`
  *Description:* Gets the Select All command.
- **`SelectNoneCommand`**: `public ICommand SelectNoneCommand`
  *Description:* Gets the Select None command.
- **`ViewModelBase`**: `public class CheckableItem : ViewModelBase {`
  *Description:* Represents an item in a checklist that can be checked or unchecked.
- **`Name`**: `public string Name {`
  *Description:* Gets or sets the display name of the item.
- **`Value`**: `public ElementId? Value {`
  *Description:* Gets or sets the ElementId value associated with the item.
- **`IsChecked`**: `public bool IsChecked {`
  *Description:* Gets or sets a value indicating whether the item is checked.

---
### Class: `RelayCommand`
**File:** [src/SyntheticShared/Shared/UI/RelayCommand.cs](../src/SyntheticShared/Shared/UI/RelayCommand.cs)

#### Methods
##### `RelayCommand`
```csharp
public RelayCommand(Action<object> execute, Predicate<object>? canExecute = null)
```
Initializes a new instance of the `RelayCommand` class.

##### `CanExecute`
```csharp
public bool CanExecute(object? parameter)
```
Determines whether the command can execute in its current state.

##### `Execute`
```csharp
public void Execute(object? parameter)
```
Executes the command.

#### Fields
- **`ICommand`**: `public class RelayCommand : ICommand {`
  *Description:* A command implementation that relays its functionality by invoking delegates.
- **`CanExecuteChanged`**: `public event EventHandler? CanExecuteChanged {`
  *Description:* Event raised when the command execution status changes.

---
### Class: `SelectSearchPathsViewModel`
**File:** [src/SyntheticShared/Shared/UI/SelectSearchPathsViewModel.cs](../src/SyntheticShared/Shared/UI/SelectSearchPathsViewModel.cs)

#### Methods
##### `SelectSearchPathsViewModel`
```csharp
public SelectSearchPathsViewModel()
```
Initializes a new instance of the `SelectSearchPathsViewModel` class.

##### `string.IsNullOrEmpty`
```csharp
public Visibility InstructionVisibility => string.IsNullOrEmpty(Instruction) ? Visibility.Collapsed : Visibility.Visible
```
Gets the visibility status of the instruction text.

##### `CheckAllItems`
```csharp
public void CheckAllItems()
```
Checks all user search paths.

##### `CheckAllDefaults`
```csharp
public void CheckAllDefaults()
```
Checks all default search paths.

#### Fields
- **`ViewModelBase`**: `public class SelectSearchPathsViewModel : ViewModelBase {`
  *Description:* ViewModel for selecting search paths.
- **`Title`**: `public string Title {`
  *Description:* Gets or sets the title of the window.
- **`Instruction`**: `public string Instruction {`
  *Description:* Gets or sets the instruction text displayed to the user.
- **`UserPaths`**: `public ObservableCollection<CheckableItem> UserPaths {`
  *Description:* Gets or sets the list of search paths added by the user.
- **`DefaultPaths`**: `public ObservableCollection<CheckableItem> DefaultPaths {`
  *Description:* Gets or sets the list of default search paths.
- **`CheckedItems`**: `public List<string> CheckedItems {`
  *Description:* Gets the list of checked search path strings.
- **`CloseAction`**: `public Action<bool>? CloseAction`
  *Description:* Gets or sets the action to close the window, passing a boolean result.
- **`OkCommand`**: `public ICommand OkCommand`
  *Description:* Gets the OK command.
- **`CancelCommand`**: `public ICommand CancelCommand`
  *Description:* Gets the Cancel command.
- **`AddPathCommand`**: `public ICommand AddPathCommand`
  *Description:* Gets the Add Path command.
- **`SelectAllUserPathsCommand`**: `public ICommand SelectAllUserPathsCommand`
  *Description:* Gets the command to select all user paths.
- **`SelectNoneUserPathsCommand`**: `public ICommand SelectNoneUserPathsCommand`
  *Description:* Gets the command to deselect all user paths.
- **`SelectAllDefaultPathsCommand`**: `public ICommand SelectAllDefaultPathsCommand`
  *Description:* Gets the command to select all default paths.
- **`SelectNoneDefaultPathsCommand`**: `public ICommand SelectNoneDefaultPathsCommand`
  *Description:* Gets the command to deselect all default paths.

---
### Class: `SharedProgressViewModel`
**File:** [src/SyntheticShared/Shared/UI/SharedProgressViewModel.cs](../src/SyntheticShared/Shared/UI/SharedProgressViewModel.cs)

#### Methods
##### `?`
```csharp
public double ProgressPercentage => MaximumValue > 0.0 ? (CurrentValue / MaximumValue) * 100.0 : 0.0
```
Gets the calculated progress percentage (0 to 100).

##### `RelayCommand`
```csharp
public ICommand CancelCommand => _cancelCommand ??= new RelayCommand(_ => Cancel())
```
Gets the command executed to cancel the operation.

##### `SharedProgressViewModel`
```csharp
public SharedProgressViewModel(CancellationTokenSource cts)
```
Initializes a new instance of the `SharedProgressViewModel` class.

#### Fields
- **`ViewModelBase`**: `public class SharedProgressViewModel : ViewModelBase {`
  *Description:* ViewModel for universal progress tracking. Implements properties for task description, progress values, and cancel command.
- **`WindowTitle`**: `public string WindowTitle {`
  *Description:* Gets or sets the title of the progress window.
- **`MainTaskDescription`**: `public string MainTaskDescription {`
  *Description:* Gets or sets the main task description message.
- **`CurrentItemName`**: `public string CurrentItemName {`
  *Description:* Gets or sets the name of the current item being processed.
- **`MaximumValue`**: `public double MaximumValue {`
  *Description:* Gets or sets the total number of items to process.
- **`CurrentValue`**: `public double CurrentValue {`
  *Description:* Gets or sets the number of items processed so far.
- **`IsCancellationRequested`**: `public bool IsCancellationRequested {`
  *Description:* Gets or sets a value indicating whether cancellation has been requested.

---
### Class: `SingleItemSelectionDisplayConverter`
**File:** [src/SyntheticShared/Shared/UI/SingleItemSelectionDisplayConverter.cs](../src/SyntheticShared/Shared/UI/SingleItemSelectionDisplayConverter.cs)

#### Fields
- **`IMultiValueConverter`**: `public class SingleItemSelectionDisplayConverter : IMultiValueConverter {`
  *Description:* MultiValueConverter to dynamically resolve names of items for single-item selection. Expects two values: the item itself, and the parent DataContext implementing `ISingleItemSelectionViewModel`.

---
### Class: `SingleItemSelectionViewModel`
**File:** [src/SyntheticShared/Shared/UI/SingleItemSelectionViewModel.cs](../src/SyntheticShared/Shared/UI/SingleItemSelectionViewModel.cs)

#### Methods
##### `SingleItemSelectionViewModel`
```csharp
public SingleItemSelectionViewModel(IEnumerable<T> items, string prompt, Func<T, string> displayMemberPath)
```
Initializes a new instance of the <see cref="SingleItemSelectionViewModel{T}"/> class.

#### Fields
- **`ISingleItemSelectionViewModel`**: `public class SingleItemSelectionViewModel<T> : ViewModelBase, ISingleItemSelectionViewModel {`
  *Description:* Generic ViewModel for selecting a single item from a list.
- **`Title`**: `public string Title {`
  *Description:* Gets or sets the title of the selection window.
- **`Prompt`**: `public string Prompt {`
  *Description:* Gets or sets the instructions or prompt shown to the user.
- **`Items`**: `public ObservableCollection<T> Items`
  *Description:* Gets the collection of items to select from.
- **`SelectedItem`**: `public T? SelectedItem {`
  *Description:* Gets or sets the selected item.
- **`CloseAction`**: `public Action<bool>? CloseAction`
  *Description:* Gets or sets the action to close the window, passing a boolean dialog result.
- **`OkCommand`**: `public ICommand OkCommand`
  *Description:* Gets the OK command.
- **`CancelCommand`**: `public ICommand CancelCommand`
  *Description:* Gets the Cancel command.

---
### Class: `Win32WindowWrapper`
**File:** [src/SyntheticShared/Shared/UI/FileDialogHelper.cs](../src/SyntheticShared/Shared/UI/FileDialogHelper.cs)

#### Methods
##### `SelectFolder`
```csharp
public static string SelectFolder(IntPtr ownerHandle, string title, string? initialPath = null)
```
Displays a folder browser dialog parented to the specified window handle.

#### Fields
- **`FileDialogHelper`**: `public static class FileDialogHelper {`
  *Description:* Helper methods for showing Win32 file and folder dialogs with proper window parenting.

---
### Class: `WindowsFileDialogService`
**File:** [src/SyntheticShared/Shared/UI/WindowsFileDialogService.cs](../src/SyntheticShared/Shared/UI/WindowsFileDialogService.cs)

#### Methods
##### `SaveFileDialog`
```csharp
public string? SaveFileDialog(string filter, string title, string defaultFileName)
```
Displays the native Microsoft.Win32.SaveFileDialog.

##### `OpenFileDialog`
```csharp
public string? OpenFileDialog(string filter, string title, string defaultFileName)
```
Displays the native Microsoft.Win32.OpenFileDialog.

#### Fields
- **`IFileDialogService`**: `public class WindowsFileDialogService : IFileDialogService {`
  *Description:* Production implementation of IFileDialogService using Microsoft.Win32 file dialogs.

---
### Class: `WindowsGuardrailPromptService`
**File:** [src/SyntheticShared/Shared/UI/WindowsGuardrailPromptService.cs](../src/SyntheticShared/Shared/UI/WindowsGuardrailPromptService.cs)

#### Fields
- **`IGuardrailPromptService`**: `public class WindowsGuardrailPromptService : IGuardrailPromptService {`
  *Description:* Production implementation of IGuardrailPromptService using custom GuardrailPromptWindow.

---
### Class: `WindowsSummaryDisplayService`
**File:** [src/SyntheticShared/Shared/UI/WindowsSummaryDisplayService.cs](../src/SyntheticShared/Shared/UI/WindowsSummaryDisplayService.cs)

#### Methods
##### `ShowSummary`
```csharp
public void ShowSummary(object viewModel, IntPtr parentWindowHandle)
```
Displays the WPF ImportSummaryWindow dialog.

##### `ShowSummary`
```csharp
public void ShowSummary(object viewModel, IntPtr parentWindowHandle)
```
Does nothing.

#### Fields
- **`ISummaryDisplayService`**: `public class WindowsSummaryDisplayService : ISummaryDisplayService {`
  *Description:* Production implementation of ISummaryDisplayService displaying a WPF summary dialog.
- **`ISummaryDisplayService`**: `public class NoOpSummaryDisplayService : ISummaryDisplayService {`
  *Description:* No-op implementation of ISummaryDisplayService for headless testing contexts.

---
### Class: `WindowsUserPromptService`
**File:** [src/SyntheticShared/Shared/UI/WindowsUserPromptService.cs](../src/SyntheticShared/Shared/UI/WindowsUserPromptService.cs)

#### Methods
##### `ShowMessage`
```csharp
public void ShowMessage(string message, string title)
```
Displays an informational message box.

##### `ConfirmAction`
```csharp
public bool ConfirmAction(string message, string title)
```
Displays a confirmation message box with Yes/No options.

#### Fields
- **`IUserPromptService`**: `public class WindowsUserPromptService : IUserPromptService {`
  *Description:* Production implementation of IUserPromptService using WPF's MessageBox.

---
## Namespace: `Synthetic.Modules.StandardsManagement.Views`

### Class: `IntentToColorConverter`
**File:** [src/SyntheticShared/Modules/StandardsManagement/Views/IntentToColorConverter.cs](../src/SyntheticShared/Modules/StandardsManagement/Views/IntentToColorConverter.cs)

#### Fields
- **`IValueConverter`**: `public class IntentToColorConverter : IValueConverter {`
  *Description:* Converts a QueueIntent value to a themed SolidColorBrush defined in the SyntheticTheme resources.

---

## Namespace: `Synthetic.Modules.StandardsManagement.Utilities`

### Class: `FindReplaceService`
**File:** [src/SyntheticShared/Modules/StandardsManagement/Utilities/FindReplaceService.cs](../src/SyntheticShared/Modules/StandardsManagement/Utilities/FindReplaceService.cs)

#### Fields
- **`IFindReplaceService`**: `public class FindReplaceService : IFindReplaceService {`
  *Description:* Service that executes case-insensitive batch find and replace operations on ElementModels and ParameterModels.

---
### Class: `StandardsExportService`
**File:** [src/SyntheticShared/Modules/StandardsManagement/Utilities/StandardsExportService.cs](../src/SyntheticShared/Modules/StandardsManagement/Utilities/StandardsExportService.cs)

#### Methods
##### `StandardsExportService`
```csharp
public StandardsExportService(IGuardrailPromptService guardrailService, IFileDialogService dialogService)
```
Initializes a new instance of the `StandardsExportService` class.

##### `Export`
```csharp
public List<SerializationResultModel> Export( List<QueueItemModel> fileItems, string? targetPath, List<SerializationResultModel> dbResults, HashSet<string> protectedPaths, out string? finalPathUsed)
```
Exports the queued standards to a JSON file.

#### Fields
- **`IStandardsExportService`**: `public class StandardsExportService : IStandardsExportService {`
  *Description:* Service that encapsulates Phase 2 (File I/O) execution and protected file overwrite guardrails.

---

## Namespace: `Synthetic.Modules.StandardsManagement.Engine`

### Class: `StandardsExtractionOrchestrator`
**File:** [src/SyntheticShared/Modules/StandardsManagement/Engine/StandardsExtractionOrchestrator.cs](../src/SyntheticShared/Modules/StandardsManagement/Engine/StandardsExtractionOrchestrator.cs)

#### Methods
##### `StandardsExtractionOrchestrator`
```csharp
public StandardsExtractionOrchestrator(IIdentityService identityService)
```
Initializes a new instance of the `StandardsExtractionOrchestrator` class.

#### Fields
- **`IStandardsExtractionOrchestrator`**: `public class StandardsExtractionOrchestrator : IStandardsExtractionOrchestrator {`
  *Description:* Service responsible for managing the recursive extraction and dependency harvesting of Revit standards. Uses a Directed Acyclic Graph (DAG) pattern with a visited registry to avoid infinite recursion.

---
